# ต้นฉบับภาพความละเอียดเต็มที่สำรองไว้ในเครื่อง (บนเว็บใช้ตัวย่อ — รอบ 164/166)
# อยู่เป็นโฟลเดอร์ originals/ ข้างไฟล์จริงของแต่ละโซน เช่น img/gifts/originals/
**/originals/

# สำรองฐานข้อมูล/ผู้ใช้จาก server (รอบ 211 · tools/backup_db.sh)
# ⚠️ มีข้อมูลเด็ก (ชื่อเล่น/เซฟ) — เก็บในเครื่องเท่านั้น ห้ามขึ้น repo สาธารณะ
backups/

# ค็อกพิตเฮลิฯ ตัวเก่า (เลิกใช้รอบ 342 · ใช้ new_heli_cockpit.jpg แทน)
# เก็บไว้ในเครื่องอย่างเดียว ไม่ต้องขึ้น repo/deploy — 2.8MB
img/heli_cockpit.png
img/new_heli_cockpit.jpg

# ต้นฉบับโมเดลบ้านจาก Tripo 62MB (รอบ 431) — เกมใช้ house_01_lite.glb (1.1MB) ที่ลดโพลี/ย่อเทกซ์เจอร์แล้ว
# เก็บต้นฉบับไว้ในเครื่องอย่างเดียว ห้ามขึ้น repo/deploy
img/models/house_01.glb

# ต้นฉบับเฮลิลายพรางจากผู้ใช้ 2.3MB (รอบ 561) — เกมใช้ heli_ca_lite.glb (446KB) ที่ลดโพลี/ย่อเทกซ์เจอร์แล้ว
img/models/heli_ca.glb

# ต้นฉบับโมเดลผีจาก Tripo 54MB (รอบ 689) — เกมใช้ ghost_lite.glb (510KB) ที่ลดโพลี/ย่อเทกซ์เจอร์แล้ว
# เก็บต้นฉบับไว้ในเครื่องอย่างเดียว ห้ามขึ้น repo/deploy
img/models/ghost.glb

# 🔐 แพ็กเกจ Android + กุญแจ signing สำหรับ Play Store (รอบ 858) — ห้ามขึ้น repo สาธารณะเด็ดขาด
# (signing.keystore + รหัสใน signing-key-info.txt = ใครได้ไปปลอมแอปเราได้)
store/android/
