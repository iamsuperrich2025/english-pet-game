# 🧊 PROMPTS_MODELS_3D.md — เจนโมเดล 3D ตัวละครหน้า Lobby (.glb)

> **รอบ 104 (9 ก.ค. 2026):** หน้า lobby แสดงตัวละคร **3D จริง** — ขยับ idle (หายใจ/โยกเบาๆ) + **ปัดซ้าย-ขวาหมุนดูรอบตัว 360°**
> ระบบพร้อมแล้ว เหลือแค่คุณเอา **ไฟล์ .glb** มาวาง เกมจะสลับจาก PNG → 3D ให้เอง
> (ยังไม่วางไฟล์ = โชว์ภาพ PNG เดิม ไม่พัง)

---

## 📦 ต้องได้ไฟล์อะไรบ้าง (วางใน `img/models/`)

| ตัวละคร | ชื่อไฟล์ (ตรงเป๊ะ ตัวพิมพ์เล็ก) |
|---------|-------------------------------|
| ผู้เลี้ยงชาย | `caretaker_male.glb` |
| ผู้เลี้ยงหญิง | `caretaker_female.glb` |
| น้องหมา 🐶 | `pet_dog.glb` |
| น้องแมว 🐱 | `pet_cat.glb` |
| น้องมังกร 🐉 | `pet_dragon.glb` |

> ต้องมีครบ **"ผู้เลี้ยง + น้องของชนิดนั้น"** คู่ใดคู่หนึ่งถึงจะขึ้น 3D
> (เริ่มจากที่ใช้บ่อยก่อนก็ได้ เช่น `caretaker_male.glb` + `pet_dog.glb`)

**สเปกไฟล์:** `.glb` (binary glTF) · โมเดลยืนหันหน้า **+Z (หน้าเข้าหากล้อง)** · เท้าอยู่ที่พื้น (แกน Y ขึ้น) · ไม่ต้องมี animation ก็ได้ (เกมใส่ idle หายใจ/โยกให้เองอัตโนมัติ) · ขนาดไฟล์ควร < 5–8 MB/ตัว (มือถือลื่น)

---

## 🛠️ วิธีเจนโมเดล 3D จากภาพตัวละครเดิม (image-to-3D ฟรี)

ใช้ **ภาพตัวละครที่เราออกแบบไว้แล้ว** (`img/player_male.png`, `img/dog_adult_normal.png` ฯลฯ) เป็นต้นแบบ → อัปโหลดเข้าเครื่องมือ image-to-3D → ดาวน์โหลดเป็น `.glb`

**เครื่องมือแนะนำ (มีเครดิตฟรี · เลือกอันที่ถนัด):**
| เครื่องมือ | ลิงก์ | หมายเหตุ |
|-----------|-------|----------|
| **Meshy AI** | meshy.ai | image-to-3D ดี รองรับ export GLB · มีเครดิตฟรี/เดือน |
| **Tripo3D** | tripo3d.ai | เร็ว ฟรีเยอะ export GLB ได้ |
| **Luma Genie** | lumalabs.ai/genie | text/image-to-3D |
| **Rodin (Hyper3D)** | hyper3d.ai | คุณภาพสูง |

**ขั้นตอน (ตัวอย่าง Meshy/Tripo):**
1. เลือกโหมด **Image to 3D**
2. อัปโหลดภาพตัวละคร (ภาพเดียว พื้นหลังโปร่ง/ขาว ยิ่งดี — ใช้ไฟล์ใน `img/` ได้เลย)
3. ตั้งสไตล์ให้ตรงงานเรา (ถ้ามีช่อง prompt เสริม ใส่ตามด้านล่าง)
4. กด Generate → รอ → **Export / Download → เลือก `.glb`**
5. เปลี่ยนชื่อไฟล์ตามตาราง แล้ววางใน `img/models/`

**prompt เสริม (ถ้าเครื่องมือมีช่องให้พิมพ์ประกอบภาพ):**
```text
cute kawaii chibi character, clean stylized 3D game model, soft pastel colors,
big head, smooth low-poly friendly look, T/A-pose or standing idle, full body,
facing forward, game-ready, single character, no base, no text.
```

> ⚖️ **ลิขสิทธิ์:** ใช้ภาพออริจินอลของเราเจนเท่านั้น ห้ามก็อป/อ้างอิงตัวละครเกมจริง

---

## 🎬 อยากได้ท่า idle/เดินจริง (ตัวเลือกเสริม สำหรับผู้เลี้ยง)

โมเดลจาก image-to-3D ปกติเป็น **โมเดลนิ่ง (ไม่มีกระดูก)** — เกมใส่ **idle หายใจ+โยกเบาๆ ให้เองแล้ว** พอสวยงามระดับ COD lobby

ถ้าอยากให้ **ผู้เลี้ยง** มีท่าทางจริง (โบกมือ/ยืนขยับ):
1. เอา `caretaker_*.glb` (ที่เป็นคน) เข้า **Mixamo** (mixamo.com · ฟรี) → auto-rig → เลือกท่า **Idle / Breathing Idle / Waving** → download **glb** (with skin)
2. ตั้งชื่อคลิปให้มีคำว่า `idle` (เกมจะเล่นคลิปชื่อ idle/breath/stand ให้อัตโนมัติ) แล้ววางทับไฟล์เดิม
3. น้องสัตว์ (ไม่ใช่คน) Mixamo ไม่รองรับ → ใช้ idle procedural ของเกมพอ

---

## ✅ เสร็จแล้ว

1. วางไฟล์ `.glb` ครบใน `img/models/`
2. เปิด/refresh เกม → ตัวละครกลายเป็น **3D** ขยับ idle ได้ · **ลาก/ปัดซ้าย-ขวา = หมุนดูรอบตัว**
3. ระบบร่างยักษ์ (รอบ 103) ยังใช้ได้กับ 3D — ยิ่งอัพเกรด น้องยิ่งตัวใหญ่ ผู้เลี้ยงยืนแค่เข่า

> เริ่มจาก `caretaker_male.glb` + `pet_dog.glb` คู่เดียวก่อนเพื่อทดสอบก็ได้ครับ
