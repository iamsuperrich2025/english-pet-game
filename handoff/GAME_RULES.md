# GAME_RULES.md — กติกาเกมปัจจุบัน (state รุ่นใหม่)

> **🧍 มาตรฐานตัวละครผู้เล่น 3D (รอบ 1197):** ตัวละครผู้เล่นที่มองเห็นได้ทั้งของตนเอง/เพื่อน/multiplayer ในโลกใหม่และโลกเดิมที่ครอบคลุม ต้องใช้ **Soft Cuboid Chibi 3D** (น่ารัก แบบของเล่น ทรงเหลี่ยมนุ่ม chibi เป็นมิตรกับเด็ก และไม่คล้าย Roblox เกินไป) · **ยกเว้นโลกยานแม่และ Adventure World โหมดเดิม** ให้คงมาตรฐานเดิม · สเปกบังคับ ขอบเขต checklist ตรวจรับ และกฎ migration อยู่ที่ `docs/PLAYER_CHARACTER_STYLE.md` (source of truth)

> อ่านเมื่อ: แก้ระบบเกม (สัตว์/มื้ออาหาร/ความร้อน/บ้าน/บิล/แรงค์/โรงงาน/ตลาด/ออนไลน์/ชื่อ)

- `state.pets` = array (เลี้ยงหลายตัว ซื้อเพิ่มที่ร้านสัตว์ screen-select, จำกัดชนิดละ 1 ตัว), `state.active` = ตัวที่ดูแลอยู่, เลเวล/EXP/ชุด/ป่วย เก็บรายตัว, `state.owned` = ตู้เสื้อผ้ารวม
- **EXP โตช้าลง**: `expNeed(level)` = Lv.1→2 ต้อง 250 · Lv.2→3 ต้อง 600 · หลังจากนั้นเลเวลละ 400 (จับคู่ถูก +5 EXP, เมนูโปรด +15, สอบผ่านครั้งแรก +30)
- **🛡️ อาหารสัตว์ต้องปลอดภัยเท่านั้น (รอบ 1346)**: เมนูให้อาหารและร้าน Paws & Pantry ใช้ `foodSafeForPetMenu` กรองรายการที่เป็น `human:true` หรือมี `badFor` ออกจากสัตว์ทุกชนิดโดยไม่มีปุ่มฝืนคำเตือน; `feedWith` ปฏิเสธซ้ำก่อนหัก stock · ข้อมูลพิษเดิมคงไว้รองรับเซฟเก่า แต่สต็อกอันตรายเป็นข้อมูลเฉื่อย ไม่แสดง/ซื้อ/ป้อน/กินพื้นที่ชั้น
- **🦸 ตัวละครผู้เลี้ยง (ข้อ 4 รอบ 37)**: `state.playerAvatar` 'male'/'female' — บังคับเลือกตอนลงทะเบียน (`#reg-avatar` index.html + main.js) · เปลี่ยนได้ในตั้งค่า (แถว `#set-avatar` util.js) · โชว์ในแถบโปรไฟล์ (`playerAvatarHTML()` ui.js — มีภาพ `img/player_male/female.png` ใช้ภาพ ไม่มีใช้อีโมจิ 🦸‍♂️/🦸‍♀️) · **เฉพาะในเครื่อง ไม่ sync ขึ้น cloud profile** (ทำเมื่อไหร่ต้องแตะ rules) · ผู้เล่นเดิม migration = null ค่อยเลือกในตั้งค่า
- **🎫 การ์ดตั๋วโลกผจญภัย (ข้อ 7 รอบ 37)**: `state.advTicket` + `TICKET_PRICE` 5,000 (items.js) · การ์ด `#ticket-card` ในแผงร้านค้า (`renderTicketCard`/`buyTicket` ui.js) — **ล็อกจนกว่ามีสัตว์โตเต็มวัย (Lv.3) ≥1 ตัว** · ตั๋วเฉพาะตัว ขายต่อ/ส่งต่อไม่ได้ · นับใน assetValue ราคาเต็ม · โลก 3D (ข้อ 8) ยังไม่เปิด — การ์ดโชว์ "กำลังก่อสร้าง"
- **⚠️ sw.js v2 (fix รอบ 37)**: cache เฉพาะ `res.ok` — เดิม cache 404 ของรูปถาวร ทำให้ภาพที่เจนเพิ่มทีหลังไม่โผล่ให้ผู้เล่นเก่า · `CACHE_VERSION` v1→v2 ล้างของเสียทั้งหมด
- **💪 รูปร่างตามคุณภาพการกิน (ข้อ 5.2 รอบ 36)**: `p.shape` normal/fat/thin/strong · นับมื้อที่กินเต็มหลอด (`shapeMealDone` ครั้งเดียว/slot กันนับซ้ำผ่าน `p.shapeSlot` · `p.mealJunk`=มื้อนี้มีอาหารโทษ รีเซ็ตตอนขึ้นมื้อใหม่ใน careTick) — กินสะอาดติด `SHAPE_CLEAN_MEALS` 3 มื้อ→**strong** (EXP แถม `SHAPE_EXP_BONUS` +2/คำใน checkMatch) · มื้อมีของโทษติด 3→**fat** · ป่วยหิวติด `SHAPE_MISS_MEALS` 2 มื้อ→**thin** (นับใน careTick hunger sick) · ลำดับ thin>fat>strong (`petShapeOf`) · ภาพ `<pet>_adult_fat/thin/strong.png` (probe ใน petImageKeys · currentPetImg ลำดับ ป่วย>หิว>ดีใจ>**ร่าง**>ชุด>ปกติ ไม่มีภาพ=ตกไปปกติ) — prompt ใน `PROMPTS_CHARACTERS.md` · สถานะโชว์บนการ์ดสัตว์ (`.shape-text` + แถบความคืบหน้ากินดี x/3)
- **มื้ออาหาร + ชั้นตุนอาหาร**: หิวทุก 3 ชม. (slot 0,3,6,...,21 น. — `currentSlotStart` ปัดลงขอบ 3 ชม., `SLOT_MS`) — `p.fedUpTo` = slot ล่าสุดที่กินครอบคลุม; หิวเกิน 2 ชม.ไม่กิน → ป่วย (`HUNGRY_SICK_MS`); feast อิ่ม 6 ชม. = ข้ามมื้อถัดไป. การให้กินหัก 1 ชิ้นจาก `state.petPantry.stock` เท่านั้น **ไม่หักเหรียญตรงหน้าอาหาร**; ต้องซื้อชั้นเปล่า 30/75/160 ช่อง แล้วขับรถไปซื้ออาหารมาตุน. เมนู `chicken` คง id เดิมเพื่อรักษาสต็อกเซฟ แต่เปลี่ยนเป็น **อกไก่ต้มสุกไร้กระดูกเท่านั้น** เพราะกระดูกไก่อาจทิ่มลำไส้สัตว์ได้; ชั้นนับเป็นทรัพย์สินถาวรใน `assetValue`; อาหารตุนเป็นของสิ้นเปลืองไม่นับ
- **🚗🐾 โลกซื้อของสัตว์เลี้ยง**: ปุ่มใต้ปุ่มให้อาหารพาเข้าโลก first-person ตาม GPS ไปหน้าร้านอาหาร/แฟชั่น; **ทั้งทริปซื้ออาหารและแฟชั่นเปิดได้แม้น้องป่วยและไม่มีค่ารถ**. ถ้าไม่มีรถหรือรถส่วนตัวที่เลือกค้างค่างวด ระบบให้ยืม `car_01` ฟรีโดยไม่เพิ่มใน `state.cars` และไม่บล็อกการเข้าร้าน. ร้านแฟชั่น 3D และ `openDressUpBoard` ใช้แคตตาล็อกเดียวกัน: การ์ดใหญ่ลากซ้าย–ขวา, ลองใส่กับภาพน้องจริง และซื้อผ่านรหัส 6 หลัก; เพลงใช้วิทยุรถชุดเดียวกับโลกขับรถเดิม
- **🎁 เงินปรับตัวระบบใหม่**: migration รุ่น `PET_SHOPPING_GRANT_VERSION` เติม 10,000 เหรียญครั้งเดียวต่อเซฟ/บัญชีและเก็บ `petShoppingGrantNotice` ค้างจนกด “รับทราบ”; notice มีภาพ+เสียงและไม่ปิดด้วยการแตะฉากหลัง
- **ความร้อน**: ไม่มีบ้าน/บ้านไม่มีแอร์ → `p.heatFrom` นับสะสม ครบ 6 ชม.ป่วย (`HEAT_SICK_MS`) มีแถบร้อน; มังกร immune; ปลอดภัยเมื่อ `heatProtected()` = ปราสาท หรือบ้านกลาง+แอร์; ค่ารักษาป่วยทุกกรณี 1,000
- **ที่พัก**: เพิง 1,000 (กันฝนอย่างเดียว ติดแอร์ไม่ได้) · บ้านกลาง 100,000 (แอร์ 20,000+ติดตั้ง 5,000 = 25,000) · ปราสาท 1,000,000 (มีแอร์ในตัว); ย้ายบ้านแล้วแอร์ไม่ติดตามไป (`state.ac=false`)
- **ค่าบำรุงบ้าน**: 0.5% ของราคาบ้าน/เดือน (เพิง 5 · บ้าน 500 · ปราสาท 5,000) บิลออกทุกวันที่ 1 (`billTick` ใน careTick) เดือนแรกฟรี; ค้างจ่ายถึงวันที่ 5 → บ้านเสื่อม (`homeDecayed()` ภาพ `home_<id>_decayed.png`); ค้างข้ามเดือน → บ้านพัง (`state.pendingRuin` → ฉาก `showHomeRuined`) home=null; เซฟเก่ามีบ้าน → เริ่มบิลเดือนปัจจุบันฟรี
- **ค่าไฟ**: บิล id `elec` 0.5%/เดือน วันที่ 1 เดือนแรกฟรี; ค้างข้ามเดือน → **ถูกตัดไฟ** (`state.powerCut`, บิลค้างทบ) บ้านมืด (`home_<id>_dark.png`/ฟิลเตอร์) `heatProtected()` คืน false → สัตว์ร้อนป่วยทุก 6 ชม.แม้มีแอร์/ปราสาท (มังกร immune); จ่ายบิลค้างต้องซื้อ**หม้อแปลง 1,000** ก่อน (`state.transformerBought`, ผ่าน `payUtility('elec')`/`buyUtilityFix('elec')`) จ่ายครบ → ไฟกลับ เคลียร์ heatFrom; บ้านพัง(maint)→ล้างบิลสาธารณูปโภค+สถานะตัด; ซื้อ/ย้ายบ้าน→มิเตอร์ใหม่ฟรีเดือนแรก
- **ค่าน้ำ**: บิล id `water` 0.5%/เดือน (โครงเดียวกับค่าไฟ): ค้างสิ้นเดือน → **ตัดน้ำ** (`state.waterCut`) `home_<id>_nowater.png`; สัตว์**ทุกชนิดรวมมังกร** นับขาดน้ำ (`p.thirstFrom` แถบ `.thirst-fill`) ครบ 6 ชม. → ป่วย `cause:'thirst'` รักษา 1,000; ต้องจ่าย**ค่าติดตั้งระบบน้ำ 1,000** (`state.plumbingBought`) ก่อน; ภาพบ้านจัดลำดับ มืด(ไฟ) > แห้ง(น้ำ) > หม่น(ทรุด)
- **ค่าจัดการขยะ (ข้อ 13)**: บิล id `trash` 0.5%/เดือน วันที่ 1 เดือนแรกฟรี — **ไม่ตัด/ไม่พัง** ค้างข้ามเดือน→ปรับ +500 ทบใน `bills.trash.fine` (จ่ายครบ→รีเซ็ต 0); ไม่ผ่านเครื่อง UTILITIES (ไม่มี cutKey) ใช้ `trashBillUI()`/`payTrash()`
- **🏭 โรงงานผลิต & 🏪 ตลาด (แยกคนละแผง "รอบยี่สิบเก้า")**:
  - **โรงงาน (`#factory-card` = `renderFactoryCard`)**: เลือกสินค้าจากแคตตาล็อก 153 ชนิด (50 เดิม + เค้กชุด 2026 อีก 103 · dropdown กรอง 5 หมวด `factoryCat` · แถบปัดซ้ายขวา/ลูกศร) กด "ผลิต" → `state.producing={id,progress}` · **ตอบคำศัพท์ถูก 1 คำ = 1 แต้มผลิต** (`addCraft` hook ใน checkMatch + finishQuiz) ครบแต้ม → เข้า `state.collection` + `producedCount++` + ฉาก `showCollectReveal(id,null,true)` · เค้กในโรงงาน 107 แบบราคา 3,000–5,000; ภาพใหม่ใช้ thumbnail 256 แบบ lazy-load และ 512 เฉพาะฉากเปิด
  - **ตลาด (`#market-card` = `renderMarketCard`)**: `/market` เป็นตลาดรวมของผู้เล่นที่ล็อกอินทุกคน ไม่กรองตามเพื่อน; เจ้าของเห็นประกาศของตัวเองใน `renderMarketBrowse` พร้อมป้าย “โพสต์ขายแล้ว” แต่ซื้อเองไม่ได้ · **📦 ออเดอร์พิเศษ** (`orderTick` ค้างสูงสุด 2 · เข้าใหม่ทุก 2–4 ชม. · หมดอายุ 24 ชม. · จ่าย 1.3–1.8× · ยังไม่เคยผลิต→ได้ common เท่านั้น) มีของในคลัง→ปุ่ม "ส่งมอบ" (`deliverOrder`) · **🎁 คลังของฉัน** (`renderCollectMine`): ตั้งราคาขาย `openListDialog`/`listingStatus` · `marketTick` ลูกค้าจำลองซื้อตามเวลา · กล่องขายสำเร็จ `tradeSold`
  - `assetValue` นับของในคลัง+ลงขายด้วยราคาฐาน (ของกำลังผลิตยังไม่นับ)
- **🎁 ส่งของขวัญ (ข้อ 0.5)**: ส่งของขวัญร้าน (gifts.js ซื้อด้วยเหรียญ; หมวดเค้ก 125 แบบ = 22 เดิม + 103 ใหม่ ราคา 3,000–5,000) หรือ collectible จากคลังให้เพื่อน · escrow ตัดของ/หักเหรียญทันทีตอนส่ง · ผู้รับกดรับ→เข้า `state.giftBox` / ไม่รับ→คืนของ·เหรียญ · ค้าง 7 วัน→หมดอายุคืน · รอบ 1276 คืนส่วนต่างเฉพาะเค้กร้านที่ส่งให้เพื่อน (ไม่รวมโรงงาน/ตลาดผู้เล่น) ด้วย server ledger+campaign marker กันซ้ำ และเก็บ escrow ยอดเดิมของรายการ pending · **ของใน giftBox ขายต่อ/ส่งต่อไม่ได้ ไม่รวม assetValue** · ภาพจำนวนมากใช้ thumbnail 256 + lazy-load; display 512 เมื่อเปิดดูเท่านั้น
- **เหรียญ**: `addCoins()` อัปเดต `state.coins` + `state.daily.coins` (รีเซ็ตข้ามวัน `dailyTick()`) — dashboard 2 pill. สัตว์ป่วย **ไม่หยุดรายได้/การหาเหรียญ** (รวมเกม คอม และออนไลน์); ปิดเฉพาะ EXP/ความสามารถพิเศษและการให้อาหารจนรักษาหาย
- **แรงค์**: ยึด **net worth = เหรียญ + assetValue** (ทรัพย์สินคิดราคาเต็มที่ซื้อ รวมมือถือ/คอม; ราคาขายคืนใช้ตอนขายจริง → ซื้อแล้วแรงค์ไม่ตก); 7 แรงค์ (บรอนซ์ 0/ซิลเวอร์ 10k/โกลด์ 200k/แพลตินัม 1M/ไดมอนด์ 2M/มาสเตอร์ 4M/จอมพล 20M) ขั้นย่อย III→II→I; เลื่อน **แรงค์ใหญ่** แต่ละครั้งรับ 10,000 เหรียญครั้งเดียว (ขั้นย่อยไม่ได้) และเซฟเก่าได้ย้อนหลัง; ป้องกันซ้ำด้วย `state.rankRewardIdx` · กล่องยินดีค้างจนกดรับทราบ
- **Letter Cannon / Dragon Sky Siege**: คะแนนภารกิจที่จบหรือออกจากเกมสะสมใน `state.lcScore` → `/leaderboard.lc`; กระดาน Top 100 และตัดรางวัลรายเดือน Top 10 ผ่าน `/lcAward` ตามตาราง 10,000–1,000 เหรียญ
- **👻 ภารกิจพิเศษเดี่ยวโรงแรมผีสิง (รอบ 1354–1356)**: แยกจาก Daily Quest; รอบโรงแรมใหม่มี 5 คำและผู้เล่นต้องเก็บครบโดยไม่ GAME OVER จึงรับ 10,000 เหรียญครั้งเดียวต่อบัญชีทันที. ความคืบหน้าเป็นของแต่ละคน โดยนับคำให้เฉพาะผู้เล่นที่ชนะ canonical transaction ของตัวอักษรสุดท้ายของคำนั้นและตรวจว่าอักษรตรงกับคำ; คำที่เพื่อนเก็บไม่นับ. GAME OVER/ออกจากโรงแรมรีเซ็ตรอบส่วนตัว. กล่องยินดี, ป้ายเชิญ 2 login แรก และป้ายตอนเข้าโรงแรมค้างจนผู้เล่นกดปุ่มเอง—Escape/คลิกพื้นหลังห้ามปิด; ป้ายเข้าโรงแรมหยุดเกมและเริ่มเล่นเมื่อกด “รับทราบ”. HUD ไม่จับเวลา/ไม่มี “ไปหาเพื่อน”; ป้ายภารกิจอยู่แถวสองด้านขวา ส่วนคำเป้าหมายเป็นกล่องกะทัดรัดด้านบน ไม่บังกลางฉาก
- **รางวัลอันดับดีขึ้น**: 11 กระดานใน `RANK_MOVE_TOPICS` เก็บอันดับดีสุดแยกกระดานใน `state.rankMoveBest`; ทำลายสถิติเดิมทุก 1 ตำแหน่งรับ 1,000 เหรียญ; อันดับตกไม่หักเงิน/ไม่รีเซ็ตสถิติ จึงไต่กลับมารับซ้ำไม่ได้; snapshot ครั้งแรกเป็น baseline ไม่จ่ายย้อนหลัง · กล่องยินดีค้างจนกดรับทราบ
- **นาฬิกา**: `#clock-chip` โชว์ วัน·วันที่·เวลา (th-TH) เดินทุกวินาที (`renderClock`)
- **การ์ดเพื่อนออนไลน์** `#online-card`: ต่อ Firebase→ผู้เล่นจริง (`Online.friends` จาก `/presence`, onDisconnect ลบเอง, ผีค้าง 10 นาทีกรอง) / ออฟไลน์→เพื่อนจำลอง · **🏆 Leaderboard** `#leaderboard-card` Top 50 อันดับ**เหรียญคงเหลือ** (`state.coins`) จาก `/leaderboard` สด · แถวตัวเองไฮไลต์เหลือง+⭐ · คลิกชื่อ→การ์ดโชว์เงิน/จำนวนทรัพย์สิน/มูลค่าทรัพย์สิน (av/ni)
- **คำศัพท์ตามชั้น**: register ป.1–ม.6 → `gradeBand()` เลือก band; ใช้ `catsForStudent()`/`vocabForStudent()`
- เล่นเกม/สอบได้แม้ยังไม่มีสัตว์ (ได้เหรียญ+RP ไม่ได้ EXP) — เริ่มเกมเก็บ 3,000 ก่อนซื้อหมา/แมว
- **🔐 Google Login (ข้อ 0.1)**: เข้าเกมต้อง login (แบบ ก. บังคับ) `#screen-login` · เซฟจริง `/users/<uid>/save={data:JSON, at}` — localStorage เป็น cache เทียบ `state.savedAt` vs `cloud.at` · `state.ownerUid`=เจ้าของ (null=ยุคก่อน login ถามผูก 2 ชั้น · บัญชีอื่น=เริ่มใหม่เงียบ) · push ทุก 1 นาที+beforeunload+จุดสำคัญ · logout=push→signOut→ล้าง cache→reload · **ปุ่มรีเซ็ตถูกถอด** (รีเซ็ตทาง console เท่านั้น) · login จริงเฉพาะ http/https · presence/leaderboard ใช้ **uid** เป็น key (`onlineKey()`)
- **📛 ชื่อในเกม (ข้อ 0.2)**: `state.profileName` ตั้งตอนลงทะเบียน (`#reg-nick`) · ผู้เล่นเดิมไม่มีชื่อ→กล่องบังคับ `authAskProfileName()` (ปิดข้ามไม่ได้) · ตรวจ `checkName(raw,2,20)` · เก็บสำเนาสาธารณะ `/users/<uid>/profile/name` (`authPushProfile()`) · `onlineDisplayName()` ใช้ profileName ก่อน · ชื่อจาก DB ทุกจุด `escapeHTML()` ก่อนแทรก innerHTML
- **📛 ชื่อสัตว์ (ข้อ 7)**: `p.name` บังคับตั้งตอนซื้อ (ยกเลิก=ไม่ซื้อ) · ปุ่ม ✏️ เปลี่ยนภายหลัง · `checkName(raw,1,15)` · migration ตัวเดิม=ชื่อชนิด
- **ล็อกร้านค้า**: ไม่มีสัตว์ หรือ active เป็นไข่/แรกเกิด (Lv.1) → ร้านไอเทม lock-banner
