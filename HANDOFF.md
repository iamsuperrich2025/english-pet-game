# HANDOFF.md — แม่แบบส่งต่องานสำหรับ session ใหม่

> ไฟล์นี้คือบริบททั้งหมดของโปรเจกต์ อ่านจบแล้วทำงานต่อได้ทันที
> **✅ ปิดจบแล้ว (รอบยี่สิบเจ็ด · 6 ก.ค. 2026): เรื่อง "แถบ URL / status bar กินพื้นที่จอมือถือ"** → manifest เป็น `fullscreen` (ซ่อน status bar Android) + ปุ่ม 📲 ติดตั้งแอพในเกม + ระบบแจ้งเวอร์ชันใหม่ · **ผู้ใช้ยืนยัน fullscreen ซ่อนแถบสำเร็จแล้ว** (รายละเอียดในบล็อก "รอบยี่สิบเจ็ด" ด้านล่าง + `NOTE_URLBAR_FULLSCREEN.md`)
>
> **สถานะล่าสุด (6 ก.ค. 2026 · รอบยี่สิบเจ็ด):** ✅ **จอเต็ม fullscreen + ปุ่ม 📲 ติดตั้งแอพในเกม + ระบบแจ้งเวอร์ชันใหม่ (commit `65612b5`) + `.nojekyll` (commit `8cf2031`) — push+deploy แล้ว · ผู้ใช้ยืนยัน fullscreen ซ่อน status bar สำเร็จ** (ดูบล็อก "รอบยี่สิบเจ็ด" ในหัวข้อแผน) · (รอบยี่สิบหก) ✅ **PWA ติดตั้งเป็นแอพบนมือถือได้ (commit 31d16d8)** + ✅ บีบหน้า login พอดีจอแนวนอน (commit d44bd2c) — manifest.json + sw.js (network-first โค้ด/cache-first รูป/ข้าม Firebase) + ไอคอนแอพ 4 ไฟล์ (มาสคอตหมายิ้ม) · ทดสอบ preview SW active+แคช 26 ไฟล์ ไม่มี error · ⬜ เหลือผู้ใช้ลอง "เพิ่มลงหน้าจอโฮม" บนมือถือจริง · (ก่อนหน้า · รอบยี่สิบห้า) ✅ **คลิกชื่อผู้เล่น (การ์ดเพื่อน+กระดาน) → การ์ดโชว์เงิน/จำนวนทรัพย์สิน/มูลค่าทรัพย์สิน แยกกัน (commit 0daa179)** — leaderboard เพิ่มฟิลด์ av (มูลค่าทรัพย์สิน)+ni (จำนวนชิ้น) · state.assetCount() · online.fetchPlayerStats() · ui.showPlayerCard()/.pl-click · **⚠️ ต้องอัปเดต rules เพิ่ม av/ni ในโซน /leaderboard (ก้อน rules อัปเดตแล้วในไฟล์นี้) — ยังไม่ publish av/ni จะไม่ถูกบันทึก (มี fallback กัน leaderboard พัง)** · (ก่อนหน้า) ✅ **ซ่อมเพื่อนให้ครบสองฝ่ายอัตโนมัติ self-heal (commit 9748206)** — friendsHeal() ใน online.js เขียนยืนยันฝั่งตรงข้ามให้ครบทั้งคู่ ทดสอบ fake db 7 เคสผ่าน · (ก่อนหน้า) ✅ แก้ค้นหาเพื่อนขึ้นชื่อจริง (commit 84c4894) + ✅ แจ้งเตือนข้อความแชทใหม่ badge/toast/ปุ่ม "ใหม่!" (commit 9f3ec4b) · ✅ ผู้ใช้ publish rules โซน /chats แล้ว 🎉 · **🎯 งานถัดไป: ข้อ 0.5 ส่งของขวัญ (มีคำถามค้างต้องถามก่อนทำ) · ⚠️ ยังค้างทดสอบแชท+self-heal จริง 2 บัญชีบน Pages** · (ประวัติ) ✅ **ข้อ 0.4 แชทกับเพื่อน + emoji เสร็จ ("รอบยี่สิบสอง")** — chat engine ใน online.js + กล่องแชท `openChat` + emoji picker 44 ตัว + ปุ่ม 💬 ในแถวเพื่อน · กรองคำหยาบ `nameHasBadWord` ≤200 ตัว · เก็บ 100 ข้อความล่าสุด/คู่ · **⚠️ ค้าง: ผู้ใช้ต้องวาง rules ใหม่ (เพิ่มโซน `/chats`) + ทดสอบจริงบน Pages 2 บัญชี** — 🎯 งานถัดไป: ข้อ 0.5 ส่งของขวัญ · (ประวัติเก่า) ✅ **ข้อ 0.1 Google Login + sync เซฟ cloud ปิดงานสมบูรณ์ ("รอบสิบห้า") 🎉** — บังคับ login แบบ ก. · rules โซน `/users` วางแล้ว (REST ผ่าน 15 เคส) · ผู้ใช้ทดสอบ login จริงบน Pages ผ่านครบ · ✅ รอบสิบหก: ถอดปุ่มรีเซ็ตอันตราย + แคตตาล็อกโรงงานแบ่งหน้า 5 รายการปัดซ้ายขวา — ✅ **รอบสิบแปด (6 ก.ค. 2026): ข้อ 0.2 display name เสร็จ** — js/data/badwords.js (ตัวกรองชื่อ+คำหยาบ แชร์กับข้อ 7) + ตั้งชื่อในเกมตอนลงทะเบียน/กล่องบังคับตั้งชื่อผู้เล่นเดิม + presence/leaderboard เปลี่ยน key เป็น uid + escape ชื่อจาก DB — ทดสอบ preview ผ่านครบ **⚠️ ค้าง: ผู้ใช้ต้องวาง rules ฉบับใหม่ (อยู่ท้ายรอบสิบแปด) + ทดสอบจริงบน Pages** — ✅ **รอบสิบเก้า: ข้อ 7 ตั้งชื่อสัตว์ + ✏️ แก้ชื่อในเกม + 📛 บนแถบโปรไฟล์** · ✅ **รอบยี่สิบ: เอฟเฟกต์ฝนเต็มจอ** · ✅ **รอบยี่สิบเอ็ด: ข้อ 0.3 ค้นหา id + เพิ่มเพื่อน (แผง 👥) — ผู้ใช้วาง rules ชุดใหม่แล้ว** · ✅ ฝนภาพจริง (ผู้ใช้ผ่าน) + เพิ่มระดับชั้น ป.ตรี ฯลฯ — **🎯 session ถัดไป: อ่านหัวข้อ "📌 แผนสำหรับ session ถัดไป" ด้านบนสุดก่อนเลย** (งานถัดไป: ข้อ 0.4 แชท → 0.5 ของขวัญ) (สเปกใหม่ดูข้อ 0.5 · ภาพ 38/50) — **งานขนาน:** ข้อ 9 คำศัพท์ให้ Sonnet 5 ทำ session แยกตาม `TASK_VOCAB_SONNET.md` (ยังไม่ได้รัน) · ธีมเมืองโทนฟ้าภาพครบ 6/6 พร้อมทำ session แยก · 🆕 อัพเดท 5725691326 บันทึกครบแล้ว (0.5 แก้สเปก · ข้อ 7/8/9 ใหม่) · ✅ **อัพเดท 5725691826 เสร็จแล้ว ("รอบสิบเจ็ด" 5 ก.ค. ดึก): ยกเครื่อง UI ทั้งเกมเป็น Lobby แนวนอนธีมเมืองโทนฟ้า + การ์ดสินค้าสไตล์ Trade HQ — ทดสอบผ่านครบ commit+push แล้ว (เหลือผู้ใช้ลองบนมือถือจริง)** · ⚠️⚠️ **ระวังสุดๆ: "รอบสิบเจ็ด" มี hotfix ด่วน — commit 9accb89 เคยทำเกมพังทั้งเว็บ (เข้าเกมไม่ได้เลย) เพราะเผลอติด script tag `js/data/vocab/band1-5.js` ที่ยังไม่มีไฟล์จริงขึ้น production (งานคำศัพท์ Sonnet ทำค้างครึ่งทาง — มีแค่ band1.js ยังไม่ commit) — ห้ามแก้ index.html ชี้ vocab/ จนกว่างานคำศัพท์เสร็จครบทุกไฟล์และ commit พร้อมกันในครั้งเดียว** · 🆕 **Project Dictionary A–Z จดไว้แล้ว (backlog ข้อ 10 + `TASK_DICTIONARY_SONNET.md`) — ผู้ใช้ให้ Fable/Opus เลือกจังหวะทำเอง** · ✅ **ภาพของขวัญครบ 50/50 แล้ว** (ผู้ใช้วางการ์ดเทศกาล 12 ไฟล์สุดท้าย — ข้อ 0.5 พร้อมด้านภาพ)

## 📌 แผนสำหรับ session ถัดไป (Fable เขียนไว้ตอนใกล้เต็ม limit — 6 ก.ค. 2026 · Opus อ่านหัวข้อนี้ก่อนหัวข้ออื่น)

**✅ รอบยี่สิบเจ็ด (6 ก.ค. 2026): จอเต็ม fullscreen + ปุ่มติดตั้งแอพในเกม + ระบบแจ้งเวอร์ชันใหม่ — push+deploy แล้ว (commit `65612b5`, `8cf2031`) · ผู้ใช้ยืนยัน fullscreen ซ่อน status bar สำเร็จ 🎉**
- **ที่มา:** ต่อจากรอบยี่สิบหก (PWA standalone → แถบ URL หายแล้ว) ผู้ใช้ยังติดใจว่า **status bar Android (นาฬิกา/แบต/สัญญาณ) ด้านบนยังกินพื้นที่จอแนวนอนเตี้ย**
- **สิ่งที่ทำ 3 อย่าง:**
  1. **`manifest.json`: `display` `standalone` → `fullscreen`** (+ `"display_override":["fullscreen","standalone"]` เผื่อ fallback) → ตอนเปิดจากแอพที่ติดตั้ง ซ่อน status bar = เต็มจอสุด · **⚠️ แอพที่ติดตั้งไว้แล้วต้องถอน+ติดตั้งใหม่ 1 ครั้ง** ถึงเปลี่ยนโหมด (Android ล็อก display mode ตอนติดตั้ง)
  2. **ปุ่ม 📲 ติดตั้งแอพในเกม** (สำหรับคนใช้ไม่เป็น) — 2 จุด: `#btn-install` (ปุ่มเขียวในการ์ด login) + `#btn-install-top` (icon-btn ในหัว dashboard ข้าง 🔊/🚪) · โผล่เฉพาะตอนติดตั้งได้: Android ดัก `beforeinstallprompt` (กด=เด้ง prompt จริง) · iOS โชว์เสมอ กดแล้วเด้ง overlay สอน "แชร์→Add to Home Screen" · เปิดจากแอพติดตั้งแล้ว (standalone/fullscreen)=ไม่โชว์ · `appinstalled`=ซ่อน · โค้ดสคริปต์ท้าย `index.html` · CSS `.install-btn`/`.install-guide-*` (style.css) + squeeze จอเตี้ย (lobby.css)
  3. **ระบบแจ้งเวอร์ชันใหม่** — `version.json` (`{"v":"..."}`) + สคริปต์ใน `index.html` (ใต้ SW register) เทียบเวอร์ชัน server กับที่รันอยู่ (เช็ก load+ทุก 3 นาที+focus/online · no-store) → ต่าง=เด้งแถบ `.update-banner` "✨ มีเกมเวอร์ชันใหม่! 🔄 อัปเดตเลย" กด=`location.reload()` (network-first ดึงโค้ดใหม่) · CSS `.update-banner` (style.css)
- **➕ เพิ่ม `.nojekyll`** (commit `8cf2031`) — ให้ GitHub Pages ข้าม Jekyll เสิร์ฟไฟล์ตรงๆ (build เร็วขึ้น+กันพลาด)
- 🔑 **ขั้นตอน deploy สำคัญ (จำให้ขึ้นใจ): ทุกครั้งที่ push โค้ดใหม่ ให้บัมพ์เลข `v` ใน `version.json`** (เช่น `2026-07-06.1`→`.2`) ผู้ใช้ที่เปิดค้างถึงจะเห็นแถบ "มีเวอร์ชันใหม่" · ถ้าลืมก็ไม่พัง (network-first ยังให้โค้ดใหม่ตอนเปิดรอบหน้า) แต่จะไม่เตือนทันที
- ⚠️ **บทเรียน deploy:** GitHub Pages build หน่วง ~2–5 นาทีหลัง push — **อย่าเพิ่งทดสอบ/ติดตั้งบนมือถือทันที** (รอบนี้ผู้ใช้ถอน+ติดตั้งใหม่ตอน build ยังไม่เสร็จ เลยได้ manifest เก่า standalone มาอีก) · วิธีเช็คว่า deploy เสร็จ: `curl` ดู `version.json`/`manifest.json` บน `iamsuperrich2025.github.io` ว่าขึ้นค่าใหม่หรือยัง
- **เรื่องอัปเดตระยะยาว:** โค้ด/เนื้อหาปกติ (ฟีเจอร์/บั๊ก/คำศัพท์) ผู้ใช้ติดตั้งแล้วได้อัตโนมัติ (network-first) ไม่ต้องติดตั้งใหม่ · ต้องติดตั้งใหม่เฉพาะแก้ **manifest** (display mode/ไอคอน/ชื่อ) เท่านั้น
- **ไฟล์ที่แตะ:** `manifest.json` · `index.html` · `css/style.css` · `css/lobby.css` · `version.json` (ใหม่) · `.nojekyll` (ใหม่) · `NOTE_URLBAR_FULLSCREEN.md`
- ⬜ **ยังค้าง (เล็กน้อย):** ปุ่ม 📲 บนเครื่องผู้ใช้เองไม่โชว์ (เพราะติดตั้งแล้ว = ถูกต้อง) — ยังไม่ได้เห็นปุ่มจริงบนเครื่องที่ยังไม่ติดตั้ง/แถบ update จริง (จะเห็นรอบ deploy ถัดไปที่บัมพ์ version.json)

**สถานะ (อัปเดต 6 ก.ค. 2026 · รอบยี่สิบสาม):** ข้อ 0.2 ✅ · ข้อ 7 ✅ · **ข้อ 0.3 (เพิ่มเพื่อน) ✅ "รอบยี่สิบเอ็ด"** · **ข้อ 0.4 (แชท + emoji) ✅ "รอบยี่สิบสอง"** · **🆕 รอบยี่สิบสาม (6 ก.ค. 2026):** (1) ✅ แก้ค้นหาเพื่อนขึ้น "ผู้เล่น ชั้น " (ชื่อ/ชั้นว่าง) — friendSearch อ่านชื่อจาก presence+leaderboard (เลือกอันมีชื่อ) + main.js ลงทะเบียนเสร็จ push presence/leaderboard ทันที (commit `84c4894`) (2) ✅ แจ้งเตือนข้อความแชทใหม่แม้ปิดกล่องแชท — badge ปุ่มเพื่อน + toast + เสียง + ปุ่มเขียว "ใหม่!" (chatWatchSync/chatMarkSeen/Online.chatUnread/state.chatSeen — commit `9f3ec4b`) · **✅ ผู้ใช้ publish Security Rules โซน `/chats` เรียบร้อยแล้ว** (แชทเขียนขึ้น server ได้จริง — publish ได้หลังกด spacebar 1 ครั้งให้ console เห็นว่ามีการแก้) · **⚠️ ยังค้าง 2 อย่าง: (ก) implement self-heal เพื่อนครบสองฝ่าย (งานถัดไป ดูด้านล่าง) (ข) ทดสอบแชทจริงบน Pages 2 บัญชี**

**✅ ซ่อมเพื่อนให้ครบสองฝ่ายอัตโนมัติ (self-heal mutual friendship) — เสร็จ "รอบยี่สิบสี่" (6 ก.ค. 2026 · commit `9748206`)**
- **ปัญหาที่เจอจริง (2 บัญชี hulk↔ครูรุต):** ข้อมูลเพื่อน "ไม่ครบสองฝ่าย" (A มี B ในรายชื่อเพื่อน แต่ B ไม่มี A) ค้างมาจากช่วง rules `/chats` ยังไม่ถูก publish → ฝั่งที่หายไปไม่มีทั้ง**ปุ่มเปิดแชท**และ**ตัวเฝ้าข้อความ** (chatWatchSync วนเฉพาะ `Online.myFriends`) จึงไม่เห็น/ไม่ถูกเตือนเลย
- **สิ่งที่ทำจริง:** online.js เพิ่ม `Online.friendsHealed:{}` + ฟังก์ชัน `friendsHeal()` (วางหลัง `friendDecline`) เรียกในตัวฟัง `/friends/<id>` (หลังตั้ง `Online.myFriends` ก่อน chatWatchSync) · วน myFriends เขียนยืนยันฝั่งตรงข้าม `friends/<f.uid>/<onlineKey()>` = {n,g,ts} — **rules อนุญาต** เพราะ `auth.uid === $friendUid` (ตัวเรา) · เขียนเฉพาะตอน `Online.ready && state.student && onlineDisplayName()` (กัน validate ล้ม) · ข้ามถ้า `f.uid===me` · throttle ครั้งเดียว/uid/เซสชันด้วย `friendsHealed` · เขียนล้ม `.catch` ปลดล็อก flag ให้ลองใหม่รอบหน้า
- **ทดสอบ preview (fake db spy) ผ่านครบ:** T1 เรียกครั้งแรก→เขียน 2 ฝั่ง path+data+ts ถูก · T2 เรียกซ้ำ→ไม่เขียน (throttle) · T3 เพื่อนใหม่→เขียนเฉพาะคนใหม่ · T4 ไม่มีชื่อ/ไม่มี student→ไม่เขียน · T5 ยังไม่ ready→ไม่เขียน · T6 เพื่อน uid=ตัวเอง→ข้าม · T7 เขียนล้ม→healed ปลดล็อกกลับว่าง · ไม่มี console error
- **⚠️ ยังค้าง: ทดสอบแชทจริงบน Pages 2 บัญชี** (hulk กด "รับ" คำขอค้างก่อน แล้วลองส่งข้อความ → ครูรุตต้องเด้ง toast+badge+"ใหม่!" · self-heal ควรทำให้ทั้งคู่เห็นปุ่มแชท+ถูกเตือนครบ)

**✅ การ์ดข้อมูลผู้เล่น (คลิกชื่อ) — เสร็จ "รอบยี่สิบห้า" (6 ก.ค. 2026 · commit `0daa179`)**
- **สเปกผู้ใช้:** คลิกชื่อในการ์ดเพื่อนออนไลน์ (#online-card) และกระดาน (#leaderboard-card) → การ์ดโชว์ **เงินรวม · จำนวนทรัพย์สิน (กี่ชิ้น) · มูลค่าทรัพย์สินรวม** แยกกัน (ไม่รวมยอด net worth) เพื่อเป็นแรงบันดาลใจ
- **สิ่งที่ทำจริง:** state.js `assetCount()` (นับชิ้น: pets+home+ac+owned+phone+computer+farm+collection+listings) · online.js `onlinePushScore` เพิ่มเขียน `av`(=Math.round(assetValue())) + `ni`(=assetCount()) ขึ้น `/leaderboard` · เปลี่ยน throttle `lastCoins`→`lastScoreSig`(coins|av|ni) · **fallback:** ถ้า set av/ni ถูก reject (rules ยังไม่อัปเดต) เขียนเวอร์ชันเดิม {n,g,coins,at} แทน กัน leaderboard พัง · `fetchPlayerStats(uid)` (ตัวเอง=ค่าสดจาก state · คนอื่น=อ่าน `/leaderboard/<uid>` สดตอนคลิก) · leaderboard listener เก็บ av/ni ด้วย · ui.js `.pl-click` ชื่อคลิกได้ทุกการ์ด (แถวเรา+เพื่อนจริง+กระดาน · โหมดจำลองออฟไลน์คลิกได้เฉพาะแถวเรา) + `bindPlayerClicks()` delegated listener ผูกครั้งเดียว + `showPlayerCard()` overlay · lobby.css `.pl-overlay/.pl-card/.pl-stat/.pl-gold` ธีมฟ้า-ทอง · av/ni ที่ไม่มี (ผู้เล่นก่อนอัปเดต) โชว์ "—"
- **ทดสอบ preview (mock login + fake db) ผ่านครบ:** คลิกตัวเอง→ค่าสด (12,345/6ชิ้น/42,500)+"⭐นี่คือหนูเอง" · คลิก hulk (กระดาน)→อ่าน leaderboard (99,999/20ชิ้น/500,000) · ผู้เล่นไม่มี av/ni→"—" · uid ไม่พบใน db→"ยังไม่มีข้อมูล" · onlinePushScore เขียน av/ni ครบ+throttle ไม่เขียนซ้ำ · fallback ตอน reject→เขียน {n,g,coins,at} · หัวการ์ด/ค่าทองสี gold-d ถูก · ปิด overlay ได้ · ไม่มี console error
- **⚠️ ต้องทำฝั่งผู้ใช้:** **publish rules ใหม่** (ก้อน rules ในไฟล์นี้เพิ่ม `av`/`ni` ใน `/leaderboard` แล้ว) — ถ้ายังไม่ publish, av/ni จะถูก reject → การ์ดคนอื่นโชว์ "—" (แต่ coins ยังทำงานผ่าน fallback)

**✅ PWA ติดตั้งเป็นแอพบนมือถือ — เสร็จ "รอบยี่สิบหก" (6 ก.ค. 2026 · commit `31d16d8`)**
- **ผู้ใช้เลือก:** ทำ PWA ก่อน (แนะนำ) — ติดตั้งหน้าจอโฮมได้ทันที ไม่ต้องขึ้นสโตร์ · (ทางเลือกถัดไปถ้าอยากได้สโตร์จริง: Google Play ผ่าน Capacitor/Bubblewrap — ต้องลง Node+Android Studio · iOS ต้องมี Mac)
- **ไฟล์ใหม่:** `manifest.json` (standalone · orientation landscape · theme #123a6b · bg #0a1f3c · 3 icons) · `sw.js` (service worker: **network-first** สำหรับ HTML/JS/CSS = ออนไลน์ได้โค้ดใหม่เสมอ กันโค้ดค้าง · **cache-first** สำหรับรูป · ข้ามคำขอข้ามโดเมน = Firebase/Google login/ฟอนต์ ไม่ยุ่ง · CACHE_VERSION='pet-vocab-v1' bump เมื่อแก้ shell list · skipWaiting+clients.claim) · `img/icons/` 4 ไฟล์ (192/512/maskable-512/apple-touch-180 — มาสคอต dog_adult_happy บนพื้นไล่เฉดฟ้า สร้างด้วย Pillow)
- **index.html:** เพิ่ม `<link rel=manifest>` + meta theme-color/apple-mobile-web-app/apple-touch-icon + viewport-fit=cover + สคริปต์ลงทะเบียน SW (เฉพาะ http/https · file:// ข้าม)
- **ทดสอบ preview ผ่าน:** manifest โหลดได้ (standalone/landscape/3 icons) · SW state=active + คุมหน้าเว็บ (controlled) · แคช `pet-vocab-v1` 26 ไฟล์ (shell+icons) · เกมเปิดหน้า login ปกติ ไม่มี console error · icon-512 เข้าถึงได้ 200
- **หมายเหตุอัปเดตโค้ด:** network-first ทำให้ผู้ใช้ออนไลน์ได้โค้ดใหม่อัตโนมัติ (ไม่ต้อง bump version) — bump `CACHE_VERSION` เฉพาะตอนแก้รายการ SHELL ใน sw.js · **แก้ไอคอน:** รันสคริปต์ Pillow ใหม่ (dog_adult_happy → gradient ฟ้า scale 0.78 ปกติ / 0.62 maskable)
- **⬜ เหลือทดสอบจริงบนมือถือ:** เปิดลิงก์ Pages บนมือถือ → เมนู "เพิ่มลงหน้าจอโฮม/Install" → เปิดจากไอคอนได้เต็มจอเหมือนแอพ (Android เด้ง prompt ติดตั้งเอง · iOS ต้อง Safari → แชร์ → Add to Home Screen)

**🎯 งานถัดไป = ข้อ 0.5 ส่งของขวัญ** (สเปก backlog ข้อ 0.5 · **มีคำถามค้างต้องถามผู้ใช้ก่อนทำ:** collectible ที่ถูกปฏิเสธคืนคลังผู้ส่งไหม + ค้าง "ยังไม่มีผู้รับ" นานๆ หมดอายุคืนของไหม) · **ทางเลือกเสริม: ยกระดับ PWA → Google Play (Capacitor)**

**งานเรียงลำดับ:**
1. **✅ ข้อ 0.4 แชทกับเพื่อน (มี emoji) — เสร็จ "รอบยี่สิบสอง" (6 ก.ค. 2026):** online.js เพิ่ม chat engine (`chatPairId`=uid 2 ตัวเรียง alphabet ต่อด้วย `_` · `chatRef`/`chatListen`(limitToLast 100 real-time)/`chatSend`(กรอง `nameHasBadWord`+≤200 ตัว)/`chatPrune`(ตัดเหลือ 100 ล่าสุดตอนส่ง)) · ui.js `openChat(friend)` กล่องแชทลอย + emoji picker 44 ตัว (`CHAT_EMOJIS`) + ปุ่ม `.fr-chat-btn` 💬 ในแถวเพื่อน (`refreshFriendData`) · lobby.css `.chat-*`/`.fr-chat-btn` · **⚠️ ค้างฝั่งผู้ใช้: วาง rules ใหม่ (มีโซน `/chats` แล้ว ดูก้อนด้านล่าง) + ทดสอบแชทจริงบน Pages 2 บัญชี** · ทดสอบ preview ผ่านครบ (fake in-memory Firebase — ดูรอบยี่สิบสองท้ายไฟล์)
2. **ข้อ 0.5 ส่งของขวัญ (งานถัดไป)** — สเปก backlog ข้อ 0.5 · **มีคำถามค้างต้องถามผู้ใช้ก่อนทำ**: collectible ที่ถูกปฏิเสธคืนคลังไหม + ค้าง "ยังไม่มีผู้รับ" นานๆ หมดอายุไหม

**Rules (ครอบ 0.1+0.2+0.3+0.4 — มีโซน `/chats` · ✅ ผู้ใช้ publish แล้ว "รอบยี่สิบสาม" · เก็บไว้อ้างอิง/เผื่อวางใหม่ · self-heal ไม่ต้องแก้ rules ชุดนี้):**
```json
{
  "rules": {
    "presence": {
      ".read": true,
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['n','g','act','at'])",
        "n":   { ".validate": "newData.isString() && newData.val().length >= 1 && newData.val().length <= 40" },
        "g":   { ".validate": "newData.isString() && newData.val().length <= 8" },
        "act": { ".validate": "newData.isString() && newData.val().length <= 60" },
        "at":  { ".validate": "newData.isNumber()" },
        "$other": { ".validate": false }
      }
    },
    "leaderboard": {
      ".read": true,
      ".indexOn": "coins",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['n','g','coins','at'])",
        "n":     { ".validate": "newData.isString() && newData.val().length >= 1 && newData.val().length <= 40" },
        "g":     { ".validate": "newData.isString() && newData.val().length <= 8" },
        "coins": { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "av":    { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "ni":    { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "at":    { ".validate": "newData.isNumber()" },
        "$other": { ".validate": false }
      }
    },
    "friendCodes": {
      "$code": {
        ".read": true,
        ".write": "auth != null && newData.val() === auth.uid",
        ".validate": "newData.isString()"
      }
    },
    "friendReq": {
      "$toUid": {
        ".read": "auth != null && auth.uid === $toUid",
        "$fromUid": {
          ".write": "auth != null && (auth.uid === $fromUid || auth.uid === $toUid)",
          ".validate": "newData.hasChildren(['n','g','ts'])",
          "n":  { ".validate": "newData.isString() && newData.val().length <= 40" },
          "g":  { ".validate": "newData.isString() && newData.val().length <= 8" },
          "ts": { ".validate": "newData.isNumber()" },
          "$other": { ".validate": false }
        }
      }
    },
    "friends": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        "$friendUid": {
          ".write": "auth != null && (auth.uid === $uid || auth.uid === $friendUid)",
          ".validate": "newData.hasChildren(['n','g','ts'])",
          "n":  { ".validate": "newData.isString() && newData.val().length <= 40" },
          "g":  { ".validate": "newData.isString() && newData.val().length <= 8" },
          "ts": { ".validate": "newData.isNumber()" },
          "$other": { ".validate": false }
        }
      }
    },
    "chats": {
      "$pairId": {
        ".read":  "auth != null && $pairId.contains(auth.uid)",
        ".write": "auth != null && $pairId.contains(auth.uid)",
        "$msgId": {
          ".validate": "newData.hasChildren(['f','t','ts'])",
          "f":  { ".validate": "newData.isString() && newData.val() === auth.uid" },
          "t":  { ".validate": "newData.isString() && newData.val().length >= 1 && newData.val().length <= 200" },
          "ts": { ".validate": "newData.isNumber()" },
          "$other": { ".validate": false }
        }
      }
    },
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        "save": {
          "data": { ".validate": "newData.isString()" },
          "at":   { ".validate": "newData.isNumber()" }
        },
        "profile": {
          "name": { ".validate": "newData.isString() && newData.val().length >= 2 && newData.val().length <= 20" }
        }
      }
    }
  }
}
```

**💰 แนวทางประหยัด tokens (งานไม่เสีย):**
- **ห้ามอ่านไฟล์ใหญ่ทั้งไฟล์** โดยเฉพาะ ui.js (~1,800 บรรทัด) / HANDOFF.md / vocab.js / collectibles.js — ใช้ Grep หาชื่อฟังก์ชันก่อน แล้ว Read เฉพาะช่วงบรรทัดที่เกี่ยว (offset+limit)
- HANDOFF อ่านแค่: บรรทัดสถานะบนสุด + หัวข้อนี้ + หัวข้อกติกาเฉพาะระบบที่จะแตะ + สเปก backlog ข้อที่ทำ — ประวัติ "รอบ..." เก่าๆ ไม่ต้องอ่านถ้าไม่ติดปัญหา
- mock login สำหรับทดสอบ: สูตรอยู่หัวข้อ "ธรรมเนียมการทำงาน" (มี authWriteProfileName แล้ว) · ทดสอบเฉพาะจุดที่แก้ ไม่ต้อง regression ทั้งเกม · กดปุ่มใน overlay/async ผ่าน eval ให้แยกคนละ call กับตอนสร้าง (เคย race หลายครั้ง) · screenshot อาจ timeout — เช็ก DOM ผ่าน eval แทน · จอ preview จัตุรัสจะโดน overlay "หมุนจอ" — `preview_resize` เป็น 1280×720 ก่อน screenshot
- **⚠️ http.server cache เหนียวมาก** (แก้ไฟล์ js แล้ว reload ยังได้โค้ดเก่า — เช็กด้วย `typeof ฟังก์ชันใหม่`): บังคับโหลดใหม่ด้วย eval `await Promise.all(files.map(f=>fetch(f,{cache:'reload'})))` (URL ตรงไม่มี query) แล้ว `location.reload()` — ทดสอบระบบเพื่อนใช้ fake `Online.db={ref:path=>({get,set,remove})}` + `Online.ready=true` (firebase SDK โหลดจริงบน localhost ได้ ServerValue.TIMESTAMP ใช้ได้)
- commit: add เฉพาะไฟล์ที่แก้ **ห้าม `git add -A`** (มี `js/data/vocab/` untracked ของงาน Sonnet ค้างอยู่ — ห้ามแตะ ห้าม commit จนงานคำศัพท์เสร็จครบ)
- ตอบผู้ใช้กระชับ ตารางสั้น ไม่ต้องเล่ารายละเอียดโค้ดยาว

## ภาพรวมโปรเจกต์

**Pet Vocab Adventure** — เว็บเกมเลี้ยงสัตว์เพื่อเรียนคำศัพท์อังกฤษ สำหรับนักเรียน ป.1–ม.6 (ผู้ใช้คือครู/ผู้ปกครองชาวไทย ตอบเป็นภาษาไทยเสมอ)

- โฟลเดอร์: `C:\Users\rober\english-pet-game\` เปิดได้ทั้ง file:// และ http:// — **ห้าม**ใช้ fetch กับไฟล์ local ให้ใช้ `new Image()` probe (`probeImages()` ใน js/images.js)
- **🌐 ออนไลน์แล้ว (5 ก.ค. 2026):** repo GitHub `iamsuperrich2025/english-pet-game` (branch `main`) + GitHub Pages เปิดแล้ว → เกมเล่นได้ที่ **https://iamsuperrich2025.github.io/english-pet-game/** · โฟลเดอร์ในเครื่องเป็น git repo แล้ว (identity ตั้งเป็น iamsuperrich2025 / freddommun@gmail.com, push ผ่าน HTTPS credential ในเครื่องใช้ได้เลย ไม่มี gh CLI) · **ธรรมเนียมใหม่: แก้เกมเสร็จ+ทดสอบผ่าน ให้ commit + push ด้วย** (Pages อัปเดตเองใน 1–2 นาที)
- **🔥 Firebase (5 ก.ค. 2026):** โปรเจกต์ `english-pet-game` (Google account ผู้ใช้ · Spark ฟรี) · Realtime DB `https://english-pet-game-default-rtdb.asia-southeast1.firebasedatabase.app` · console: https://console.firebase.google.com/project/english-pet-game/database (ผู้ใช้ล็อกอินเองในเบราว์เซอร์ — Claude แก้ rules เองไม่ได้ ต้องส่งให้ผู้ใช้วาง) · ทดสอบ/seed/ลบข้อมูลจากเครื่องได้ผ่าน REST `<dbURL>/<path>.json` (python urllib — ระวัง encoding ไทยตอน print, ตั้ง `$env:PYTHONIOENCODING='utf-8'`) · แก้ rules เมื่อไหร่ทดสอบ allow/deny ผ่าน REST ได้เลย (ดูตัวอย่าง 10 เคสในรอบสิบสาม)
- เซฟใน LocalStorage key `petVocabAdventure_v1` ผ่าน `state` + `saveState()`/`loadState()` — `loadState()` มี **migration เซฟรุ่นเก่า (สัตว์ตัวเดียว) → รุ่นใหม่ (หลายตัว)** แล้ว
- ทดสอบ: preview server `english-pet-game` (python http.server พอร์ต 8642 ใน `C:\Users\rober\.claude\launch.json`) — ทดสอบเสร็จให้ `localStorage.removeItem('petVocabAdventure_v1')` + reload เสมอ
- เครื่องนี้ไม่มี Node มีแต่ Python 3.12

## โครงสร้างไฟล์ (แยก data ออกจาก logic — อัปเกรดเนื้อหาได้โดยไม่กระทบเซฟผู้เล่น)

```
index.html            โครง HTML + โหลดสคริปต์ตามลำดับ (data → engine)
css/style.css         CSS ทั้งหมด (รวม rank-card, rankup-overlay, heat-bar, online-card, clock-chip, farm-*, mkt-*/collect-*/collect-reveal สินค้าสะสม)
js/data/pets.js       PETS (dog/cat 3,000 · dragon 10,000) + FOODS ราคาหลัก 100 (100–500 · เมนูโปรด 300 · feast 1,000 อิ่ม 6 ชม.)
js/data/items.js      ITEMS ราคาใหม่: โบว์ 500 · ทั่วไป 1,200–5,000 · มงกุฎ 25,000
                      + มือถือ (PHONE_PRICE/SELL/BONUS · NET_FEE · netCost) + คอม (COMP_PRICE/SELL/RATE · DATA_FEE · dataCost)
js/data/fruits.js     FRUITS 4 ชนิด (ส้ม/แอปเปิล/มังคุด/ทุเรียน) + fruitInfo/fruitGrowMs/fruitMsLeft — ข้อ 12 ✅ เสร็จครบ (UI ใน ui.js: renderFarmCard/renderFarmClock/fruitCountdown/buyFruit/sellFruit)
js/data/collectibles.js  COLLECTIBLES 50 ชิ้น (5 หมวด×10 · tier common→mythic ราคาฐาน 500–2M · field `words`=แต้มคำผลิต ~25-300 เหรียญ/คำตาม tier) + COLLECT_TIERS (มี common ใหม่ 🔹) + COLLECT_CATS 5 หมวด + collectInfo/collectTier + listingSellMs/listingStatus — สินค้าผลิต+ตลาดขายต่อ
js/data/vocab.js      VOCAB_BANDS 5 ระดับ (ป.1-2/ป.3-4/ป.5-6/ม.1-3/ม.4-6) × 8 หมวด × 10 คำ
                      band 1 คง id เดิม (animals,...) เพื่อ compat เซฟเก่า / gradeBand(), catsForStudent(), findCat()
js/data/ranks.js      RANKS 7 แรงค์ + rankInfo(worth) [net worth] + rankFromKey — ขั้นย่อย III→II→I
js/data/homes.js      HOMES 3 ระดับ (1,000/100,000/1,000,000) + AC 20,000+5,000 + MAINT_RATE 0.5%/เดือน + DECAY_DAY=5 + maintCost() + ELEC_RATE 0.5% + TRANSFORMER_COST=1000 + elecCost() + WATER_RATE 0.5% + WATER_INSTALL_COST=1000 + waterCost() + WEATHERS + weatherNow()
js/data/players.js    ONLINE_NAMES 36 ชื่อ + ONLINE_ACTIVITIES + onlineBaseCount(hour) — เพื่อนจำลอง (fallback ตอนออฟไลน์)
js/data/badwords.js   ตัวกรองชื่อ (ข้อ 0.2 + ข้อ 7): NAME_ALLOWED_RE (ไทย/อังกฤษ/เลข/เว้นวรรค) ·
                      BAD_PART (เจอแฝงก็บล็อก) + BAD_EXACT (บล็อกเฉพาะทั้งชื่อ — กันชนคำปกติ ฟักทอง/แมงมุม) ·
                      nameNormalize (ตัวเล็ก→เลขแทนอักษร→ตัดวรรค→ตัดวรรณยุกต์→ยุบตัวซ้ำ) · checkName(raw,min,max)→{ok,name|msg}
js/data/firebase-config.js  FIREBASE_CONFIG (config สาธารณะ + databaseURL asia-southeast1) — ลบไฟล์/ค่าไม่ครบ = เกมเข้าโหมดออฟไลน์เอง
js/online.js          เครื่องยนต์ออนไลน์: โหลด Firebase SDK 10.14.1 (compat) แบบ dynamic ไม่บล็อกเกม ·
                      + ระบบเพื่อน (ข้อ 0.3): friendCode(uid) รหัส 6 ตัว deterministic · friendSearch/friendRequest/friendAccept/friendDecline ·
                        Online.myCode/reqs/myFriends/presenceMap · listen /friendReq/<uid> + /friends/<uid> · เผยแพร่ /friendCodes/<code>=uid ตอน connect
                      Online {ready,friends,board} · presence (onDisconnect ลบเอง + กรองผีค้าง 10 นาที) ·
                      leaderboard push ทุก 1 นาทีเฉพาะเหรียญขยับ + ฟัง Top 50 (orderByChild coins) ·
                      onlineDisplayName (ชื่อ+อักษรแรกนามสกุล — ถนอมความเป็นส่วนตัวเด็ก) · onlineActivity (ดูจาก screen ที่เปิด)
js/auth.js            เครื่องยนต์ Google Login (ข้อ 0.1 ✅): Auth {user,booted,sdkReady,gated,lastPushedAt} ·
                      authStart (initializeApp+onAuthStateChanged) · authLoginClick (popup→fallback redirect) ·
                      authSyncOnLogin (เทียบ savedAt vs cloud.at · ถามผูกเซฟเก่า authAskLink 2 ชั้น) ·
                      authPushSave (ทุก 1 นาที+ปิด/พับแท็บ เฉพาะเซฟขยับ) · authLogout (push→signOut→ล้าง cache→reload) ·
                      authGateOffline (SDK ไม่มา 20 วิ/config หาย) · จุด mock ทดสอบ: stub authFetchCloud/authWriteCloud/authDeleteCloud+onlineStart แล้วเรียก authOnLogin({uid,email})
js/util.js            shuffle/fmtNum/escapeHTML/toast/floatFx/sfx(beep)/showScreen/askConfirm/seededRand(mulberry32)
                      + askNameDialog(opt) กล่องตั้งชื่อกลาง (validate ด้วย checkName — ใช้ทั้งชื่อผู้เล่น/ชื่อสัตว์ · ไม่ส่ง cancelText = บังคับตั้ง)
js/state.js           state, DEFAULT_STATE, loadState (migration), careTick, addCoins/addRP/addExp,
                      expNeed(level), currentSlotStart/nextSlotStart, heatProtected, CURE_COST=1000,
                      เครื่องยนต์บิลรายเดือน: ymStr/billOutstanding/homeDecayed/billTick (state.bills)
                      + สาธารณูปโภค: ทะเบียน UTILITIES {elec, water} (cost/cutKey/fixKey) — billTick วน loop
                      ค่าไฟ: state.powerCut/transformerBought (heatProtected() คืน false ตอนถูกตัดไฟ)
                      ค่าน้ำ: state.waterCut/plumbingBought + p.thirstFrom + THIRST_SICK_MS=6 ชม. ใน careTick
                      สินค้าสะสม: state.collection/listings/tradeSold + assetValue() นับด้วยราคาฐาน + marketTick(now) ใน careTick
                      โรงงานผลิต: state.producing {id,progress}/producedCount + addCraft(n) · ออเดอร์พิเศษ: state.orders/nextOrderAt + newOrder/orderTick (max 2 · หมดอายุ 24 ชม. · จ่าย 1.3–1.8× · producedCount=0 → common เท่านั้น)
js/images.js          IMG_FILES, probeImages(keys,dir), probeCollectImages, currentPetImg, equippedItem, makeHappy
js/ui.js              renderDashboard/renderClock/renderOnlineCard/renderShop/renderHomeCard/
                      renderPetShop/renderRankCard/showRankUp/showLevelUp/feedPet/curePet/renderStats
                      + UI สาธารณูปโภคกลาง: UTILITY_UI (ข้อความ/ปุ่ม/onRestored ต่อชนิด) +
                      utilityBillUI(id,h,lastDay)/payUtility(id)/buyUtilityFix(id) — ค่าเน็ต (ข้อ 7) เสียบเพิ่มได้
                      + โรงงานผลิต+ตลาด: renderCollectCard/renderFactory/renderOrdersUI/renderCollectMine/
                      startProduce/cancelProduce/deliverOrder/renderOrderClock/openListDialog/cancelListing/
                      showCollectReveal(id,price,produced)/collectImg (การ์ด #collect-card · module var collectView/factoryCat)
js/game.js            เกมจับคู่ + หมวด/แบบทดสอบ (renderCats/startQuiz/finishQuiz) + hook แต้มผลิต: จับคู่ถูก addCraft(1)+note "🏭 x/y" · finishQuiz addCraft(ข้อถูก) — ครบแล้วเรียก showCollectReveal(id,null,true)
js/main.js            ปุ่ม + init: โชว์ #screen-login รอ auth → bootGame() (เรียกจาก authEnterGame ครั้งเดียว) ·
                      careTick interval ทุก 1 นาที guard ด้วย Auth.booted (ห้ามเดินก่อน login — จะบัมพ์ savedAt
                      ทำให้เซฟเก่าในเครื่องดูใหม่กว่า cloud) + renderClock ทุก 1 วินาที · ปุ่มรีเซ็ตถูกถอดแล้ว (ดูกติกา 🔐)
TASK_VOCAB_SONNET.md  คำสั่งงานคำศัพท์ (backlog ข้อ 9 = อัพเดท 5725691326 ข้อ 5/5.1/5.2) ให้ Sonnet 5 ทำใน session ใหม่ — แยกไฟล์ vocab/ + POS + ขยาย ≥2,000 คำ (🔶 ค้างครึ่งทาง — มีแค่ band1.js untracked · ⚠️ เคยทำ production พัง ดูรอบสิบเจ็ด)
TASK_DICTIONARY_SONNET.md  🆕 คำสั่งงาน Project Dictionary A–Z (backlog ข้อ 10) ให้ Sonnet ทำหลายๆ session — สเปก+กฎเหล็ก+ตารางความคืบหน้าอยู่ในไฟล์ (⬜ ผู้ใช้สั่ง note ไว้ก่อน ยังไม่เริ่ม)
PROMPTS.md            prompt ภาพสัตว์ 75 ภาพ (เจนแล้ว ~52 เหลือ dragon เกือบหมด + dog/cat_newborn)
PROMPTS_RANK_HOME.md  prompt เหรียญตราแรงค์ 7 + ที่พัก 3 (✅เจนแล้ว) + บ้านเสื่อม 3 + บ้านพัง 3 + บ้านถูกตัดไฟ (มืด) 3 + บ้านถูกตัดน้ำ (แห้ง) 3 (⬜ยังไม่เจน)
PROMPTS_COLLECTIBLES.md  prompt สินค้าชุดแรก 12 ชิ้น (✅ เจนแล้ว)
PROMPTS_PRODUCTS.md   prompt สินค้าผลิต 38 ชิ้นใหม่ + ตารางแคตตาล็อกเต็ม 50 (✅ เจนครบแล้ว 5 ก.ค. 2026)
PROMPTS_GIFTS.md      prompt ของขวัญเทศกาล 50 ภาพ (เค้ก 22 · กุหลาบ 14 · การ์ดคริสต์มาส/ปีใหม่ 14) → วาง img/gifts/gift_<id>.png (⬜ ผู้ใช้กำลังเจน — ใช้กับระบบส่งของขวัญ backlog ข้อ 0)
PROMPTS_THEME.md      prompt ธีม "เมืองทันสมัยโทนฟ้า" 6 ภาพ (พื้นหลังตั้ง/กว้าง + ตึกระฟ้า/บ้าน/สวน/เมฆ) → วาง img/theme/ (⬜ — **ผู้ใช้เปลี่ยนสไตล์ 5 ก.ค. ค่ำ: จากฟาร์มพาสเทล → เมืองสีฟ้าตามภาพตัวอย่างที่สอง** · ทำธีมใน session ใหม่ตามคำสั่งผู้ใช้)
img/                  ภาพสัตว์ · img/rank/ ✅ครบ 7 · img/home/ ✅ครบ 3 · img/collectibles/ ✅ครบ 50 ·
                      img/gifts/ ✅ครบ 50/50 (เค้ก 22 · กุหลาบ 14 · การ์ดเทศกาล 14 — ผู้ใช้วางครบ+commit "รอบสิบเจ็ด" · พร้อมใช้กับข้อ 0.5) ·
                      img/theme/ ✅ครบ 6 (ผู้ใช้วาง 5 ก.ค. ดึก — **ธีมเมืองโทนฟ้าพร้อมทำใน session ใหม่แล้ว**)
```

## กติกาเกมปัจจุบัน (state รุ่นใหม่)

- `state.pets` = array (เลี้ยงหลายตัว ซื้อเพิ่มที่ร้านสัตว์ screen-select, จำกัดชนิดละ 1 ตัว), `state.active` = ตัวที่ดูแลอยู่, เลเวล/EXP/ชุด/ป่วย เก็บรายตัว, `state.owned` = ตู้เสื้อผ้ารวม
- **EXP โตช้าลง** (ปรับตามคำสั่งผู้ใช้ "เปลี่ยนช่วงวัยไวเกินไป"): `expNeed(level)` = Lv.1→2 ต้อง 250 · Lv.2→3 ต้อง 600 · หลังจากนั้นเลเวลละ 400 (จับคู่ถูก +5 EXP, เมนูโปรด +15, สอบผ่านครั้งแรก +30)
- **มื้ออาหาร**: หิวทุก 3 ชม. (slot 0,3,6,...,21 น. — `currentSlotStart` ปัดลงขอบ 3 ชม., `SLOT_MS`) — `p.fedUpTo` = slot ล่าสุดที่กินครอบคลุม; หิวเกิน 2 ชม.ไม่กิน → ป่วย (`HUNGRY_SICK_MS`); feast อิ่ม 6 ชม. = ข้ามมื้อถัดไป (fedUpTo = nextSlot); ราคาอาหาร 100–500, feast 1,000 (≥2 เท่า)
- **ความร้อน**: ไม่มีบ้าน/บ้านไม่มีแอร์ → `p.heatFrom` นับสะสม ครบ 6 ชม.ป่วย (`HEAT_SICK_MS`) มีแถบร้อน; มังกร immune; ปลอดภัยเมื่อ `heatProtected()` = ปราสาท หรือบ้านกลาง+แอร์; ค่ารักษาป่วยทุกกรณี 1,000
- **ที่พัก**: เพิง 1,000 (กันฝนอย่างเดียว ติดแอร์ไม่ได้) · บ้านกลาง 100,000 (แอร์ 20,000+ติดตั้ง 5,000 = 25,000) · ปราสาท 1,000,000 (มีแอร์ในตัว); ย้ายบ้านแล้วแอร์ไม่ติดตามไป (`state.ac=false`)
- **ค่าบำรุงบ้าน**: 0.5% ของราคาบ้าน/เดือน (เพิง 5 · บ้าน 500 · ปราสาท 5,000) บิลออกทุกวันที่ 1 (`billTick` ใน careTick) เดือนแรกที่ซื้อฟรี; ค้างจ่ายถึงวันที่ 5 → บ้านเสื่อมสภาพ (`homeDecayed()` ภาพ `home_<id>_decayed.png` ไม่มีภาพใช้ฟิลเตอร์หม่น); ค้างข้ามเดือน → บ้านพัง (`state.pendingRuin` → ฉาก `showHomeRuined` ภาพ `home_<id>_ruined.png`) home=null กลายเป็นคนไร้บ้าน; เซฟเก่าที่มีบ้าน → เริ่มบิลเดือนปัจจุบันแบบฟรี
- **ค่าไฟ**: บิล id `elec` 0.5% ของราคาบ้าน/เดือน ออกวันที่ 1 เดือนแรกฟรี (โครงเดียวกับ maint); ค้างจ่ายข้ามเดือน → **ถูกตัดไฟ** (`state.powerCut=true` บิลค้างทบเข้าบิลเดือนใหม่ไม่หายไป) บ้านมืด (ภาพ `home_<id>_dark.png` ไม่มีภาพใช้ฟิลเตอร์ `.home-dark-img`) `heatProtected()` คืน false → สัตว์ร้อนป่วยทุก 6 ชม.แม้มีแอร์/ปราสาท (มังกร immune); จะจ่ายบิลค้างได้ต้องซื้อ**หม้อแปลงใหม่ 1,000** ก่อน (`state.transformerBought`, ปุ่มใน home-card — ตอนนี้ผ่านเครื่องยนต์กลาง `payUtility('elec')`/`buyUtilityFix('elec')`) จ่ายครบ → ไฟกลับมา เคลียร์ heatFrom; บ้านพังจาก maint → ล้างบิลสาธารณูปโภคทั้งหมด+สถานะตัด; ซื้อ/ย้ายบ้าน → มิเตอร์ใหม่ (บิลฟรีเดือนแรก สถานะตัดหาย); เซฟเก่ามีบ้าน → เริ่มบิลฟรีเดือนนี้
- **ค่าน้ำ**: โครงเดียวกับค่าไฟผ่านเครื่องยนต์กลาง (บิล id `water` 0.5%/เดือน วันที่ 1 เดือนแรกฟรี บิลค้างทบข้ามเดือน): ค้างถึงสิ้นเดือน → **ถูกตัดน้ำ** (`state.waterCut`) ภาพ `home_<id>_nowater.png` (ไม่มีภาพใช้ภาพปกติ — สถานะบอกผ่าน tag+กล่องบิล+แถบขาดน้ำ); สัตว์**ทุกชนิดรวมมังกร** นับขาดน้ำสะสม (`p.thirstFrom` แถบสีฟ้า `.thirst-fill`) ครบ 6 ชม. → ป่วย `cause:'thirst'` รักษา 1,000; ต้องจ่าย**ค่าติดตั้งระบบน้ำใหม่ 1,000** (`state.plumbingBought`) ก่อนจึงจ่ายบิลค้างได้ จ่ายครบ → น้ำกลับมา เคลียร์ thirstFrom ทุกตัว; ภาพบ้านจัดลำดับ มืด(ไฟ) > แห้ง(น้ำ) > หม่น(ทรุด)
- **🏭 โรงงานผลิตสินค้า + ตลาดขายต่อ (แทนตลาดซื้อจำลองเดิม — "รอบสิบสี่")**: การ์ด `#collect-card` มีกล่องออเดอร์ด้านบน + 2 แท็บ — **🏭 โรงงานผลิต**: เลือกสินค้าจากแคตตาล็อก 50 ชนิด (dropdown กรอง 5 หมวด `factoryCat` · **แบ่งหน้า 5 รายการ/หน้า ปัดซ้าย-ขวา/ลูกศร — "รอบสิบหก"**) กด "ผลิต" → `state.producing={id,progress}` (เปลี่ยนกลางคันแต้ม>0 มีกล่องยืนยันเตือนแต้มหาย · ยกเลิกได้) · **ตอบคำศัพท์ถูก 1 คำ/ข้อ = 1 แต้มผลิต** (`addCraft` — hook ใน checkMatch จับคู่ +note "🏭 ชื่อ x/y" และ finishQuiz +ข้อถูก โชว์ในกล่องผลสอบ) ครบแต้ม → เข้า `state.collection` + `producedCount++` + **ฉาก `showCollectReveal(id,null,true)` "🏭 ผลิตสำเร็จ!"** · กล่องงานผลิตโชว์แถบ % สี tier + ปุ่ม "🎮 ไปเล่นเกม" — **📦 ออเดอร์พิเศษ**: `orderTick` ใน careTick (ค้างสูงสุด 2 · เข้าใหม่ทุก 2–4 ชม. · หมดอายุ 24 ชม.หายเงียบ · จ่าย 1.3–1.8× ราคาฐาน · น้ำหนักสุ่ม common 40/rare 35/epic 18/legendary 6/mythic 1 · **ยังไม่เคยผลิต → ได้ common เท่านั้น**) แถวออเดอร์โชว์ผู้สั่ง+% กำไร+นับถอยหลัง (`renderOrderClock` เดินกับนาฬิกา) · มีของในคลัง → ปุ่ม "ส่งมอบ" (`deliverOrder` เอาของออก+`addCoins(payout)`) — **🎁 คลังของฉัน**: เหมือนเดิม (ตั้งราคาขาย `openListDialog`/`listingStatus` · `marketTick` ลูกค้าจำลองซื้อตามเวลา `listingSellMs` · กล่องขายสำเร็จ `tradeSold`) · **assetValue นับของในคลัง+ลงขายด้วยราคาฐานเหมือนเดิม** (ของกำลังผลิตยังไม่นับ) · สถิติมีแถว "🏭 สินค้าที่ผลิตสำเร็จ" · migration: producing/orders/nextOrderAt/producedCount มี default+คัดข้อมูลเสีย · **ของเดิมที่ถูกถอด:** marketListings/marketSeed/buyFromMarket/renderCollectShop/marketBought (ตลาดซื้อจากผู้เล่นจำลอง)
- **เหรียญ**: `addCoins()` อัปเดตทั้ง `state.coins` และ `state.daily.coins` (รีเซ็ตข้ามวันด้วย `dailyTick()`) — dashboard โชว์ 2 pill ให้เด็กแคปส่งครู
- **แรงค์**: ยึด **net worth = เหรียญ + มูลค่าทรัพย์สินสุทธิ** (`netWorth()`/`assetValue()` ใน state.js — **ทรัพย์สินทุกชิ้นที่ถือครองคิดราคาเต็มที่ซื้อ** รวมมือถือ `PHONE_PRICE`/คอม `COMP_PRICE`; ราคาขายคืนใช้ตอนขายจริงเท่านั้น → ซื้อแล้ว net worth เท่าเดิม แรงค์ไม่ตก); เกณฑ์ใหม่ยากขึ้นเท่าตัว 7 แรงค์ (บรอนซ์ 0, ซิลเวอร์ 10,000, โกลด์ 200,000, แพลตินัม 1,000,000, ไดมอนด์ 2,000,000, มาสเตอร์ 4,000,000, จอมพล 20,000,000) ขั้นย่อย III→II→I; **ซื้อทรัพย์สินแรงค์ไม่ตก** (เงินแปลงเป็นของ) ใช้จ่ายกิน/บิลถึงลด; ตรวจเลื่อน/ลดที่ `refreshRank()` (เรียกใน `careTick` จุดนิ่ง — เทียบ `state.rankKey` ที่เก็บไว้ กันขายทรัพย์สินฉลองผิด) ข้ามแรงค์ใหญ่ → `showRankUp()` ฉาก · ขั้นย่อย → toast · ลด → เงียบ; RP ยังเก็บเป็นแต้มความพยายามโชว์ในสถิติ; **ภาพเหรียญตราครบ 7 ไฟล์**
- **นาฬิกา**: `#clock-chip` ใต้ชื่อผู้เล่น โชว์ วัน·วันที่·เวลา (th-TH) เดินทุกวินาที (`renderClock` + interval ใน main.js)
- **การ์ดเพื่อนออนไลน์**: `#online-card` — **ต่อ Firebase ได้ → ผู้เล่นจริง** (`Online.friends` จาก `/presence` ป้าย "🌏 ออนไลน์จริง" · หลุดแล้วชื่อหายเอง onDisconnect · ผีค้างเกิน 10 นาทีถูกกรอง) / **ออฟไลน์ → เพื่อนจำลองเดิม** (สุ่ม `seededRand` เปลี่ยนทุก 5 นาที จำนวนตามช่วงเวลา) ผู้เล่นเองอยู่แถวบนไฮไลต์เหลืองทั้ง 2 โหมด
- **🏆 Leaderboard**: `#leaderboard-card` "สุดยอดนักสะสมเหรียญ Top 50" — อันดับ **เหรียญคงเหลือ** (`state.coins` ไม่ใช่ net worth ตามที่ผู้ใช้สั่ง) ข้อมูลจริงจาก `/leaderboard` อัปเดตสด · เหรียญ 🥇🥈🥉 3 อันดับแรก · แถวตัวเองไฮไลต์เหลือง+⭐ + บอกอันดับ ("หนูอยู่อันดับที่ N จาก M คน") · ออฟไลน์โชว์ข้อความเชิญชวนแทน · id ผู้เล่น = `state.onlineId` (สุ่มครั้งเดียวต่อเครื่อง มี migration ใน loadState)
- **คำศัพท์ตามชั้น**: register เลือก ป.1–ม.6 → `gradeBand()` เลือก band; เกม/หมวด/สถิติ ใช้ `catsForStudent()`/`vocabForStudent()`
- **ล็อกร้านค้า**: ไม่มีสัตว์ หรือ active ตัวเป็นไข่/แรกเกิด (Lv.1) → ร้านไอเทมโชว์ lock-banner
- เล่นเกม/สอบได้แม้ยังไม่มีสัตว์ (ได้เหรียญ+RP แต่ไม่ได้ EXP) — เริ่มเกมต้องเก็บ 3,000 ก่อนซื้อหมา/แมว
- **🔐 Google Login (ข้อ 0.1 — "รอบสิบห้า")**: เข้าเกมต้อง login เท่านั้น (แบบ ก. ผู้ใช้เลือก) หน้า `#screen-login` (สถานะ เชื่อมต่อ/พร้อม/offline+ปุ่มลองใหม่) · เซฟจริงอยู่ `/users/<uid>/save = {data: JSON ทั้ง state, at}` — localStorage เป็น cache เทียบ `state.savedAt` (บัมพ์ทุก saveState) vs `cloud.at` เอาอันใหม่กว่า · `state.ownerUid` = เจ้าของเซฟ (null = เซฟยุคก่อน login → ถามผูกเข้าบัญชี 2 ชั้นกันกดพลาด · เป็นของบัญชีอื่น → เริ่มใหม่เงียบๆ กันขโมยเซฟ) · push ขึ้น cloud ทุก 1 นาที (เฉพาะเซฟขยับ) + beforeunload/พับแท็บ + จุดสำคัญ (ลงทะเบียน/ผูก/logout) · logout = push→signOut→ล้าง cache→reload · **ปุ่มรีเซ็ตเกมถูกถอดออกแล้ว (ผู้ใช้สั่ง 5 ก.ค. ดึก — เด็กเข้าใจผิดว่าเป็น logout แล้วเซฟหายถาวร อันตราย)** รีเซ็ตได้เฉพาะทาง console/เครื่องมือทดสอบ: `localStorage.removeItem(STORAGE_KEY)` + ลบ `/users/<uid>/save` · อ่าน cloud fail (rules ยังไม่วาง) → เข้าเกมด้วยเซฟเครื่อง+toast เตือน · ⚠️ login จริงได้เฉพาะ http/https (localhost/Pages) — file:// ไม่ได้ · presence/leaderboard ใช้ **uid** เป็น key แล้ว (เปลี่ยนใน "รอบสิบแปด" ข้อ 0.2 — `onlineKey()` ใน online.js · onlineId สุ่มเดิมเหลือเป็น fallback)
- **📛 ชื่อในเกม (ข้อ 0.2 — "รอบสิบแปด"):** `state.profileName` (migration: ไม่ใช่ string → null) ตั้งตอนลงทะเบียน (ช่อง `#reg-nick`) · ผู้เล่นเดิมไม่มีชื่อ → กล่องบังคับตั้ง `authAskProfileName()` ตอนเข้า dashboard (ปิดข้ามไม่ได้) · ตรวจด้วย `checkName(raw,2,20)` จาก badwords.js · เก็บสำเนาสาธารณะที่ `/users/<uid>/profile/name` (`authPushProfile()` — push ตอนตั้งชื่อ+ทุก login) · `onlineDisplayName()` ใช้ profileName ก่อน (fallback ชื่อจริง+อักษรแรกนามสกุลแบบเดิม) · ชื่อจาก DB ทุกจุด escape ด้วย `escapeHTML()` (util.js) ก่อนแทรก innerHTML

## ทดสอบล่าสุด — ผ่านทุกข้อใน preview

**รอบยี่สิบสาม (6 ก.ค. 2026 — Opus session แก้ 2 บั๊กระบบเพื่อน/แชท ✅):**
- **(1) ค้นหาเพื่อนขึ้น "ผู้เล่น ชั้น " (ชื่อ/ชั้นว่าง):** เหตุ — friendSearch อ่านชื่อจาก `/leaderboard` แหล่งเดียว แต่เอนทรียังไม่มี (เพิ่งลงทะเบียน/coins ไม่ขยับ) + ตอนลงทะเบียนไม่ push presence/leaderboard ทันที (รอ beat 60 วิ) ขณะที่ friendCodes เผยแพร่ตั้งแต่ connect → "ค้นเจอแต่ยังไม่มีชื่อ" · **แก้:** online.js `friendSearch` อ่านทั้ง presence (สดถ้าออนไลน์) + leaderboard (ค้างถาวร) เลือกอันที่มีชื่อ (fallback "ผู้เล่น" เฉพาะไม่มีทั้งคู่จริงๆ) · main.js `btn-register` เสร็จแล้วเรียก onlinePushPresence()+onlinePushScore() ทันที · **ทดสอบ preview (fake db):** leaderboard-only(coins0)/presence-only/มีทั้งคู่(เลือก presence สดกว่า)/ไม่มีเลย→"ผู้เล่น"/self→"นี่คือหนูเอง"/รหัสไม่พบ→null + ลงทะเบียนจริง spy เขียน presence+leaderboard พร้อมชื่อ+ชั้น — ผ่านครบ ไม่มี console error (commit `84c4894`)
- **(2) แจ้งเตือนข้อความแชทใหม่แม้ไม่เปิดกล่องแชท:** เหตุ — เดิม chatListen ฟังเฉพาะตอนเปิดกล่อง ปิดแล้วไม่มีตัวเตือน · **แก้:** online.js เพิ่ม `chatWatchSync()` (เฝ้า `chats/<pairId>` limitToLast 1 ต่อเพื่อนทุกคนใน myFriends เทียบ `state.chatSeen` · flag `primed` กัน toast เด้งจาก snapshot แรกตอนโหลด · ข้อความของตัวเอง/อ่านแล้ว ไม่นับ) + `chatMarkSeen(uid,ts)` (เปิด/อ่านกล่อง = เคลียร์ unread + จำ ts) + `chatSeenTs`/`chatUnreadCount` + `Online.chatUnread{}` · เรียก chatWatchSync ในตัวฟัง `/friends/<id>` · state.js `chatSeen:{}` + migration · ui.js `updateFriendBadge` รวม unread + ปุ่มแชทเพื่อน `.has-unread` + ป้าย "ใหม่!" + openChat เรียก chatMarkSeen · lobby.css `.fr-chat-btn.has-unread`(เขียว)/`.fr-unread`(กะพริบ) · **ทดสอบ preview (fake reactive db รองรับ push/on/off/orderByKey/limitToLast):** ยังไม่มีข้อความ→ไม่เตือน · เพื่อนส่ง(ปิดกล่อง)→unread1+badge"1"+toast+เสียง · เปิดกล่อง→เคลียร์+จำ ts1000+badgeหาย · reload(อ่านแล้ว)→ไม่เตือนซ้ำ · ข้อความใหม่ ts2000→เตือนอีก · ข้อความตัวเอง→ไม่นับ · UI: ปุ่มเพื่อน `has-unread`+"ใหม่!" ขึ้นถูก · ไม่มี console error (commit `9f3ec4b`)
- **✅ ผู้ใช้ publish rules `/chats` แล้ว** (แชทเขียน server ได้จริง) · จบงานรีเซ็ต localStorage คืนหน้า login · **⚠️ งานถัดไป: self-heal เพื่อนครบสองฝ่าย (ดูหัวข้อแผน session ถัดไปบนสุด) + ทดสอบแชท 2 บัญชีบน Pages**

**รอบยี่สิบสอง (6 ก.ค. 2026 — Opus session ข้อ 0.4 แชทกับเพื่อน ✅):** online.js เพิ่ม chat engine (chatPairId/chatRef/chatListen/chatSend/chatPrune + const CHAT_MAX_LEN 200/CHAT_KEEP 100) / ui.js เพิ่ม `openChat(friend)` + `CHAT_EMOJIS` 44 ตัว + module var `chatUnsub` + ปุ่ม `.fr-chat-btn` 💬 ต่อแถวเพื่อนใน refreshFriendData (data-i ชี้ index ใน Online.myFriends) / lobby.css `.chat-overlay/.chat-box/.chat-head/.chat-msgs/.chat-bubble(.mine ขวาสีทอง)/.chat-emoji/.chat-emo/.chat-input-row/.chat-send/.fr-chat-btn` / HANDOFF rules เพิ่มโซน `/chats/$pairId` (read/write เฉพาะ `$pairId.contains(auth.uid)` + validate f===auth.uid, t 1–200, ts number) · **ทดสอบผ่านครบ (fake in-memory Firebase รองรับ push/on/off/once/update/orderByKey/limitToLast — mock login test1):** pairId เรียง alphabet ถูก (aaa_test1 / test1_zzz) deterministic · chatSend: ว่าง/>200/คำหยาบ "ไอ้ควย"/leet "sh1t" ถูก reject พร้อมข้อความ · ข้อความดี+200 ตัวพอดี ส่งผ่าน เก็บใต้ path คู่ถูก · prune: ยิง 105 ข้อความ → DB เหลือ 100 (ตัด msg1-5) listener เห็น 100 (limitToLast) · unsub หยุดรับ update จริง · UI: openChat โชว์ head ชื่อ+ชั้น · bubble ตัวเองขวา(mine)/เพื่อนซ้าย · XSS `<img onerror>` ในข้อความ+ชื่อเพื่อน escape เป็น text ไม่ยิง · emoji picker toggle เปิด/ปิด+คลิกแทรกลง input · ส่งจากปุ่ม→bubble ใหม่ขึ้น input เคลียร์ · คำหยาบจาก UI→ไม่เพิ่ม bubble+input ไม่โดนล้าง · ปิด→overlay หาย chatUnsub=null · ปุ่ม 💬 ในแถวเพื่อน 2 แถวเปิดแชทถูกคน · online-dot ตาม presenceMap · screenshot กล่องแชทสวยตรงธีม (หัวฟ้า bubble ทอง/ขาว emoji panel) · ไม่มี console error-warning · จบงานรีเซ็ต localStorage คืนหน้า login · **⚠️ รอผู้ใช้วาง rules ใหม่ (โซน `/chats`) ก่อนแชทจริงบน Pages — ทดสอบด้วย 2 บัญชี Google**

**รอบยี่สิบเอ็ด (6 ก.ค. 2026 — Opus session ข้อ 0.3 ค้นหา id + เพิ่มเพื่อน ✅):** online.js เพิ่มระบบเพื่อน (friendCode/Search/Request/Accept/Decline + listeners + presenceMap + Online.myCode/reqs/myFriends) / index.html ปุ่มเมนู "👥 เพื่อน" (+badge คำขอ) + `#panel-friends`/`#friend-card` / lobby.js PANEL_TITLES / ui.js renderFriendPanel (สร้างโครงครั้งเดียว dataset.built กันช่องค้นหาโดนล้างตอน tick) + friendDoSearch + refreshFriendData + updateFriendBadge (เรียกใน renderDashboard+onlineRerender) / lobby.css `.fr-*`/`.rail-badge`/`.online-dot.off` · **ทดสอบผ่านครบ (fake Online.db):** friendCode 6 ตัว deterministic/ตัวอักษรถูก/uid ต่างกันรหัสไม่ชน / render: รหัสตัวเองถูก + badge นับคำขอ + เพื่อน dot ออนไลน์/ออฟไลน์ตาม presenceMap + ชื่อ XSS `<img onerror>` escape เป็นข้อความไม่ยิง / ค้นหา: รหัสตัวเอง→"นี่คือหนูเอง" · ไม่พบ→ข้อความ · เจอ→ชื่อ+ปุ่มส่งคำขอ · <6 ตัว→เตือน / ส่งคำขอ→เขียน `friendReq/<toUid>/<me>={n,g,ts}` / รับ→เขียน `friends/<me>/<from>`+`friends/<from>/<me>` แล้วลบ `friendReq/<me>/<from>` / ปฏิเสธ→ลบคำขอ / ไม่มีเพื่อน→ข้อความชวน / offline→ข้อความต่อเน็ต + built reset / ไม่มี console error-warning / รีเซ็ต localStorage คืนหน้า login · **⚠️ รอผู้ใช้วาง rules ใหม่ (เพิ่มโซนเพื่อน — ดูหัวข้อแผนบนสุด) ก่อนใช้จริงบน Pages**

**เพิ่มเติมรอบยี่สิบ (6 ก.ค. 2026 — เปลี่ยนหยดน้ำเป็นภาพจริง):** ผู้ใช้เจนภาพเม็ดน้ำ 5 แบบวางที่ `img/fx/raindrop*.png` (พื้นหลังโปร่งใสแล้ว แต่มี **halo เทากึ่งโปร่งใส** รอบเม็ด — ล้างด้วย Pillow: alpha ramp ตัด <35→0 ถึง 110→เต็ม + ย่อ 1024→256 เหลือ 17-54KB) · `rainFxDrop` เปลี่ยนจาก `<span>` gradient เป็น `<img>` สุ่ม 5 แบบ + สุ่มขนาด 16-46px/ตำแหน่ง/ความทึบ `--o`/องศา `--r` · `.glass-drop` keyframe ใช้ตัวแปรต่อเม็ด · **ทดสอบผ่าน:** ฝนตก→เม็ดน้ำจริงเกาะกระจกทั่วจอดูธรรมชาติ (screenshot) · ฝนหยุด→หาย · ไม่มี error · **หมายเหตุ:** ภาพที่เจนมามัก baked พื้นหลัง/halo เทา — ตรวจ alpha ด้วย Pillow ก่อนใช้เสมอ (`grayHalo` ดูจาก partial-alpha pixel ที่ RGB เทา)

**รอบยี่สิบ (6 ก.ค. 2026 — Fable session เอฟเฟกต์ฝนเต็มจอ ✅ ตามคำสั่งผู้ใช้):** ฝนตกจริง (19:00–20:00 `rainNow`) + ไม่มีบ้านสภาพดี (`!rainProtected()` — บ้านทรุดก็โดน) → overlay `#rain-fx` เต็มจอ (pointer-events:none z-index 9000 ทุกหน้าจอ): เม็ดฝนจาง 2 ชั้น (CSS repeating-gradient แกน 100°/96° เส้นเกือบตั้งเอียง ~10° · เลื่อน**ขนานกับเส้น**ให้ loop เนียน — ⚠️ เคยพลาดใช้ 170° แล้วเส้นออกมาเกือบนอน) + หยดน้ำเกาะกระจกจอ `.glass-drop` (spawn สุ่มทุก 0.35–1.15 วิ เกาะ ~6 วิ แล้วไหลลงจางหาย · loop เช็ก `document.contains` หยุดเองเมื่อ overlay ถูกถอด) · `rainFxTick()` ใน ui.js + interval 5 วิใน main.js (guard `Auth.booted`) · **แค่ภาพล้วน ไม่แตะ state/เซฟ** · ทดสอบ: mock 19:30 โชว์ 2 ชั้น+หยดเพิ่มเรื่อยๆ / ซื้อบ้าน→หายทันที / ขายบ้าน→กลับมา / mock 20:30 ฝนหยุด→หาย+spawn หยุด / screenshot สวย เส้นฝนเฉียงธรรมชาติ / ไม่มี console error / รีเซ็ตคืนหน้า login

**รอบสิบเก้า (6 ก.ค. 2026 — Fable session ต่อยอดข้อ 0.2: ข้อ 7 ตั้งชื่อสัตว์ + ✏️ แก้ชื่อในเกม + 📛 บนแถบโปรไฟล์ ✅):** util.js เพิ่ม `askNameDialog(opt)` กล่องตั้งชื่อกลาง (ใช้ 4 จุด: ตั้งชื่อผู้เล่นครั้งแรก/แก้ชื่อผู้เล่น/ตั้งชื่อสัตว์ตอนซื้อ/เปลี่ยนชื่อสัตว์ — id เดิม `#pf-name-input/#pf-name-err/#pf-name-ok/#pf-name-cancel`) / auth.js refactor `authAskProfileName` ใช้กล่องกลาง + `authApplyProfileName(name)` (จุดเดียว: save+push profile+push save+รีเฟรช presence/leaderboard) + `authEditProfileName()` / state.js `newPet(type,name)` + migration สัตว์ไม่มีชื่อ→ชื่อชนิด / ui.js: student-chip เป็น `📛 <b>ชื่อในเกม</b> ✏️ · 🎓 ชื่อจริง · ชั้น...` (ปุ่ม `#btn-edit-name`) · ซื้อสัตว์: askConfirm เดิม → ต่อด้วยกล่องบังคับตั้งชื่อ (ยกเลิก=ไม่ซื้อ เหรียญไม่หัก) · ชื่อสัตว์โชว์แทนชื่อชนิดที่ plate-head (+ปุ่ม `#btn-pet-rename` ✏️)/pet-tabs/sick-banner/fav-tag/showLevelUp/สถิติ (สถิติมีวงเล็บชนิดต่อท้าย) — escape ทุกจุด / lobby.css `.chip-edit` · **ทดสอบผ่านครบ:** ลงทะเบียน→chip โชว์ 📛 ชื่อในเกม+✏️ / แก้ชื่อในเกม: prefill ชื่อเดิม·ยกเลิกได้ชื่อเดิมอยู่·เปลี่ยนแล้ว chip+การ์ดเพื่อน+push profile อัปเดตทันที (spy 2 รายการ) / ซื้อหมา: ยืนยัน→กล่องตั้งชื่อ·ยกเลิก=ไม่ซื้อเหรียญเท่าเดิม·ชื่อหยาบ "ควยทอง" ถูกบล็อกกล่องไม่ปิด·"บ็อบบี้" ซื้อสำเร็จ −3,000 ชื่อขึ้น plate+tab / เปลี่ยนชื่อน้อง→"Lucky 8" อัปเดต plate/tab/สถิติ "Lucky 8 (น้องหมา)" / checkName 16 ตัวถูกบล็อก·1 ตัวผ่าน (ข้อ 7 ใช้ 1–15) / migration เซฟเก่า: สัตว์ไม่มีชื่อ→"น้องหมา" + ไม่มี profileName→กล่องบังคับ (ไม่มีปุ่มยกเลิก) ตั้งแล้ว push ถูก / ไม่มี console error-warning / จบงานรีเซ็ต localStorage คืนหน้า login · **หมายเหตุทดสอบ:** กดปุ่มใน overlay ผ่าน eval ควรแยกเป็นคนละ eval call กับตอนสร้าง overlay (คลิกรวดเดียวใน call เดียวอาจ race — เจอ 2 ครั้งในรอบนี้) · screenshot timeout ได้ ให้เช็ก DOM ผ่าน eval แทน

**รอบสิบแปด (6 ก.ค. 2026 — Fable session ข้อ 0.2 display name ✅):** สร้าง `js/data/badwords.js` (BAD_PART/BAD_EXACT + nameNormalize + checkName — แชร์กับข้อ 7 ตั้งชื่อสัตว์: `checkName(raw,1,15)`) / state.js `profileName` + migration / index.html ช่อง `#reg-nick` + script badwords / main.js validate ชื่อตอนลงทะเบียน + เรียก `authAskProfileName()` ใน bootGame / auth.js `authWriteProfileName`/`authPushProfile` (push ตอนตั้งชื่อ+ทุก login) + กล่องบังคับตั้งชื่อ + logout ลบ presence ด้วย `onlineKey()` / online.js `onlineKey()` = uid (fallback onlineId) + `onlineDisplayName()` ใช้ profileName ก่อน / ui.js ไฮไลต์ตัวเองด้วย uid + `escapeHTML()` (เพิ่มใน util.js) ชื่อ/ชั้น/act จาก DB ทุกจุด · **ทดสอบผ่านครบ:** ตัวกรอง 27/27 เคส (คำปกติผ่านหมด รวม ฟักทอง/แมงมุม/กะหรี่ปั๊บ/Bob/กูเกิล · คำหยาบโดนดักทุกท่า: เว้นวรรคหลบ "ค ว ย" / ตัวซ้ำ fuuuck / เลขแทน Sh1t / ไม่มีวรรณยุกต์ เหีย / แฝงในชื่อ) / ลงทะเบียน: ชื่อหยาบ+อักขระพิเศษถูกบล็อกพร้อม toast · ชื่อดี " น้องบีม   007 " → จัดระเบียบเป็น "น้องบีม 007" เข้าเกม + push profile {uid:test1} / ผู้เล่นเดิมเซฟไม่มี profileName → เข้า dashboard เจอกล่องบังคับตั้งชื่อ ชื่อหยาบขึ้น error แดงกล่องไม่ปิด ชื่อดีปิดกล่อง+push+การ์ดอัปเดตทันที / การ์ดเพื่อน+leaderboard: ชื่อแฝง `<img onerror>`/`<script>` โชว์เป็นข้อความเฉยๆ ไม่ทำงาน (escapeHTML) + ไฮไลต์ตัวเอง "⭐ ...(หนูเอง)" ด้วย uid ถูกแถว / ไม่มี console error-warning ทั้งเซสชัน / จบงานรีเซ็ต localStorage คืนหน้า login — **⚠️ งานค้างฝั่งผู้ใช้: วาง Security Rules ฉบับใหม่ใน console** (เพิ่ม validate `/users/$uid/profile/name` ≤20 + ผูก presence/leaderboard เข้ากับ `auth.uid === $uid` — rules ฉบับเต็มดูในแชท session รอบสิบแปด หรือขอ Claude สร้างใหม่ได้) แล้วทดสอบ login จริงบน Pages: ตั้งชื่อ → ชื่อขึ้นการ์ดเพื่อน/กระดาน → เช็ก `/users/<uid>/profile/name` ใน console · **หมายเหตุ:** rules ใหม่ปิดเขียน presence/leaderboard แบบไม่มี token → seed ทดสอบผ่าน REST ธรรมดาไม่ได้แล้ว (ตามแผนเดิม — ใช้หน้าเกมจริง/Emulator แทน) · เอนทรีเก่าใน DB ที่ key เป็น onlineId ถูกลบทิ้งแล้ว (เปลี่ยนเป็น uid)

**รอบสิบเจ็ด (5 ก.ค. 2026 ดึก — Fable session ยกเครื่อง UI ทั้งเกม "อัพเดท 5725691826" ✅ + hotfix production):**
- **🚨 hotfix ด่วน (commit `3bc1bfe`):** พบว่า commit `9accb89` (รอบสิบหก) เผลอติด script tag `js/data/vocab/band1-5.js + index.js` ขึ้น production ทั้งที่ไฟล์ยังไม่ถูก commit (งานคำศัพท์ Sonnet ค้างครึ่งทาง — ในเครื่องมีแค่ band1.js untracked) → เกมบน Pages เปิดไม่ได้ทั้งเว็บ (`gradeBand is not defined`) — แก้โดย stash งาน UI → ชี้กลับ `js/data/vocab.js` เดิม → commit+push แยกก่อน → stash pop ทำงานต่อ · **บทเรียน: commit ต้องตรวจว่าไม่มี script tag ชี้ไฟล์ untracked**
- **โครงใหม่ (commit `c0dd2a1` เฟส 1-2 + เฟส 3):** บังคับแนวนอน (จอตั้ง → overlay "หมุนจอ" ผ่าน `@media orientation:portrait`) · `screen-dashboard` กลายเป็น Lobby: แถบบน (โปรไฟล์+นาฬิกา · `#rank-mini` คลิกเปิดแผงแรงค์ · เหรียญ 2 pill · 🔊 · 🚪 logout) + เมนูไอคอนซ้าย `.rail-btn` (บ้าน/ร้านค้า/ฟาร์ม/โรงงาน/สถิติ) + **สัตว์ active การ์ดกระจกกลางจอ** + การ์ดสดขวา (เพื่อนออนไลน์+Top50 scroll ภายใน) + ปุ่มเหลืองใหญ่เล่นเกมขวาล่าง · **ระบบแผง `js/lobby.js`**: `openPanel/closePanel` + `PANEL_TITLES` · การ์ดเดิมย้ายเข้าแผงโดย**คง id เดิมทุกตัว ไม่แตะ logic** (panel-home: rain+home · panel-shop: ห้องแต่งตัว+มือถือ+คอม · panel-farm · panel-factory: collect-card · panel-rank: rank-card) · ESC/แตะฉากหลัง/`showScreen` (ถูก wrap ใน lobby.js) = ปิดแผงอัตโนมัติ · **ธีม `css/lobby.css`** โหลดทับ style.css: พื้นหลัง theme_bg_wide + ฟอนต์ Kanit + หน้าอื่นเป็นแผงลอยขาว `.screen.active:not(#screen-dashboard)` (⚠️ `:not(#id)` specificity สูง — rule ที่จะชนะต้องเขียน `#id.screen.active`) · เกมจับคู่จอกว้าง ≥640px เป็น grid 2 คอลัมน์ EN|TH · ร้านสัตว์ 3 การ์ดแถวเดียว
- **เฟส 3 การ์ดสินค้า Trade HQ:** แคตตาล็อกโรงงาน + คลังของฉัน ใช้ `.hq-card` (หัวการ์ดน้ำเงินชื่อสินค้า + ภาพใหญ่ + badge 🔤แต้ม/×จำนวน + ดาว tier + **แถบราคาพื้นทอง = ปุ่มผลิต/ตั้งขาย**) ใน `.hq-grid` (auto-fill 136px — ~5 ใบ/แถวในแผง) — pagination/ปัดซ้ายขวา/อนิเมชันสไลด์เดิมใช้ต่อ (`mkt-catalog`+`factory-list` คงไว้) · ร้านแต่งตัวจัดทรงเดียวกันด้วย CSS ล้วน (`.it-name` order:-1 เป็นหัวการ์ด) markup เดิม
- **ทดสอบผ่านครบ:** ลงทะเบียน→Lobby / rank-mini "ซิลเวอร์ III" (fix tier ซ้ำ) / แผงทั้ง 5 เปิด-ปิด เนื้อหา render ครบ / เกมจับคู่ 2 คอลัมน์ จับคู่ถูก +10 / หมวดคำศัพท์+สถิติ+ร้านสัตว์เป็นแผงลอย / overlay หมุนจอโชว์เฉพาะจอตั้ง / น้องป่วยเพราะฝน 19:00 จริง → รักษา 1,000 → เมนูอาหาร → feast กิน / กดผลิตจากการ์ด hq / เปลี่ยนหน้า pager / คลัง ×2 badge / กล่องตั้งราคาขาย 300 → listing row / ซื้อโบว์ 500 ใส่ทันที / เปิดแผงค้าง→เปลี่ยนจอ→ปิดเอง / ESC ปิด / ไม่มี console error-warning ทั้งเซสชัน / จบงานรีเซ็ต localStorage คืนหน้า login
- **เหลือ/ตามต่อ:** ผู้ใช้ลองบนมือถือจริง (แนวนอน+ปัดสัมผัส) · ถ้าจอเตี้ยกว่า 430px มี media query ย่อส่วนแล้วแต่ควรดูของจริง · หน้า quiz/สอบยังใช้โครงเดิมในแผงลอย (ใช้ได้ ไม่ได้แตะ logic)
- **เก็บงานตาม feedback ผู้ใช้ (รอบเดียวกัน):** (1) **น้องตัวใหญ่เต็มเวทีแบบตัวละคร lobby เกม shooter** — `.stage-hero` เป็น layer absolute หลังสุด น้องยืนชิดล่าง scale ตามความสูงจอ (`--pet-zoom` 1.4 / ≥480px:1.9 / ≥620px:2.5) + ข้อความ/แถบสถานะทั้งหมดย่อรวมใน **แผ่นสถานะกระจกเข้ม `.stage-plate` แผ่นเดียวทับปลายเท้า** (ตัวอักษรเล็กลง scroll ภายในถ้าล้น · restructure markup ใน renderDashboard เป็น stage-hero+stage-plate · ยังไม่มีสัตว์ = การ์ดกระจกเดิม class `no-pet`) (2) **แผงทุกแผงเปลี่ยนพื้นขาว → ฟ้าไล่เฉดแบบ Global Trade HQ** (feedback: "พื้นขาวเหมือนเว็บ shopping") — `.panel-box` gradient ฟ้า 3 ระดับ · `.hq-card`/`.shop-item` หัวการ์ด gradient ฟ้าสด ตัวการ์ดฟ้าอ่อน · การ์ดใน panel โปร่งขาว 50% — ทดสอบผ่าน: หัวน้องไม่โดนตัดทุกขนาดจอ (390/600 สูง) · แผงโรงงานอารมณ์เกม · ไม่มี console error · commit แล้ว — **พร้อมรอบเดียวกัน: ผู้ใช้วางภาพการ์ดเทศกาล 12 ไฟล์ → img/gifts/ ครบ 50/50** 🎉

**รอบสิบหก (5 ก.ค. 2026 ดึก — Fable session ปรับ UI ตามผู้ใช้สั่ง 2 จุด):** (1) **ถอดปุ่ม "เริ่มเกมใหม่ทั้งหมด (ลบข้อมูล)" ออกถาวร** — อันตราย เด็กเข้าใจผิดว่าเป็น logout เซฟหายถาวรทั้ง cloud (เหลือปุ่มออกจากระบบปุ่มเดียว · รีเซ็ตทำได้ทาง console เท่านั้น) (2) **แคตตาล็อกโรงงานแบ่งหน้า 5 รายการ/หน้า** — `factoryPage`/`factorySlide`/`FACTORY_PAGE_SIZE` ใน ui.js + แถบ `.mkt-pager` (ลูกศร ◀▶ + จุด `.pg-dot` + "หน้า x/y") + **ปัดซ้าย-ขวาบนจอสัมผัส** (touchstart/touchend บน `#factory-list` เกณฑ์ dx>45 และ dx>1.5|dy| · `touch-action:pan-y` เลื่อนแนวตั้งไม่เพี้ยน) + อนิเมชันสไลด์ตามทิศ · เปลี่ยนหมวด→รีเซ็ตหน้า 1 · **ทดสอบผ่าน:** 5 รายการ/หน้า 10 หน้า@ทุกหมวด · ลูกศรหน้า 2 ถูกชุด+dot ขยับ+animation class มา · หน้า 10 ปุ่ม next disabled · กรองหมวดเหลือ 2 หน้า รีเซ็ตหน้า 1 · TouchEvent จำลอง: ปัดซ้าย→หน้า 2 ปัดขวา→กลับ ปัดแนวตั้ง→ไม่เปลี่ยน · ไม่มี console error · commit `f2fa11c` (ปุ่ม reset) + pagination push แล้ว

**รอบสิบห้า (5 ก.ค. 2026 ดึก — Fable session ข้อ 0.1 Google Login + sync เซฟ cloud):** ผู้ใช้เปิด Google provider (Public-facing name: Pet Vocab Adventure) + authorized domain `iamsuperrich2025.github.io` ใน console เอง (Claude พาทีละขั้น) + เลือกแบบ ก. บังคับ login · สร้าง js/auth.js + `#screen-login` + ปุ่ม logout + CSS `.login-*`/`.google-btn` / state.js เพิ่ม `savedAt` (บัมพ์ใน saveState) + `ownerUid` + migration / online.js โหลด `firebase-auth-compat.js` เพิ่ม → เรียก `authStart()` แทน `onlineStart()` (initializeApp ย้ายไป authStart · onlineStart เรียกหลัง login ใน authEnterGame) / main.js แยก `bootGame()` + guard interval + reset ลบ cloud · **ทดสอบ mock ผ่านครบ** (stub authFetchCloud/Write/Delete + onlineStart กันเขียน DB จริง): หน้า login ปุ่ม Google โชว์หลัง SDK โหลด / เคส A บัญชีใหม่ → ลงทะเบียน (โชว์อีเมล) → push เซฟ ownerUid ถูก / เคส B เซฟเก่าไม่ผูก → กล่องถามผูก (สรุปชื่อ+เหรียญ+สัตว์) → ผูกแล้ว push / เคส B2 ไม่ผูก → ยืนยันซ้ำ+ปุ่มกลับใช้ได้ → ลบเริ่มใหม่จริง / เคส C cloud ใหม่กว่า → โหลดทับเครื่อง / เคส D เครื่องใหม่กว่า (เจ้าของเดียวกัน) → ใช้ของเครื่อง+push ขึ้น / เคส E เซฟของบัญชีอื่น → เริ่มใหม่เงียบไม่ถามผูก / reset → เซฟ cloud ถูกลบ / logout → ล้าง cache กลับหน้า login / หน้าประตู offline โชว์ปุ่มลองใหม่ / ไม่มี console error-warning ทั้งเซสชัน / จบงานรีเซ็ต localStorage · **✅ ผู้ใช้ทำครบแล้ว (5 ก.ค. ดึก):** วาง rules (ทดสอบ REST ผ่าน 15 เคส — allow/deny ทุกโซนถูกต้อง) + ทดสอบ login จริงบน Pages ผ่านครบ (login→ลงทะเบียน→ปิดเปิดใหม่เซฟอยู่→logout/login เซฟกลับมา)

**รอบสิบสี่ (5 ก.ค. 2026 — Fable session ระบบโรงงานผลิต 🏭 + ออเดอร์พิเศษ 📦 + จัด layout):** เขียน collectibles.js ใหม่ 50 ชิ้น (+tier common+COLLECT_CATS+words) / state.js (producing/orders/producedCount/nextOrderAt + addCraft + newOrder/orderTick + migration) / game.js hook 2 จุด / ui.js (renderFactory/renderOrdersUI/startProduce/cancelProduce/deliverOrder/renderOrderClock แทน marketListings/buyFromMarket เดิม + **fix bug การ์ด collect ไม่ render ตอนมีสัตว์**) / CSS .craft-*/.order-* / index.html ย้าย rain-banner ไปติด home-card + ห้องแต่งตัวไปติด pet-card · **ทดสอบผ่านครบ:** โหลดสะอาด 50 ชนิด 5 หมวด ภาพครบ 50 / ออเดอร์แรกผู้เล่นใหม่เป็น common เสมอ (เพิ่ม guard producedCount===0 หลังเจอเคสสุ่มได้สร้อย 300k) / เริ่มผลิตคัพเค้ก → จับคู่ถูกจริง 1 คู่ +10🪙 progress 0→1 / ตั้ง 19/20 + จับคู่ → **ฉาก "🏭 ผลิตสำเร็จ!" ภาพจริง** เข้าคลัง producedCount=1 producing=null / ส่งมอบออเดอร์ว่าว +1,640 ของออกจากคลัง แถวหาย / ตั้งขาย 300 (hint 🔥) mock +21 นาที → ขายได้ +300 กล่องแจ้ง / สอบถูก 10/10 → +10 แต้ม โชว์ในกล่องผลสอบ "🏭 แต้มผลิต +10 (โดนัทเคลือบ 10/30)" / เปลี่ยนสินค้ากลางคัน → กล่องยืนยัน กด no แต้มเดิมอยู่ / ออเดอร์ใหม่ spawn + นับถอยหลังเดิน / ยกเลิกผลิต → โรงงานว่าง / migration เซฟเก่า+ข้อมูลเสีย (producing id ปลอม→null · orders payout ไม่ใช่ number→คัดทิ้ง · nextOrderAt string→0) / ไม่มี console error-warning ทั้งเซสชัน / รีเซ็ต localStorage คืนหน้าลงทะเบียน / commit `441a40e` push แล้ว (รวมภาพ 50 ไฟล์)

**รอบสิบสาม (5 ก.ค. 2026 — Fable session ระบบออนไลน์จริง Firebase: เพื่อนจริง + Leaderboard Top 50):** สร้าง `js/data/firebase-config.js` + `js/online.js` + การ์ด `#leaderboard-card` + แก้ renderOnlineCard (จริง/จำลอง) + CSS `.lb-*`/`.online-live`/`.online-note` + `state.onlineId` (migration: ไม่ใช่ string → null) · **Firebase โปรเจกต์ `english-pet-game`** (Google account ผู้ใช้ freddommun@gmail.com · Spark ฟรี · RTDB **asia-southeast1** · ผู้ใช้กดสร้างเองใน console ผมพาทีละขั้น) · **ทดสอบครบ:** DB ยังไม่สร้าง → fallback เพื่อนจำลอง+กระดานโชว์ "ต่ออินเทอร์เน็ต..." ไม่มี error / สร้าง DB → ต่อติด ป้าย 🌏 ออนไลน์จริง + ตัวเองขึ้นกระดานอันดับ 1 ชื่อย่อ "ทดสอบ ร." / seed 60 ผู้เล่น+4 presence ผ่าน REST (python) → การ์ดเพื่อนโชว์ 3 คนสด **ผีค้าง 20 นาทีถูกกรองทิ้ง** นับ 4 คนรวมตัวเอง / กระดานตัดเหลือ 50 พอดี เรียงมาก→น้อย 🥇🥈🥉 ถูก ตัวเอง 0 เหรียญตกกระดาน hint "เก็บเหรียญเพิ่ม..." / addCoins 600,000 + push → ขึ้น 🥇 อันดับ 1 แถวไฮไลต์+⭐ "หนูอยู่อันดับที่ 1 จาก 50 คน" **อัปเดตสดไม่ต้อง reload** / console ไม่มี error (มีแค่ warning indexOn — หายเมื่อวาง Rules จริง) / ลบข้อมูลทดสอบทั้ง DB (DELETE /.json → null) + รีเซ็ต localStorage คืนหน้าลงทะเบียน — **✅ Security Rules ฉบับจริงวางแล้ว (5 ก.ค. 2026):** อ่าน/เขียนได้เฉพาะ `/presence` + `/leaderboard` (โซนอื่นปิดหมด) + `.validate` ฟิลด์ครบ/ชนิดถูก/จำกัดความยาว (n≤40, g≤8, act≤60, coins เป็นตัวเลข≥0) + `.indexOn: coins` — **ทดสอบผ่าน 10 เคส** (อ่าน-เขียนที่ถูกต้อง=ผ่าน · อ่านทั้งฐาน/โซนอื่น/เขียนฟิลด์ไม่ครบ/ชื่อยาวเกิน/coins เป็น string=โดนบล็อก 401 หมด) + reload แล้ว warning indexOn หายจริง · **เฟส 2 (ตลาด `/market`) ต้องเพิ่ม rules ของโซนใหม่ด้วย ไม่งั้นเขียนไม่ได้**

**รอบสิบสอง (4 ก.ค. 2026 — Opus session เพิ่มระบบสินค้าสะสม + ตลาดขายต่อ Global Trade HQ เฟส 1):** สร้างไฟล์ `js/data/collectibles.js` (12 ชิ้น 4 tier) + การ์ด `#collect-card` (index.html) + probeCollectImages (main.js/images.js) + assetValue/migration/marketTick (state.js) + UI ครบ (ui.js) + CSS · **ทดสอบครบใน preview:** โหลดไม่มี console error / ตลาดโชว์ 8 ประกาศเรียงถูก→แพง + ผู้ขายจำลอง + ป้าย %ถูก-แพง / **dropdown ค้นหา goldcar → เหลือ 5 ประกาศ goldcar ล้วน** / ซื้อ teddy −14,800 → เข้าคลัง + **ฉากเปิดภาพใหญ่** (title/name/tier ถูก, ภาพ+ประกายสวย) / assetValue นับ teddy 20,000 (ราคาฐาน) net worth ขึ้น 5,200 (ซื้อต่ำกว่าฐาน) / ตั้งราคาขาย 12,000 (ratio 0.6 hint "🔥 ตั้งถูกมาก...") ย้ายเข้า listings คลังว่าง net worth คงเดิม (ยังนับของที่ลงขาย) / mock +21 นาที careTick → ลูกค้าซื้อ +12,000 listings ว่าง tradeSold 1 กล่องแจ้งโชว์ กดรับทราบเคลียร์ / ตั้งขายแพง 50,000 (ratio 2.5) mock +2 วัน → ไม่มีคนซื้อ / cancelListing คืนเข้าคลัง / migration เซฟเก่าไม่มี field → []; คัดข้อมูลเสีย (collection เหลือ teddy, listings เหลือ rccar, tradeSold 'notarray'→[]) / กรอบสีตาม tier ถูก / ไม่มี console error / รีเซ็ต localStorage คืนหน้าลงทะเบียน — **หมายเหตุ:** ผู้ซื้อ-ขายเป็น "จำลอง" (โครงเดียวกับการ์ดเพื่อนออนไลน์) เฟส 2 ค่อยต่อ Firebase ให้ผู้เล่นจริงเทรดกัน โดยสลับแค่แหล่งข้อมูล marketListings/listings ไม่ต้องรื้อ UI

**รอบสิบเอ็ด (4 ก.ค. 2026 — Opus session อัปเกรดแรงค์เป็น net worth + ยกเลิกข้อ 2):** เปลี่ยนแรงค์จาก `state.coins` → **net worth = เหรียญ + `assetValue()`** (ทรัพย์สินทุกชิ้นที่ถือครองคิดราคาเต็ม — มือถือ `PHONE_PRICE` 10,000/คอม `COMP_PRICE` 50,000/สัตว์/บ้าน/แอร์/เสื้อผ้า/ต้นไม้; ราคาขายคืนใช้ตอนขายจริงเท่านั้น) · เกณฑ์ยากขึ้นเท่าตัว (บรอนซ์ 0/ซิลเวอร์ 10k/โกลด์ 200k/แพลตินัม 1M/ไดมอนด์ 2M/มาสเตอร์ 4M/จอมพล 20M) · ตรวจเลื่อน/ลดย้ายจาก `addCoins` → **`refreshRank()` ใน `careTick`** (เทียบ `state.rankKey` ที่เก็บไว้ + `rankFromKey()`) · **ทดสอบครบ:** assetValue ถูก (207,500 ทรัพย์สินครบชุด) / ซื้อมือถือ-คอม net worth เท่าเดิม (แรงค์ไม่ตก) / ขายคืน net worth ลดตามค่าเสื่อม (เงียบ ไม่ฉลองผิด) / ได้เหรียญข้ามแรงค์→ฉาก · ข้ามขั้นย่อย→toast · จ่ายกิน/บิล→ลดเงียบ / migration rankKey null→careTick แรกตั้งค่าเงียบ / การ์ดแรงค์+สถิติโชว์ breakdown (เหรียญ+ทรัพย์สิน+รวมสุทธิ) / ไม่มี console error / รีเซ็ตคืนหน้าลงทะเบียน · **ยกเลิกข้อ 2 (โรงเรียนสัตว์) ตามคำสั่งผู้ใช้ → แผน upgrade เสร็จหมดทุกข้อ**

**รอบแรก:** ลงทะเบียน ป.5 ได้ศัพท์ band 3 / จับคู่ +10🪙+2RP / เคลียร์รอบ +20🪙+5RP / toast ขั้นย่อย / ฉาก RANK UP / ซื้อหมา 3,000 + ล็อกร้านตอนแรกเกิด / หิว 18:00 → ป่วย 20:01 / รักษา 1,000 / feast ข้ามมื้อ / ร้อนป่วย (cause:heat) / มังกร immune / เพิง 500 ยังร้อน / บ้านกลาง+แอร์ 75,000 หายร้อน / ซื้อมังกรตัวที่ 2 + สลับแท็บ / โบว์ 500 ใส่เฉพาะตัว active / สอบผ่านครั้งแรก +100🪙+100RP / migration เซฟเก่าสมบูรณ์

**รอบสอง:** นาฬิกาเดินทุกวินาที / การ์ดเพื่อนจำลองแสดง 7 คน+ตัวเอง / EXP curve ใหม่: Lv.2 ที่ 250 พอดี, Lv.3 ที่ +600, ถัดไป /400 / ภาพ rank_bronze.png แสดงในการ์ดแรงค์จริง / ไม่มี console error / รีเซ็ต localStorage คืนหน้าลงทะเบียนแล้ว

**รอบสี่ (4 ก.ค. 2026 — upgrade ข้อ 3):** ร้านบ้านโชว์ราคาใหม่+ค่าบำรุง/เดือน / ซื้อเพิง −1,000 บิลเดือนแรกฟรี / mock 1 ส.ค. บิล 🪙5 ออก+ปุ่มจ่าย / mock 5 ส.ค. ค้างจ่าย → ทรุดโทรม (tag+ฟิลเตอร์หม่น) / จ่ายแล้วหายทรุด / ค้างข้ามเดือน → ฉากบ้านพัง home=null ac=false / เซฟเก่ามีบ้านไม่มีบิล → เริ่มบิลฟรีเดือนนี้ / ไม่มี console error — **หมายเหตุ:** เบราว์เซอร์ cache JS จาก http.server เหนียว ให้ `fetch(src,{cache:'reload'})` ทุกสคริปต์ก่อน reload เวลาไฟล์ไม่อัปเดต

**รอบหก (4 ก.ค. 2026 — upgrade ข้อ 6 ค่าน้ำ):** โค้ดเสร็จครบทุกไฟล์ (รวม refactor เครื่องยนต์สาธารณูปโภคกลาง UTILITIES/UTILITY_UI แทน payElec/buyTransformer เดิม) — smoke test ผ่าน

**รอบเจ็ด (4 ก.ค. 2026 — ทดสอบเต็มชุดข้อ 6 ✅ ผ่านครบ 8 รายการ):** ซื้อเพิงผ่าน UI จริง −1,000 บิลน้ำฟรีเดือนแรก + ร้านบ้าน/กล่องยืนยันโชว์ 3 ค่า (บำรุง+ไฟ+น้ำ) ทั้ง 3 หลัง / mock 1 ส.ค. บิลน้ำ 🪙5 ออก จ่ายผ่านปุ่ม → toast "🚰 จ่ายค่าน้ำเรียบร้อย" + กล่อง "✅ จ่ายค่าน้ำเดือนนี้แล้ว" / ค้างน้ำ+ไฟ ก.ย.→ต.ค. → ตัดทั้งคู่ บิลทบ 10 / payUtility('water') ก่อนติดตั้ง → บล็อก+toast เหรียญไม่ขยับ / หมา+มังกร Lv.3 ขาดน้ำครบ 6 ชม. → ป่วย cause:thirst ทั้งคู่ (มังกรไม่ immune น้ำ) banner ถูก / รักษา 1,000 → thirstFrom นับรอบใหม่ทันที (ยังถูกตัดน้ำ) แถบฟ้าโผล่ต่อ / ตัดไฟ+น้ำพร้อมกัน: กล่องแดง 2 ใบ + tag 2 อัน + ภาพใช้ฟิลเตอร์ home-dark-img (มืดชนะแห้ง) / ซื้อหม้อแปลง+ติดตั้งน้ำ+จ่ายค้างรวม 2,020 → ไฟ-น้ำกลับมา tag/ฟิลเตอร์หาย thirstFrom เคลียร์ทุกตัว / ค้าง maint ข้ามเดือน (ตอน waterCut+plumbingBought=true) → บ้านพัง bills={} waterCut/plumbingBought/powerCut ถูกล้างหมด + ฉากบ้านพัง / migration: เซฟมีบ้านไม่มี bills.water → บิลน้ำฟรีเดือนนี้ + pet ไม่มี thirstFrom → null / เซฟไม่มีบ้านแต่ waterCut+plumbingBought ค้าง → เคลียร์หมด / ไม่มี console error-warning ทั้งเซสชัน / รีเซ็ต localStorage คืนหน้าลงทะเบียนแล้ว

**รอบห้า (4 ก.ค. 2026 — upgrade ข้อ 5):** ซื้อบ้าน → บิลไฟเดือนแรกฟรี+ร้านบ้านโชว์ค่าบำรุง+ค่าไฟ / mock 1 ส.ค. บิลไฟ 🪙500 ออกคู่ค่าบำรุง จ่ายแยกกันได้ / mock ค้างไฟ ก.ย.→ต.ค. → ถูกตัดไฟ บิลทบ 1,000 บ้านมืด (ฟิลเตอร์) tag ถูกตัดไฟ heatProtected=false แถบร้อนขึ้นแม้มีแอร์ / จ่ายก่อนซื้อหม้อแปลง → บล็อก+toast / ร้อนครบ 6 ชม.ตอนตัดไฟ → ป่วย cause:heat รักษา 1,000 / ซื้อหม้อแปลง 1,000 → ปุ่มจ่ายโผล่ / จ่ายค้าง 1,000 → ไฟกลับมา heatFrom เคลียร์ ฟิลเตอร์หาย / ค้างทั้ง maint+elec ข้ามเดือน → บ้านพัง ล้างบิลไฟ+powerCut / migration เซฟเก่ามีบ้านไม่มีบิลไฟ → บิลฟรีเดือนนี้ / เซฟไม่มีบ้านแต่ powerCut ค้าง → ถูกเคลียร์ / ไม่มี console error / รีเซ็ตคืนหน้าลงทะเบียน — **หมายเหตุ:** screenshot อาจติดเฟรมเก่า ให้เช็ก DOM ด้วย eval ประกอบ

**รอบสาม (4 ก.ค. 2026 — upgrade ข้อ 9):** slot 3 ชม.ปัดขอบถูกทุกช่วงเวลา / เมนูโชว์ราคาใหม่+ป้าย "อิ่ม 3/6 ชม." / กินคุกกี้ -100 มื้อถัดไป +3 ชม. / feast -1,000 อิ่มข้าม slot (6 ชม.) / หิว 1.5 ชม.ยังไม่ป่วย 2 ชม.ป่วย (cause:hunger) / migration เซฟเก่า (pet เดี่ยว+hunger เกจ) ผ่าน fedUpTo=slot ปัจจุบัน / ไม่มี console error

**รอบแปด (4 ก.ค. 2026 — Opus session ทำข้อ 4, 7, 8, 10, 11 รวด + เริ่มข้อ 12):**
- **ข้อ 4 (ฝน) ✅ ผ่าน:** ฝนตกทุกวัน 19:00–20:00 (`rainNow`/`rainStartToday`/`nextRainStart` ใน homes.js · `weatherNow` คืน rain เฉพาะช่วงนี้ นอกนั้นสุ่มแค่ แดด/ร้อน/เมฆ) / แถบนับถอยหลัง `#rain-banner` (`renderRainBar` เดินพร้อมนาฬิกาทุกวินาที) / ไม่มีบ้านสภาพดีตอนฝน → ป่วย `cause:'rain'` ครั้งเดียวต่อฝน 1 รอบ (`p.rainSickDay`) รักษา 1,000 / บ้านสภาพดี (`rainProtected`=มีบ้าน & ไม่ decayed) ปลอดภัย / บ้านทรุดโทรมก็เปียก / วันถัดไปฝนตกป่วยใหม่ได้
- **ข้อ 7 (มือถือ) ✅ ผ่าน:** ซื้อ 10,000 ขายคืน 6,000 (`PHONE_*` ใน items.js) / โบนัสจับคู่ +5/ข้อ (จับคู่ปกติ 10→15) ระงับตอนถูกตัดเน็ต (→10) / ค่าเน็ต 1,000/เดือน (บิล id `net` ใน UTILITIES — เพิ่ม field `active()` ให้ทะเบียน + `HOME_UTILITIES` แยกบิลบ้านออกจากเน็ต) เน็ตไม่มี fixKey จ่ายค้างได้เลย / การ์ด `#phone-card` (`renderPhoneCard`) / migration มี/ไม่มี phone ครบ / prompt ภาพ `item_phone.png`+`item_phone_nonet.png` เพิ่มใน PROMPTS_RANK_HOME.md
- **ข้อ 8 (โบนัสสอบตามบ้าน) ✅ ผ่าน:** `HOMES[].quizBonus` (เพิง 0/บ้านกลาง 100/ปราสาท 200) จ่ายทุกครั้งที่ทำสอบครบ 10 ข้อ (ใน `finishQuiz`) โชว์ในกล่องผลสอบ+ร้านบ้าน — เพิง +20(ผ่านซ้ำ), กลาง +120, ปราสาท +220
- **ข้อ 10 (แรงค์) ✅ ผ่าน:** ทำครั้งแรกแบบ "แรงค์=เหรียญคงเหลือ" → **ถูกแทนที่ทั้งหมดด้วยระบบ net worth ใน "รอบสิบเอ็ด"** — เกณฑ์/กลไกปัจจุบันดูที่ "รอบสิบเอ็ด" และหัวข้อกติกา "แรงค์" เท่านั้น (`addRP` เหลือแค่สะสมแต้มความพยายามโชว์ในสถิติ)
- **ข้อ 11 (คอมพิวเตอร์) ✅ ผ่าน:** ซื้อ 50,000 ขายคืน 30,000 (`COMP_*` ใน items.js) / รายได้ +0.01/วิ (`compTick` ใน state.js เรียกก่อน billTick — ตกเหรียญเต็มทุก 100 วิ เก็บเศษใน `compSince`, สะสมใน `compEarned`) / การ์ด `#computer-card` โชว์ตัวเลขวิ่งสด `#comp-live` (อัปเดตใน renderClock) CSS `.comp-earn` เขียวเรืองแสง / ค่าบริการข้อมูล 5,000/เดือน (บิล id `data`) ค้างข้ามเดือน→`dataCut` รายได้หยุดนิ่ง (compSince เลื่อนตาม now ไม่สะสม) การ์ดเป็นสีแดง `.comp-earn.off` / จ่ายครบ→`onRestored` รีเซ็ต compSince / migration ครบ / prompt `item_computer.png`+`item_computer_nodata.png` เพิ่มแล้ว
- **ข้อ 12 (ปลูกผลไม้) ✅ ผ่าน:** **data layer** `js/data/fruits.js` (FRUITS 4 ชนิด: ส้ม 3000/3วัน/ขาย300 · แอปเปิล 3500/4วัน/400 · มังคุด 4000/8วัน/800 · ทุเรียน 5000/8วัน/1000 + `fruitInfo/fruitGrowMs/fruitMsLeft`) + **state layer** (`state.farm=[]` เก็บ `{id, plantedAt}` + migration filter ต้นเสีย state.js:122-123) + **UI layer** `renderFarmCard()` ใน ui.js (ร้านซื้อ 4 ปุ่ม + รายการต้นปลูก + นาฬิกานับถอยหลังต่อต้น) · `fruitCountdown(ms)` ฟอร์แมต วัน/ชม./นาที/วิ · `renderFarmClock()` เดินพร้อมนาฬิกาใน renderClock (อัปเดต `#farm-time-<i>` + ตรวจ `el.dataset.readysig` ต้นสุกใหม่→rebuild ให้ปุ่มขายโผล่) · `buyFruit(id)`/`sellFruit(i)` (**ขายแล้ว plantedAt=now ต้นไม่หาย ออกผลรอบใหม่ทันที** ปลูกไม่จำกัด) · เรียกใน renderDashboard ครบ 2 จุด (no-pet + ปกติ) · CSS `.farm-shop/.farm-buy-btn/.farm-tree(.ready)/.farm-sell-btn` + animation `farm-bounce` · ⬜ prompt ภาพยังไม่มี (ใช้อีโมจิ 🍊🍎🫐🍈 — มังคุดใช้ 🫐)

## 📋 แผน upgrade เดิม (✅ เสร็จครบทุกข้อ 4 ก.ค. 2026 — ข้อ 2 ยกเลิก) + สินค้าสะสม/ตลาดขายต่อ เฟส 1 เสร็จ ("รอบสิบสอง") · **ถัดไป: เฟส 2 ต่อ Firebase (backlog ข้อ 0)**

**เสร็จแล้ว:** ✅ ข้อ 1 (ศัพท์ตามชั้น) · ✅ ข้อ 9 (ราคาอาหาร+หิวทุก 3 ชม.) · ✅ ข้อ 3 (บ้าน+ค่าบำรุง+เสื่อม/พัง) · ✅ ข้อ 5 (ค่าไฟ+ตัดไฟ+หม้อแปลง — "รอบห้า") · ✅ ข้อ 6 (ค่าน้ำ — "รอบเจ็ด") · ✅ ข้อ 4 (ฝน — "รอบแปด") · ✅ ข้อ 8 (โบนัสสอบตามบ้าน — "รอบแปด") · ✅ ข้อ 10 (แรงค์=เหรียญ — "รอบแปด") · ✅ ข้อ 11 (คอมพิวเตอร์ — "รอบแปด") · ✅ ข้อ 12 (ปลูกผลไม้ — "รอบเก้า") · ✅ ข้อ 13 (ค่าจัดการขยะ — "รอบสิบ")

**รอบสิบ (4 ก.ค. 2026 — Opus session ทำข้อ 13 ค่าจัดการขยะ):** data `js/data/homes.js` (`TRASH_RATE=0.005 · TRASH_FINE=500 · trashCost()`) / state `billTick` เพิ่ม special-case `trash` (โครงคล้าย maint แต่**ไม่ตัด/ไม่พัง** — ค้างข้ามเดือน→ปรับ +500 ทบใน `bills.trash.fine`, จ่าย all-or-nothing เลย track ค่าปรับตรงๆ) + migration (มีบ้านไม่มี trash→ฟรีเดือนนี้ · ไม่มีบ้าน→ลบ) + บ้านพัง(maint)→ลบ bills.trash / UI `trashBillUI()`+`payTrash()` ใน ui.js (ไม่ผ่านเครื่อง UTILITIES เพราะไม่มี cutKey) tag "ค้างค่าขยะ" ตอน fine>0 + ร้านบ้าน/กล่องยืนยันโชว์ค่าขยะ + buyHome เพิ่ม `state.bills.trash` / **ทดสอบครบ:** basic ค่าขยะ 5 · medium 500 / บิลออกวันที่ 1 เดือนแรกฟรี / ค้าง ส.ค.→พ.ย. ค่าปรับทบ 500→1,000→1,500 บ้านไม่พังไฟ-น้ำไม่ตัด (medium: due 500→1,500→2,500→3,500) / จ่ายผ่านปุ่มจริง −เหรียญ fine รีเซ็ต 0 เดือนถัดไปบิล base ล้วน / บ้านพังจาก maint → ล้าง bills.trash ด้วย / migration 2 เคสผ่าน / ไม่มี console error / รีเซ็ต localStorage + คืน Date.now native คืนหน้าลงทะเบียน

**รอบเก้า (4 ก.ค. 2026 — Opus session ทำ UI ข้อ 12 ต่อจนจบ + ปุ่มเก็บขายรวบ):** การ์ดสวนแสดง+ปุ่มซื้อ 4 ชนิด / ซื้อส้ม −3,000 + กล่องยืนยัน / นาฬิกานับถอยหลังต่อต้นเดินทุกวินาที (วัน/ชม./นาที/วิ) / ต้นสุก→ปุ่มขายโผล่อัตโนมัติ (renderFarmClock ตรวจ readysig) / ขาย +300 ต้นไม่หายออกผลรอบใหม่ทันที / หลายต้นสุก-ไม่สุกปนกัน index ปุ่มขายถูกตัว / migration: เซฟเก่าไม่มี farm→[] · ต้นเสีย(id ผิด/plantedAt ไม่ใช่ number/null)→คัดทิ้ง / **ปุ่ม `#btn-farm-sellall` (`sellAllFruit`) "เก็บขายทั้งหมดที่สุกแล้ว" โผล่เมื่อสุก ≥2 ต้น** โชว์จำนวน+ยอดรวม กดแล้ว +เหรียญรวมทุกต้นสุก ต้นทั้งหมดออกผลรอบใหม่ ต้นยังไม่สุกไม่ยุ่ง / 1 ต้นสุก→ไม่โชว์ปุ่มรวบ (ใช้ปุ่มของมันเอง) / ไม่มี console error / รีเซ็ต localStorage คืนหน้าลงทะเบียนแล้ว

**เหลือ:** — ไม่มีแล้ว 🎉 (แผน upgrade ทุกข้อเสร็จหมด)
- ~~**ข้อ 2 — โรงเรียนสัตว์**~~ ❌ **ผู้ใช้ยกเลิกแล้ว (4 ก.ค. 2026)** — ไม่ทำ (สเปกเดิม: สมัครเรียน+สอบเลื่อนชั้น+Badge ชั้น — เก็บไว้เผื่ออนาคตถ้าเปลี่ยนใจ)

**กติกาการทำงานทุกข้อ:** แยก data จาก logic · ห้ามทำเซฟผู้เล่นเดิมพัง (field ใหม่ต้องมี default ใน loadState) · ทำเสร็จทดสอบจริงใน preview (mock `Date.now` ได้ — ระวัง cache ดู "รอบสี่") รายงานเป็นตาราง · จบงานรีเซ็ต localStorage คืนหน้าแรก · ทำทีละข้อ รอผู้ใช้ยืนยันก่อนเริ่มข้อถัดไป

## 🆕 แผน upgrade เพิ่มเติม (ผู้ใช้สั่งเพิ่มเข้าคิว 4 ก.ค. 2026 — ต่อท้ายคิวเดิม ทำทีละข้อรอผู้ใช้ยืนยันเช่นเดิม)

- **✅ ข้อ 10 — แรงค์=ยอดเหรียญคงเหลือ (เสร็จ "รอบแปด") → ⭐อัปเกรดเป็น net worth "รอบสิบเอ็ด":** เดิมใช้ `state.coins`; **รอบสิบเอ็ด (4 ก.ค. 2026 ตามคำสั่งผู้ใช้)** เปลี่ยนเป็น **net worth = เหรียญ + มูลค่าทรัพย์สินที่ซื้อไว้ทั้งหมด** (`netWorth()`/`assetValue()` ใน state.js) · **ผู้ใช้เลือก (แก้รอบสิบเอ็ด):** ทรัพย์สินทุกชิ้นที่ยัง**ถือครองคิดราคาเต็ม** (มือถือ `PHONE_PRICE` 10,000 / คอม `COMP_PRICE` 50,000) — ราคาขายคืนใช้ตอนขายจริงเท่านั้น (ที่ให้เป็นเหรียญไปแล้ว) เพื่อให้ซื้อแล้ว net worth เท่าเดิมเป๊ะ · **เกณฑ์ยากขึ้นเท่าตัว** (บรอนซ์ 0 / ซิลเวอร์ 10,000 / โกลด์ 200,000 / แพลตินัม 1,000,000 / ไดมอนด์ 2,000,000 / มาสเตอร์ 4,000,000 / จอมพล 20,000,000) · ตรวจเลื่อน/ลดย้ายจาก `addCoins` → `refreshRank()` ใน `careTick` (เทียบ `state.rankKey` ที่เก็บไว้ — กันขายทรัพย์สินฉลองผิด/ซื้อของแรงค์ตก) · การ์ดแรงค์+หน้าสถิติโชว์ breakdown (เหรียญ+ทรัพย์สิน+รวมสุทธิ) · RP ยังเก็บเป็นแต้มความพยายาม · migration: rankKey null → careTick แรกตั้งค่าเงียบ ไม่ฉลอง / ทดสอบครบ: net worth ถูก · ซื้อไม่ตก · ขายไม่ฉลองผิด · ได้เหรียญข้ามแรงค์→ฉาก · ข้ามขั้นย่อย→toast · จ่ายกิน/บิล→ลดเงียบ · ไม่มี console error
- **✅ ข้อ 11 — คอมพิวเตอร์ (เสร็จแล้ว "รอบแปด"):** 50,000 / ขายคืน 30,000 / +0.01 เหรียญ/วิ (compTick) / ตัวเลขวิ่งสด / ค่าบริการข้อมูล 5,000/เดือน (บิล `data`) ตัดแล้วรายได้หยุด / prompt ภาพเจนแล้ว
- **✅ ข้อ 12 — ปลูกผลไม้ (เสร็จแล้ว "รอบเก้า"):** ราคา/เวลา/ราคาขายฟิกซ์ (ส้ม 3,000/3วัน/300 · แอปเปิล 3,500/4วัน/400 · มังคุด 4,000/8วัน/800 · ทุเรียน 5,000/8วัน/1,000) · ขายแล้วต้นไม่หาย ออกผลรอบใหม่ทันที · ปลูกไม่จำกัด · นาฬิกานับถอยหลังต่อต้น
- **✅ ข้อ 13 — ค่าจัดการขยะ (เสร็จแล้ว "รอบสิบ"):** บิล id `trash` 0.5%/เดือน ออกวันที่ 1 เดือนแรกฟรี (special case ใน `billTick` โครงคล้าย maint แต่ไม่ตัด/ไม่พัง); ค้างข้ามเดือน → ค่าปรับ +500 ทบสะสมใน `bills.trash.fine` (จ่ายครบ→รีเซ็ต 0); ไม่มี cutKey/fixKey เลยไม่เข้าเครื่อง UTILITIES — ใช้ `trashBillUI()`/`payTrash()` แยกใน ui.js

## แผนอัปเกรดรอบถัดไป (backlog ที่คุยกับผู้ใช้แล้ว — ยังไม่ได้ทำ)

> ~~ระบบโรงงานผลิตสินค้า~~ ✅ **เสร็จแล้ว "รอบสิบสี่" (5 ก.ค. 2026)** — สเปกเต็มดูหัวข้อกติกา "🏭 โรงงานผลิตสินค้า"

0. **⭐⭐⭐ ระบบสังคมออนไลน์ (ผู้ใช้สั่ง 5 ก.ค. 2026 "อัพเดท 572569641" — งานใหญ่ งานถัดไป)** — ทำทีละขั้นตามลำดับนี้ (แต่ละขั้นทดสอบก่อนไปขั้นถัดไป):
   - **✅ 0.1 Google Login — เสร็จ "รอบสิบห้า" (5 ก.ค. 2026):** ผู้ใช้เปิด provider+domain ใน console แล้ว · ผู้ใช้เลือก**แบบ ก. บังคับ login เท่านั้น** (ไม่มีโหมดทดลองเล่น) · สเปกที่ทำจริงดูหัวข้อกติกา "🔐 Google Login" · ค้าง: วาง rules `/users` + ผู้ใช้ทดสอบจริง — สเปกเดิม: เปิดใน Firebase console: Authentication → Sign-in method → เปิด Google (+เช็ก Authorized domains มี `iamsuperrich2025.github.io`) — Claude ทำเองไม่ได้ · ใช้ Firebase Auth (compat SDK เดียวกับ online.js) `signInWithPopup(GoogleAuthProvider)` · เซฟ sync ขึ้น `/users/$uid/save` (localStorage เป็น cache — โหลดจาก cloud ถ้าใหม่กว่า, push ขึ้นเป็นระยะ+ตอน beforeunload) · rules: `/users/$uid` อ่าน-เขียนได้เฉพาะ `auth.uid === $uid` · ผู้เล่นเดิม (เซฟ local) → login ครั้งแรกถามผูกเซฟปัจจุบันเข้าบัญชี · **ผู้ใช้ระบุ "login ด้วย Google เท่านั้น"** — เข้าเกมไม่ login ไม่ได้ (หรือโหมดทดลองเล่นไม่เซฟ? ถามผู้ใช้ตอนทำ)
   - **✅ 0.2 ชื่อที่แสดง (display name) — เสร็จ "รอบสิบแปด" (6 ก.ค. 2026 · สิ่งที่ทำจริงดูหัวข้อกติกา "📛 ชื่อในเกม" + รอบสิบแปด — ⚠️ รอผู้ใช้วาง rules ใหม่):** สเปกเดิม: ตั้งตอน login ครั้งแรก ไทย/อังกฤษได้ · **ระบบตรวจ:** อนุญาตเฉพาะ ก-ฮ สระ วรรณยุกต์ a-z A-Z 0-9 เว้นวรรค (regex `/^[฀-๿a-zA-Z0-9 ]{2,20}$/`) + blacklist คำหยาบไทย-อังกฤษ (ทำ list ใน js/data/badwords.js ตรวจแบบ normalize ตัดวรรค/ตัวซ้ำ) · เก็บใน `/users/$uid/profile/name` + validate ซ้ำใน rules (length ≤20) · ใช้แทน onlineDisplayName เดิมใน presence/leaderboard
   - **✅ 0.3 ค้นหา id + add friend — เสร็จ "รอบยี่สิบเอ็ด" (6 ก.ค. 2026 · ทำครบตามสเปก — รหัส 6 ตัว/ค้นหา/ส่ง-รับคำขอ/รายชื่อเพื่อน+สถานะออนไลน์ · แผง 👥 ในเมนูซ้าย · รอผู้ใช้วาง rules ใหม่):** สเปกเดิม: ผู้เล่นมี **รหัสเพื่อน** สั้นๆ (เช่น 6 ตัว จาก uid hash — โชว์ใน dashboard ให้บอกเพื่อน) · ค้นหาผ่าน `/friendCodes/$code → uid` · ส่งคำขอ → `/friendReq/$toUid/$fromUid` → อีกฝ่ายกดรับ → เขียน `/friends/$uid/$friendUid` ทั้งสองฝั่ง · การ์ดเพื่อน: รายชื่อเพื่อน+สถานะออนไลน์ (join กับ presence)
   - **✅ 0.4 แชทกับเพื่อน (มี emoji) — เสร็จ "รอบยี่สิบสอง" (6 ก.ค. 2026 · สิ่งที่ทำจริงดูงานเรียงลำดับข้อ 1 บนสุด + รอบยี่สิบสองท้ายไฟล์ — ⚠️ รอผู้ใช้วาง rules ใหม่):** สเปกเดิม: `/chats/$pairId` ($pairId = uid สองตัวเรียง alphabet ต่อกัน) เก็บ {from, text, ts} · rules อ่าน-เขียนได้เฉพาะสมาชิกคู่นั้น + text ≤200 ตัว · UI กล่องแชท + **แผง emoji picker** (ชุด emoji ที่คัดแล้ว ~40 ตัว แทรกเข้าข้อความ) · **ข้อความผ่านตัวกรองคำหยาบเดียวกับ 0.2 ก่อนส่ง** (ปลอดภัยสำหรับเด็ก) · จำกัดเก็บ 100 ข้อความล่าสุดต่อคู่
   - **0.5 ส่งของขวัญ:** ใช้แคตตาล็อกของขวัญจาก **PROMPTS_GIFTS.md 50 แบบ** (สร้าง js/data/gifts.js: id/emoji/name/price/หมวด เค้ก-กุหลาบ-การ์ด) · ซื้อด้วยเหรียญแล้วส่งให้เพื่อน → `/gifts/$toUid` push {id, from, ts} · ผู้รับเห็นกล่อง "ได้รับของขวัญ" + ฉากเปิด (reuse showCollectReveal สไตล์) → เข้า **`state.giftBox` (ห้องของขวัญแยกต่างหาก)** — **⚠️ กติกาผู้ใช้: ของขวัญที่ได้รับขายต่อไม่ได้** ไม่รวมกับ collection/ตลาด/assetValue · UI ห้องของขวัญโชว์ของ+ใครส่ง+วันที่ — **🆕 แก้สเปกตามอัพเดท 5725691326 (5 ก.ค. 2026): (1) ส่งเป็นของขวัญได้ทุกอย่าง — ทั้งของขวัญจากร้าน (gifts.js 50 แบบ) และ*สินค้า collectibles ทุกชิ้นจากคลังผู้ส่ง* · ของที่รับมาขายต่อ/ส่งต่อให้คนอื่นไม่ได้ (2) ผู้รับเลือก "รับ"/"ไม่รับ" ได้ — ผู้ส่งเห็นสถานะ: ยังไม่กดอะไร = "สินค้ายังไม่มีผู้รับ" · กดรับ = "สินค้าส่งถึงผู้รับแล้ว" · กดไม่รับ = "ผู้รับยังไม่สะดวกรับ" · ❓ถามผู้ใช้ตอนทำ: collectible ที่ถูกปฏิเสธคืนเข้าคลังผู้ส่งไหม + ค้าง "ยังไม่มีผู้รับ" นานๆ หมดอายุคืนของไหม**
   - **rules ทุกโซนใหม่ต้องส่งให้ผู้ใช้วางใน console + ทดสอบ allow/deny ผ่าน REST เหมือนรอบสิบสาม** (โซนที่มี auth ทดสอบผ่านหน้าเกมจริง/Emulator แทน เพราะ REST ธรรมดาไม่มี token)
1. **✅ ยกเครื่อง UI ทั้งเกมเป็นแนวนอน + ธีมเมืองโทนฟ้า — เสร็จแล้ว "รอบสิบเจ็ด" (5 ก.ค. 2026 ดึก ผลทดสอบ+รายละเอียดที่ทำจริงดูหัวข้อรอบสิบเจ็ด)** ("อัพเดท 5725691826" — กลืนงานธีมเดิม "อัพเดท 572569808 ข้อ 3" เข้ามาแล้ว · สเปกเดิมที่อนุมัติ:)
   - **บังคับแนวนอน:** จอตั้ง → overlay "กรุณาหมุนจอ 📱↻" · ออกแบบอิง 16:9 (มือถือ/แท็บเล็ต/จอคอม)
   - **โครง Lobby (สไตล์เกมมือถือสมัยใหม่ แต่ตัวหนังสือใหญ่อ่านง่ายสำหรับเด็ก ป.1):** แถบบน = โปรไฟล์+แรงค์ (ซ้าย) · pill เหรียญรวม+เหรียญวันนี้+นาฬิกา+ไอคอน จดหมาย/ตั้งค่า/logout (ขวา) — เมนูไอคอนแนวตั้งฝั่งซ้าย (บ้าน/ร้านค้า/ฟาร์ม/โรงงาน&ตลาด/สถิติ) — **สัตว์ active ตัวใหญ่กึ่งกลางจอบนแท่นเรืองแสง** (ตำแหน่งพระเอกแบบตัวละครหน้า lobby เกม shooter) — การ์ดสด เพื่อนออนไลน์/Top50/ออเดอร์พิเศษ ฝั่งขวา — ปุ่มเหลืองใหญ่ "เล่นเกมคำศัพท์" มุมขวาล่าง + ปุ่มรองแบบทดสอบ
   - **ระบบแผง (panel):** คลิกเมนู → แผงรายละเอียดกึ่งกลางจอทับ Lobby (ฉากหลังหรี่) · เนื้อหายาวมี scrollbar ในแผงเท่านั้น ไม่ scroll ทั้งหน้า · ปุ่ม ✕ ปิดกลับ Lobby · การ์ด/ฟังก์ชัน render เดิม (renderHomeCard/renderFarmCard/renderCollectCard ฯลฯ) ย้ายเข้าแผง reuse โค้ดให้มากที่สุด
   - **หน้าร้าน/ตลาด/คลัง สไตล์ Global Trade HQ ฉบับ original:** rail หมวดแนวตั้งซ้ายในแผง · grid การ์ดสินค้า — แถบชื่อหัวการ์ดน้ำเงิน + ภาพสินค้าใหญ่กลางการ์ด + badge จำนวน xN + แถบราคาพื้นครีม+ไอคอนเหรียญ · แบ่งหน้า/ปัดซ้ายขวา reuse เครื่อง pagination รอบสิบหก (`factoryPage`/`.mkt-pager`) · สินค้า/สัตว์กดแล้วเปิดภาพใหญ่กลางจอ (มี showCollectReveal อยู่แล้ว)
   - **โทนสี:** น้ำเงินเข้ม (พื้น/กรอบนอก) · ฟ้า (หัวการ์ด/ปุ่มทั่วไป/เมนู active) · ขาว (พื้นการ์ดเนื้อหา) · เทา-ดำ (ตัวหนังสือ) · **เหลืองทองสงวนไว้ = เงิน/ราคา/ปุ่มหลัก/ไฮไลต์ตัวเอง เท่านั้น** · พื้นหลังใช้ภาพ img/theme/ ✅ครบ 6 ภาพแล้ว (fixed/cover + gradient fallback) — **ห้ามให้ตัวหนังสืออ่านยากขึ้น** (กติกาเดิมของผู้ใช้)
   - **แผนทำเป็นเฟส:** (1) shell แนวนอน+overlay หมุนจอ+ระบบแผง (2) ย้ายการ์ดเดิมเข้าแผงทีละหมวด (3) ร้าน/ตลาด grid ใหม่ (4) พื้นหลังธีม+เก็บรายละเอียด+ทดสอบทุกจอ — **ห้ามแตะ state/เซฟ/logic** ผู้เล่นเดิมต้องเล่นต่อได้เนียน
   - mockup (Lobby + หน้าร้าน) เสนอในแชท 5 ก.ค. 2026 แล้ว — **รอผู้ใช้อนุมัติดีไซน์ก่อนเริ่มลงมือ**
2. **⭐ ตลาดสินค้าออนไลน์จริง (เฟส 2):** Firebase RTDB พร้อมแล้ว · สลับแหล่งประกาศขาย → `/market` + push `state.listings` ขึ้น DB แทน `marketTick` จำลอง — UI ใช้ต่อได้เลย (เพิ่ม rules `/market` ด้วย) · เข้ากับระบบโรงงาน: ของในตลาดจริงคือของที่เพื่อน "ผลิตเอง" · ควรทำ**หลัง 0.1** จะได้ผูกกับ uid จริง
3. **ภารกิจรายวัน (Daily Quest)** — เช่น "จับคู่ถูก 20 คำวันนี้ +100🪙" ผูกกับ `state.daily` ที่มีอยู่แล้ว
4. **การ์ดสรุปส่งครู** — ปุ่มในหน้าสถิติ สร้างการ์ดใบเดียว (ชื่อ วัน-เวลา เหรียญวันนี้ แรงค์ คะแนนสอบล่าสุด) ให้เด็กแคปส่งไลน์ครู
5. **เสียง/แอนิเมชันเพิ่ม** — เสียงตอนซื้อบ้าน, แอนิเมชันหยดฝนตอนฝนตก
6. **ภาพที่ยังไม่เจน:** dragon เกือบทั้งชุด + dog/cat_newborn (PROMPTS.md) · บ้านเสีย 12 ภาพ + มือถือ 2 + คอม 2 (PROMPTS_RANK_HOME.md) · สวนผลไม้ (ยังไม่มี prompt) · **ของขวัญ 50 ภาพ (PROMPTS_GIFTS.md)** · **ธีม 6 ภาพ (PROMPTS_THEME.md)**
7. **✅ ตั้งชื่อสัตว์เลี้ยง — เสร็จ "รอบสิบเก้า" (6 ก.ค. 2026 ตามสเปกครบ: บังคับตั้งตอนซื้อ ยกเลิก=ไม่ซื้อ · ปุ่ม ✏️ เปลี่ยนชื่อในการ์ดสัตว์ · `p.name`+migration ตัวเดิม=ชื่อชนิด · checkName(raw,1,15) จาก badwords.js · escape ทุกจุดแสดงผล)** — สเปกเดิม: (อัพเดท 5725691326 ข้อ 3 — 5 ก.ค. 2026): ซื้อสัตว์ → กล่อง**บังคับ**ตั้งชื่อก่อน (ยกเลิกไม่ตั้ง = ไม่ซื้อ) · แก้ชื่อภายหลังได้ (ปุ่ม ✏️ ในการ์ดสัตว์) · เก็บใน `p.name` (migration: ตัวเดิมไม่มีชื่อ → default ชื่อชนิด เช่น "น้องหมา") · **ระบบตรวจชื่อ:** อนุญาตเฉพาะไทย/อังกฤษ/ตัวเลข/เว้นวรรค 1–15 ตัว (regex โครงเดียวกับข้อ 0.2) + blacklist คำหยาบ/คำต้องห้ามไทย-อังกฤษ (แชร์ `js/data/badwords.js` กับข้อ 0.2 — ตรวจแบบ normalize ตัดวรรค/ตัวซ้ำ) + **กันอักขระพิเศษ/ข้อความที่ทำระบบพังเด็ดขาด** (⚠️ UI วาดด้วย innerHTML — ชื่อที่ผ่านตัวกรอง regex ขาวล้วนจะไม่มี `<>"'&` อยู่แล้ว แต่ให้ escape ตอนแสดงซ้ำอีกชั้น)
8. **⭐ รายได้ออนไลน์ +0.01 เหรียญ/วิ (อัพเดท 5725691326 ข้อ 4):** ผู้เล่นออนไลน์อยู่ในเกม (เล่นอยู่หรือเปิดค้างไว้ก็ได้) → เหรียญยอดรวม +0.01/วิ · **แสดงผลโดดเด่นชัดเจนน่าดึงดูด** (ตัวเลขวิ่งสด — โครง `compTick`/`#comp-live` ของคอมใช้ซ้ำได้ แต่อันนี้ฟรีทุกคนไม่ต้องซื้ออะไร) จุดประสงค์: ดึงดูดให้เข้ามาเล่นบ่อยๆ · ตอนทำต้องเคาะ: นิยาม "ออนไลน์" (แท็บเปิดอยู่? ต้อง focus ไหม?) + นับเฉพาะตอน login แล้ว · เหรียญส่วนนี้เข้า daily/leaderboard ตามปกติ
9. **⭐ คำศัพท์ (อัพเดท 5725691326 ข้อ 5/5.1/5.2 — มอบ Sonnet 5):** แยก vocab.js → `js/data/vocab/band1-5.js + index.js` · เพิ่ม part of speech ตัวที่ 3 `['research','การวิจัย','n']` โชว์ท้ายคำแบบ `research (n.)` ทั้งเกมจับคู่+ข้อสอบ (แก้ game.js 4 จุด) · ขยาย ≥40 คำ/หมวด + หมวดใหม่ 2-4/band รวม ≥2,000 คำ ตรงช่วงชั้น สพฐ./CEFR — **คำสั่งเต็มเขียนไว้แล้วใน `TASK_VOCAB_SONNET.md` ผู้ใช้เปิด session ใหม่เลือก Sonnet แล้วสั่งอ่านไฟล์นั้นได้เลย** (ห้ามเปลี่ยน id หมวด/en ห้ามซ้ำใน band/th ห้ามซ้ำในหมวด — เหตุผลอยู่ในไฟล์) · เสร็จแล้ว Fable 5/Opus 4.8 ตรวจใน session ถัดไป
10. **🆕 Project Dictionary A–Z (ผู้ใช้สั่ง note ไว้ 5 ก.ค. 2026 ดึก — "จะทำตอนไหนให้ Fable หรือ Opus พิจารณาตามความเหมาะสม"):** พจนานุกรมอังกฤษ→ไทยเรียง a–z ครอบคลุมถึง idioms + phrasal verbs · รูปแบบต่อคำ: `คำ (pos) /IPA/ เสียงอ่านไทย` + ประโยคอังกฤษง่ายๆ อธิบายความหมาย + ความหมายไทย (ตัวอย่าง: `cat (n.) /kæt/ แคท · A cat is a small pet. It says "meow." · แมว`) · **มอบ Sonnet ทำหลาย session** — กฎเหล็กผู้ใช้: เขียนใส่ไฟล์ในเครื่อง/GitHub · **ทุกๆ ~5k tokens ขึ้นไฟล์ใหม่ ห้ามอ่านไฟล์เก่าย้อนหลัง (ประหยัด tokens)** · ก่อนขึ้น session ใหม่ต้องบันทึกเสมอว่าคำล่าสุดคือคำไหน+คำถัดไปเริ่มคำไหน กันหลุด/ตกหล่น · **สเปกเต็ม+โครงไฟล์+ตารางความคืบหน้าเขียนไว้แล้วใน `TASK_DICTIONARY_SONNET.md`** · ฝั่งเกม (ทำทีหลัง): ปุ่ม Dictionary ที่ Lobby → เปิด layer กลางจอ **พื้นฟ้าไล่เฉดสไตล์หนัง sci-fi ล้ำสมัย** (reuse ระบบแผง lobby.js ได้) + แนะนำช่องค้นหา/แถบ A–Z/lazy-load · ข้อมูลเริ่มรันขนานได้ทุกเมื่อ (ไม่ชนไฟล์เกม) ปุ่ม+layer แนะนำหลัง 0.2–0.5

## ธรรมเนียมการทำงานกับผู้ใช้รายนี้

- ตอบภาษาไทย สุภาพ ลงท้าย "ครับ" ใช้ตาราง/อีโมจิให้อ่านง่าย
- ทำเสร็จต้องทดสอบจริงใน preview (eval จำลองเวลา/คลิก — mock `Date.now` ได้) แล้วรายงานผลทดสอบเป็นตาราง ก่อนจบให้รีเซ็ต localStorage ให้ preview กลับหน้าแรก
- **⚠️ เกมบังคับ Google login แล้ว (ตั้งแต่รอบสิบห้า) — ทดสอบใน preview ต้อง mock login ก่อนเสมอ** (login จริงใน preview ไม่ได้ · ปุ่มรีเซ็ตในเกมถูกถอดแล้วด้วย):
  ```js
  window.authFetchCloud  = ()=>Promise.resolve(null);   // จำลอง cloud ว่าง (หรือคืน {data,at} จำลองเซฟ)
  window.authWriteCloud  = ()=>Promise.resolve();       // กันเขียนขึ้น DB จริง
  window.authDeleteCloud = ()=>Promise.resolve();
  window.authWriteProfileName = ()=>Promise.resolve();  // กันเขียน /users/<uid>/profile/name จริง (ข้อ 0.2)
  window.onlineStart     = ()=>{};                      // กัน presence/leaderboard เขียน DB จริง
  authOnLogin({uid:'test1', email:'t@test.com'});       // → เข้าหน้าลงทะเบียน เล่นต่อได้ปกติ
  ```
  จบงานล้างด้วย `localStorage.removeItem('petVocabAdventure_v1')` + reload → กลับหน้า login
- ระวังไฟล์ผู้ใช้: Windows ซ่อนนามสกุลไฟล์ เคยเกิด `dog_egg.png.png` มาแล้ว
- ห้ามทำระบบใส่เครื่องแต่งตัวหลายชิ้นพร้อมกัน (เคยทำแล้วผู้ใช้สั่งยกเลิก) — ใส่ทีละ 1 ชิ้นเท่านั้น
- เรื่องลิขสิทธิ์: ไม่เจน/ไม่แนะนำให้เจนภาพเลียนแบบงานเกมจริง ให้ใช้ "สไตล์เดียวกัน ดีไซน์ออริจินอล" (ผู้ใช้ยอมรับแนวทางนี้แล้วกับชุดเหรียญตราแรงค์)
- ผู้ใช้ชอบให้เสนอไอเดียต่อยอดท้ายคำตอบ
