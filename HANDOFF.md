# HANDOFF.md — BOOT ไฟล์เดียวจบ (Pet Vocab Adventure)

> 📂 ราก `C:\Users\rober\english-pet-game\` · ⚠️ working dir = `C:\Users\rober` → **เปิดไฟล์ใช้ path เต็มเสมอ**
> 🚀 **session ใหม่: อ่านไฟล์นี้ไฟล์เดียวพอเริ่มงาน** (เดิมต้องอ่าน 3 ไฟล์ — ยุบมาไว้นี่หมดแล้ว) · ไฟล์อื่นเปิดเฉพาะตอนต้องใช้ (ดูตารางล่างสุด)

## 🏆 กฎทอง (ยึด 4 ข้อนี้ก่อนเสมอ)
1. **ภาพก่อนโค้ด** — บั๊กที่มองเห็นได้ (UI/layout) → **ขอ/ดู screenshot ก่อน ห้ามเปิดโค้ดเดา** (รอบที่ผ่านมาเดา z-index จากโค้ดเสียเวลาเปล่า พอเห็นภาพเจอต้นตอทันที)
2. **Grep ก่อน Read** — ไฟล์ใหญ่ (`ui.js` ~2,000 บรรทัด, `vocab.js`, `collectibles.js`) หาชื่อฟังก์ชันด้วย Grep แล้ว Read เฉพาะช่วง (offset+limit) · **ห้ามอ่านทั้งไฟล์**
3. **preview: resize landscape ก่อน · เชื่อ `getBoundingClientRect` ไม่เชื่อ screenshot/elementFromPoint** (ดู 🖥️ ล่าง)
4. **จบงาน: บัมพ์ `version.json` → commit เฉพาะไฟล์ที่แก้ (ห้าม `git add -A`) → อัปเดต `handoff/TASKS.md`**
5. **เลือก session ตามการประหยัด token:** งาน/ขั้นตอนไหน **ทำใน session เดิมถูกกว่า** (context โหลดแล้ว งานเล็ก) → **ทำเลย ไม่ต้องเสนอแยก session** · ไหน **New session ถูกกว่า** (session เดิมยาว/context บวม, งานใหม่ที่ไม่พึ่งของเดิม) → **เสนอให้เริ่ม New session ทุกครั้ง**
6. **อนุญาตล่วงหน้าทุกงานเกม (ผู้ใช้สั่ง 8 ก.ค. 2026):** งานเกมนี้+ไอเดียต่อยอดทุกกรณี → **ทำเลย จบงาน commit+push แล้วรายงาน ไม่ต้องถาม "สนใจไหม/ทำเลยไหม"** · **ยกเว้นต้องถามก่อน:** ทำให้ผู้ใช้เสียเงินเพิ่ม / เสี่ยงการเงิน / เสี่ยงความปลอดภัย (เช่น หย่อน rules) / งานใหญ่จนขัดกฎ token (ข้อ 5) · กฎนี้ไม่ยกเลิกการถามเมื่อ**งานกำกวม** (ไม่แน่ใจว่าหมายถึงงานไหน → ถามก่อน อย่าเดา)

## 📸 สถานะปัจจุบัน (12 ก.ค. 2026)
- ✅ **รอบ 140: เสียงติ๊ดถอยหลัง + ไฟถอยขาวรถเพื่อน + เสียงรีเลย์ไฟเลี้ยว 🔊⬜ (version .131)** — `CarSound.revBeep` ติ๊ด 1kHz ทุก 600ms (เกียร์ R หรือวิ่งถอยจริง · จอดสนิทเงียบ) · `CarSound.tlClick` ติ๊ก-ต่อกสลับสูง/ต่ำเฟส 400ms ดังทันทีตอนโยกก้าน ปิดแล้วเงียบ · ไฟถอยขาว 2 ดวงท้ายรถบล็อกเพื่อน **คำนวณฝั่งรับเองจากทิศวิ่งสวนหัวรถ** (dot<-.5 ค้าง 0.25 วิ hysteresis กันวูบ) — **ไม่มี field ใหม่ ไม่ต้องแก้ rules** (field tl ล็อก 0-2 ใน rules ส่งค่าเพิ่มไม่ได้) · ยืนยัน preview ครบทุกเคส + deploy live .131 · **⚠️ ค้าง: ผู้ใช้ฟังเสียงจริงบนมือถือ + ไฟถอยต้องดู 2 เครื่อง**
- ✅ **รอบ 139: car_10 ครบ 10 คัน + ไอคอนแอพธีม Vocab World 🌍 + ปุ่มเบรค/เกียร์ถอย R + โมเดล pet_cat/pet_dragon เข้าเกม (version .130)** — ไอคอนใหม่ (โลก+บล็อก ABC วาดด้วย Pillow — สคริปต์ทำซ้ำได้) แทนทั้ง 4 ไฟล์ `img/icons/` · โหมดขับรถ: ปุ่มเบรคแดงข้างคันเร่ง (กดค้าง=หน่วงถึง 0 ไม่ไหลไปถอย) + ปุ่มเกียร์ D/R เหนือคันเร่ง (R=คันเร่งกลายเป็นถอยหลัง เพดาน 6.5 · ป้ายสลับ ▼ถอย ส้ม · รีเซ็ต D ทุกครั้งที่เข้าโลก) · ก้านไฟเลี้ยวขยับ right 132→224 หลบเบรค · โมเดลแมว (10k tris)+มังกร (20.9k tris) โหลดผ่าน lobby3d generic — ยืนยัน preview: curKey cat/dragon โหลดจริง + เบรค/ถอย/ป้าย/reset ผ่านทุกเคส + deploy live .130 · **⚠️ ค้าง: ผู้ใช้ลองฟีลปุ่มจริงบนมือถือ + ดูโมเดลแมว/มังกร + ไอคอนหลังติดตั้ง PWA ใหม่**
- ✅ **รอบ 136–138: login โดเมนเดียว vocabworld.web.app + OAuth branding "Vocab World" + fix cache headers (version .129)** — บทเรียน: เปลี่ยน authDomain ต้องเพิ่มโดเมนใน **OAuth client (Clients → JS origins + redirect URI /__/auth/handler)** ก่อน ไม่งั้น redirect_uri_mismatch · ค่าใหม่กระจาย 5 นาที–ชม. · โค้ดเกม no-cache แล้ว (อัปเดตถึงมือถือทันที) · ⏳ รอผู้ใช้ยืนยัน login มือถือ
- ✅ **รอบ 135: ลดปุ่มขับรถลง (20vh) + ก้านไฟเลี้ยวแนวตั้งแบบรถจริง + ไอคอนแชทย้ายขึ้นบน 🎛️ (version .126)** — ก้านโยกฝั่งขวา: ดันขึ้น=ไฟซ้าย ดันลง=ไฟขวา knob ค้าง แล้วเด้งกลับเอง ~0.9 วิหลังเลี้ยวเสร็จ · ไอคอนแชท/เสียงเรียงแถวบนขวา (เฉพาะโหมด drive) · ยืนยัน preview จอ 812×375 ครบ + deploy live แล้ว · **⚠️ ค้าง: ผู้ใช้ลองฟีลจริงบนมือถือ (ความสูงปุ่ม/แรงลากก้าน จูนต่อได้)**
- ✅ **รอบ 133: เจตนาชนรถเพื่อนครบ 3 ครั้ง = ค่าซ่อม 🪙10,000 + ไฟจราจรจริง/ฝ่าไฟแดง ม.22 🚦 (version .125)** — นับเฉพาะฝ่ายพุ่งเข้าใส่ (ถูกชน/วิ่งหนีไม่เสีย) เตือนครั้งที่ 2 ครั้งที่ 3 โดน 10,000 ประกันไม่คุ้มครองเจตนา · ไฟจราจร 30 แยกใหญ่ เฟสจาก Date.now ทุกเครื่องตรงกัน · ฝ่าไฟแดง (วิ่ง>10 กม./ชม. ในแยกไฟแดง) ใบละ 🪙300 (ตัวเลขผมเคาะ — ปรับได้ที่ CAR_FINE_RED) · ยืนยัน preview ครบ + deploy live แล้ว · ✅ ผู้ใช้เพิ่ม vocabworld.web.app เข้า Auth Authorized domains แล้ว — **เกมออนไลน์เต็มระบบที่บ้านใหม่**
- ✅ **รอบ 134: เปลี่ยนชื่อเกมเป็น "Vocab World" 🌍 + ย้ายบ้านขึ้น Firebase Hosting (version .124)** — บัญชี GitHub โดน flag (โปรไฟล์/Pages 404 ทั้งหมด · repo ยังใช้ได้ด้วย credential) → deploy ใหม่ที่ **https://vocabworld.web.app** (site `vocabworld` ในโปรเจกต์ Firebase เดิม · สคริปต์ `tools/deploy_firebase.sh` เอาไฟล์จาก git HEAD เท่านั้น กัน WIP หลุด) · เปลี่ยนชื่อครบ title/manifest/login/การ์ดครู/วิธีเล่น (STORAGE_KEY เดิม เซฟไม่กระทบ) · ภาพรถผู้ใช้ครบ 9/10 เข้า git · ✅ rules field tl ผู้ใช้ publish แล้ว (ตรวจ REST) · **⚠️ ค้าง: (1) ผู้ใช้เพิ่ม `vocabworld.web.app` ใน Auth Authorized domains (ไม่งั้น login ไม่ได้) (2) ผู้ใช้ยื่นอุทธรณ์ GitHub (3) งานรอบ 133 ที่ผู้ใช้สั่ง: เจตนาชนรถครบ 3 ครั้ง=ค่าซ่อม 10,000 + ไฟจราจรจริง/ฝ่าไฟแดงโดนปรับ — ยังไม่ได้เริ่ม** · โควตา Hosting ฟรี: ดาวน์โหลด 360MB/วัน (พอช่วงทดสอบ · ใช้ทั้งห้องเรียนค่อยอัปเกรด/ย้าย)
- ✅ **รอบ 132: ชนรถเพื่อน+ไฟเลี้ยว+ตรวจแยก ม.36+เพื่อนเห็นไฟเลี้ยว 🚦 (version .123 — ปิดชุดระบบรถ 9 ข้อครบ)** — ชนรถ peer: ประกันจ่าย/ไม่มี=🪙5,000 หักตอนออก · ปุ่ม ⬅️➡️ เหนือพวงมาลัย ดับเองหลังเลี้ยว · เลี้ยวที่แยก (driveArms>=3) ไม่เปิดไฟ = ใบ ม.36 🪙100 · field `tl` ใน /world + fallback deny ไม่พังเกม + ไฟกะพริบบนรถบล็อกเพื่อน · ยืนยัน preview ครบ · **⚠️ ค้างผู้ใช้: (1) publish rules tl (Artifact ส่งแล้ว) (2) ภาพรถอีก 8 ไฟล์ (3) ทดสอบจริงมือถือ/2 เครื่อง**
- ✅ **รอบ 131: ระบบซื้อรถ+พ.ร.บ.+ประกัน+ผ่อน+ล็อกกุญแจขับรถ 🚗🔐 (version .122 — ข้อ 1–5 จากชุด 9 ข้อ)** — หมวด "🚗 ยานพาหนะ" ในตลาด 10 คัน 30,000–120,000 (probe img/cars/ · มีจริงแล้ว 2 ภาพ) · ซื้อแจ้ง 3 รายการ รถ/พ.ร.บ.600/ประกัน 5,000 · สด/ผ่อน (ดาวน์ 20% 10 เดือน ค้างงวด=ล็อกขับ ไม่ยึดรถ) โปะได้ · state.car+carLoan* (state.js) · มีตั๋วไม่มีรถ = 🔐 ราง+การ์ด+กล่องพาไปตลาด (**คนมีตั๋วเดิมต้องซื้อรถ — ผู้ใช้เคาะแล้ว**) · ยืนยัน preview ครบ · **ข้อ 6–9 (ชนรถ peer/ไฟเลี้ยว/ม.36/sync) ทำต่อรอบ 132 ใน session เดียวกัน**
- ✅ **รอบ 130: ค่าซ่อมชนสิ่งของ 🪙1,000 + prompt รถ 10 คัน 🔧🚗 (version .121)** — ชนแรง = ค่าซ่อม 1,000 (เพดาน 3/รอบ) รวมในกล่องสรุปตอนออก · `PROMPTS_CARS.md` รอผู้ใช้เจนวาง `img/cars/` · **งานต่อไปจองไว้: ระบบซื้อรถ/ประกัน/ไฟเลี้ยว 9 ข้อ ทำใน session ใหม่ (guideline ใน TASKS.md + Artifact)** · ⚠️ ถามราคารถ/นโยบายผ่อน/คนมีตั๋วเดิม ก่อนเริ่ม
- ✅ **รอบ 128: ท็อปสปีด 200 + ใบสั่งจราจร + สวิตช์สตาร์ท/เข็มขัด 🚔 (version .119)** — vmax 200 กม./ชม. เกจ 0-240 โซนแดงจาก 90 · เกิน 90 = ป้ายเตือน ม.67 + ใบสั่ง 🪙200/ครั้ง หักตอนออก (สรุปเป็นกล่อง) · แผงเตรียมออกรถ: สวิตช์สตาร์ท (เสียงไดสตาร์ทสังเคราะห์) + เข็มขัด (เสียงคลิก) + แผงกฎหมายฟ้า sci-fi ม.123/ม.67/จำคุก"รอลงอาญา" · ไม่คาดเข็มขัดแล้วขับ = หัก 🪙300 ทันที+กล่องแจ้ง · testkit ใหม่ `_t.setDriveSpeed` · ยืนยัน preview ครบ · **⚠️ ค้าง: ลองจริงมือถือ (เสียง)**
- ✅ **รอบ 127: ปุ่มคอนโซลโหมดขับรถ 🎛️ (version .118)** — ปุ่มจางๆ 2 ปุ่มบน console (มือถือ): แถบพวงมาลัยล่างซ้าย (แตะ/ลาก=องศาเลี้ยว dot วิ่งตามนิ้ว) + คันเร่งเขียวล่างขวา (กดค้าง=เร่ง ปล่อย=ชลอจนหยุด) · 2 นิ้วพร้อมกันได้ · จอยเดิมยังใช้นอกปุ่ม (ถอยหลัง) · ยืนยัน preview ครบ · **⚠️ ค้าง: ผู้ใช้ลองฟีลจริงบนมือถือ**
- ✅ **รอบ 126: แจ้งเตือนของที่เล็งไว้มีคนลงขาย 💖 (version .117)** — `state.wishlist` + dialog เล็งของ 50 ชิ้นในแผงตลาด · marketWatch เด้ง toast เมื่อประกาศใหม่ตรงของที่เล็ง (snapshot แรกเงียบ · ของตัวเองไม่เด้ง) · badge ปุ่มราง 🏪 + การ์ดเรืองชมพูในชั้นตลาดเพื่อน · ซื้อแล้วถอนจากลิสต์เอง · ยืนยัน fake DB ครบ · **⚠️ ค้าง: ทดสอบจริง 2 เครื่อง**
- ✅ **รอบ 125: ภาพตึกกำแพงเพชร 4 ไฟล์ขึ้น live (version .116)** — ผู้ใช้เจนภาพวาง `img/city/` ครบ (1024² ทั้ง 4) → commit เจาะจง + ยืนยัน live 200 ครบ + ยืนยันในเกม: InstancedMesh 4 กลุ่ม (1,940/1,908/695/117) ใช้ภาพจริงทุกกลุ่ม · **✅ ผู้ใช้ publish rules โซน market+msold แล้ว 11 ก.ค.** (ตรวจ REST: /market ไม่ login = 401 ถูกต้อง) → **ตลาดออนไลน์จริงเปิดใช้แล้ว** · ⚠️ ค้าง: ทดสอบจริงมือถือ + ซื้อ-ขายจริง 2 เครื่อง
- ✅ **รอบ 120–124: เคลียร์ backlog 5 ก้อนรวด (version .111–.115)** — 🌐 โบนัสออนไลน์ +0.01/วิ ฟรีทุกคน (item 8 · pill วิ่งสดใน header) · 🎯 Daily Quest 3 ภารกิจ/วัน seed ตามวันที่ ทุกคนได้ชุดเดียวกัน (item 3 · การ์ดใน aside + hook 6 จุด) · 📇 การ์ดสรุปส่งครูในหน้าสถิติ (item 4 · แคปส่งไลน์) · 🧱🚶 หุ่นบล็อกเดินได้ใน adv/haunt (picker เดียวกับโลกขับรถ · แขน-ขาแกว่งตามระยะจริง · ส่งผ่าน av เดิมไม่ต้อง publish rules) · 🏪🌏 ตลาดออนไลน์จริง /market+/msold (item 2 · transaction คนแรกได้ · กันใบเสร็จปลอม 2 ชั้น · fallback ตลาดจำลองอัตโนมัติ) · ยืนยัน preview ครบทุกก้อน · **⚠️ ค้าง: (1) ผู้ใช้ publish rules โซน market+msold — Artifact ปุ่มคัดลอกส่งแล้ว (ยังไม่วาง = ตลาดจริงปิดตัวเอง เกมปกติ) (2) ทดสอบจริงมือถือ/2 เครื่อง** · retopo โมเดล Lobby ข้ามไว้ (ต้องใช้เหรียญ Tripo = เงินผู้ใช้ ถามก่อน)
- ✅ **รอบ 119: ตัวละครบล็อกเลือกได้ในโลกขับรถ 🧱🚗 (version .110)** — ก่อนออกรถมี picker เลือกหุ่นบล็อก 8 ตัว (thumbnail เรนเดอร์จากโมเดลจริง · จำตัวล่าสุดใน `state.blockAv`) · เพื่อนใน map = รถบล็อก+หุ่นนั่งขับ 3D หมุนตาม yaw ล้อหมุนจริง · ส่งรหัสผ่าน field `av` เดิม **ไม่ต้อง publish rules** · client เก่า fallback uid hash · ชื่อตัวละคร generic (QA แก้กันชนเครื่องหมายการค้า) · ยืนยัน preview ครบ · **⚠️ ค้าง: ทดสอบจริง 2 เครื่อง** · ต่อยอด: picker เดียวกันกับโลกเดิน adv/haunt
- ✅ **รอบ 118: บังคับรถ smooth ฟีล R4 🏁 (version .109)** — feedback ผู้ใช้ "ไม่ smooth เลย" → พวงมาลัย ramp เข้าช้า-คืนไว + จำกัดอัตราหมุนหัวรถตามความเร็ว + **ทิศวิ่งจริงไถลตามหัวรถ (drift grip blend)** + กล้องหันแบบหน่วง+ชายตามองเข้าโค้ง · ยืนยัน preview: ease-in นุ่ม/ปล่อยมือนิ่ง/ไม่มี NaN · **⚠️ ค้าง: ผู้ใช้ลองฟีลจริง (จูนต่อได้ที่ ramp 3.8/6.0 · grip 6.5 · maxYaw 1.9)**
- ✅ **รอบ 117: โค้ดรองรับภาพหน้าตึกเมืองกำแพงเพชร + prompt 4 ไฟล์ 🏢 (version .108)** — ตึกแถวโลกขับรถแยก InstancedMesh 4 กลุ่มตามชั้น (1,940/1,908/695/117) probe `img/city/{house_1fl,shop_2fl,shop_3fl,shop_4fl}.png` — วางไฟล์=ผนังเป็นภาพจริงทันที ไม่มี=สีพาสเทลเดิม · prompt ใน `PROMPTS_BUILDINGS_KPP.md` (seamless แนวนอน 1024² มองตรง โทนขาวครีม) + Artifact ปุ่มคัดลอก · **⚠️ ค้างผู้ใช้: เจนภาพ 4 ไฟล์วาง `img/city/` แล้วบอก Claude commit**
- ✅ **รอบ 116: ภาพห้องคนขับจริง + เข็มหน้าปัดวิ่งจริง 🎛️ (version .107)** — ผู้ใช้เจน `img/car/dash.png`+`wheel.png` (โปร่งใสถูกต้องอยู่แล้ว ตรวจแล้ว) commit เข้า git · เพิ่ม canvas `#adv-cargauges` วาด**เข็มสปีด 0-180 + วัดรอบ 0-8 พันรอบ (โซนแดง)** ทับวงเกจของภาพจริงทุกเฟรม (ตำแหน่งวัดจากภาพ: (1096,662) r80 / (1258.5,662) r78 บน 1536×1024 · สูตร crop `s=boxW/1536, offY=(1024s-boxH)*.66`) · จัดพวงมาลัย `left:76.5%;bottom:-15vh` ให้**มองเกจลอดช่องเปิดบนพวงมาลัย**แบบรถจริง+บังแกนคอเปลือยในภาพ · ยืนยัน: เข็มแม่น (19 กม./ชม. = 165° vs ทฤษฎี 163.5°) พวงมาลัยหมุน -162° · ✅ **ผู้ใช้ publish rules โซน drive+drone แล้ว** (multiplayer โลกรถ+โดรนพร้อม) · **⚠️ ค้าง: ทดสอบจริงมือถือ + 2 เครื่อง**
- ✅ **รอบ 115: นโยบายโมเดล 3D ตัวเปล่า + เอฟเฟกต์เหรียญแรงค์ตอนเลื่อนแรงค์ (version .106)** — **นโยบายใหม่ (NOTES.md):** โมเดล 3D สัตว์ตัวเปล่าเสมอ ไม่เจนตามชุดแต่งตัว (ฟังก์ชันแต่งตัว 2D คงไว้) → เอาแรงไปเพิ่มชนิดสัตว์ (เช่น ควาย) · **เอฟเฟกต์:** แรงค์เปลี่ยน → เหรียญฉากหลังเวทีแฟลช+หมุน coin-flip สลับเหรียญใหม่ (`rank-fx` · เคารพ noAnim · ยืนยัน preview ครบ) · **⚠️ ค้าง: ผู้ใช้ดูจริงบนมือถือ**
- ✅ **รอบ 114: Lobby 3D ตาม feedback ผู้ใช้ 3 ข้อ (version .104)** — (1) **เลิกหมุนอัตโนมัติ** โมเดลยืนหันหน้าตรง (ปัดหมุนเองยังได้เหมือนเดิม) (2) **ซ่อน PNG fallback ระหว่างโหลดโมเดล** (เดิมภาพหมา PNG+อีโมจิคนวูบขึ้นก่อน 3D มา) — `hidePng()` ตั้งแต่เฟรมแรกของ attach ไม่มีโมเดลจริงค่อยคืน PNG (เทสต์เคสแมวแล้ว) (3) **ย่อโมเดล ~25%** (fitH 1.16→1.55) + **เหรียญแรงค์ใหญ่เป็นฉากหลังเวที** (`heroRankBgHTML` ui.js + `.hero-rank-bg` lobby.css · ใช้ `rankInfo(netWorth())` + glow สีตามแรงค์ · z0 หลัง canvas z1) · **⚠️ ค้าง: ผู้ใช้ดูจริงบนมือถือ**
- ✅ **รอบ 113: โลกที่ 5 — ขับรถเมืองกำแพงเพชรจริง 🚗🕰️ (version .105)** — โหมด `drive` ใน engine เดิม · **เมืองจริงจาก OpenStreetMap** รัศมี 2.2 กม. รอบหอนาฬิกาวงเวียนต้นโพธิ์ (ถนนจริง 705 สาย ชื่อไทยโชว์บน OSD · ตึกจริง 79+ป้ายชื่อ 36 · แม่น้ำปิงข้ามได้เฉพาะสะพาน · ตึกแถว 4,660 เรียงริมถนนจริง seed คงที่ — `js/data/city_kpp.js` 232KB โหลด lazy ตอนเข้าโลก) · ตั๋ว 🪙25,000 (ต้องมีตั๋วโดรน) 40🪙/คำ ขับชนตัวอักษรบนถนน · ฟิสิกส์รถ bicycle model นอกถนนช้า · เรดาร์=แผนที่ถนนทั้งเมือง · **พวงมาลัยหมุนตามเลี้ยวจริง** (`img/car/dash.png`+`wheel.png` — prompt ใน `PROMPTS_CAR.md` · ยังไม่มีภาพ=CSS จำลอง) · ยืนยัน preview ครบ (ขับ/เลี้ยว/ชน/จบคำ +40/ออกโลก) · **⚠️ ค้างผู้ใช้: (1) publish rules โซน drive — Artifact รอบ 113 ปุ่มคัดลอก (รวมโซน drone รอบ 85 ที่ยังค้าง ก้อนเดียวจบ) (2) เจนภาพ dash/wheel วาง `img/car/` (3) ทดสอบจริงมือถือ** · ขยายเมือง: แก้ RAD ใน scratchpad `bake_city.py`
- ✅ **รอบ 112: push เสียง Suno 4 ไฟล์ขึ้นเว็บ (version .103)** — `sound/haunt_ambient/chase/scare.mp3 + spark.mp3` เข้า git + ยืนยัน live 200 ครบ · โค้ดรับอยู่แล้วไม่ต้องแก้ · เสียงที่ยังไม่เจน: drone_loop + เฮลิฯ 3 ไฟล์ · **⚠️ ค้าง: ผู้ใช้ฟังจริงบนมือถือ**
- ✅ **รอบ 111: Lobby 3D turntable + เล่น clip Tripo จริง (version .102)** — GLB ทั้ง 3 มี clip ชื่อ `NlaTrack`/`NlaTrack.001` (ไม่ตรง regex) → `setupClips` fallback ใช้ clip แรก = อนิเมชัน Tripo เล่นจริงทั้งคน+หมา · turntable หมุนโชว์ 0.12 rad/s ตอน idle (แตะ=หยุด · ปล่อย 3.5 วิ=หมุนต่อ) · ยืนยัน preview ครบ (clipTime เดิน/หยุด-หมุนต่อ/ลากเองยังได้) · **⚠️ ค้าง: ผู้ใช้ทดสอบจริงบนมือถือ** · ต่อยอดที่เหลือ: retopo ลดโพลี · pet_cat/pet_dragon (ฝั่งผู้ใช้)
- 🎯 **งานถัดไป: รอผู้ใช้เคาะ** จาก backlog (`handoff/TASKS.md`) หรือ feedback หลังทดสอบ Lobby 3D บนมือถือจริง
- ✅ **รอบ 105: แก้โมเดล Lobby 3D เล็ก+หันหลังเสร็จ (version .101)** — ต้นตอ: **SkinnedMesh clone ไม่ rebind skeleton** (`gltf.scene.clone(true)` ไม่ผูก bone → GPU skinning ยึด bone ต้นฉบับ → scale/ตำแหน่ง/หมุนบน node ไม่มีผลกับ vertex) = อาการ A(เล็ก 3×)+B(หันหลัง+หมุนไม่ติด) ต้นตอเดียว · **แก้:** `cloneSkinned()` (SkeletonUtils.clone inline) แทน `.clone(true)` ใน `applyLayout` · ยืนยัน gl.readPixels fillY 24%→88% + หมุนได้จริง + screenshot คน+หมาเต็มตัวหันหน้าตรง · สมมติฐานเดิม (matrix ค้าง/adventure3d pollution) **ผิดทั้งคู่** · layout 3 คอลัมน์ [ข้อมูลน้อง|เวที3D|การดูแล] เดิมสวยอยู่แล้ว · **⚠️ ค้าง: ผู้ใช้ทดสอบจริงบนมือถือ + ลากหมุนโมเดล**
- ✅ **รอบ 85: โลกที่ 4 — โดรน FPV Racing 🛸 (การ์ดตั๋วที่ 4)** — engine เดิม (`adventure3d.js`) โหมดใหม่ `drone` · ตั๋ว 🪙20,000 (ซื้อได้เมื่อมีตั๋วเฮลิฯ) · **เมืองตึกร้าง**: `buildAbandoned()` สร้างตึกกลวง (เสา 4 มุม + มุลเลียนแบ่งหน้าต่าง + พื้นแต่ละชั้นมีปล่องกลาง + ผนังกั้นห้อง) → บิน**ลอดหน้าต่างเข้าไปในห้องต่างๆ** เก็บตัวอักษร (บินเฉียด ไม่ต้องจอด) 35🪙/คำ · ฟิสิกส์ FPV เร็ว/คล่องกว่าเฮลิฯ (accel 30, vmax 30 m/s, banking) + ชนกำแพงเด้ง (AABB `collideDrone` กับ `solids` ต่อตึก ~880 กล่อง broad-phase ตาม footprint) · เสียงมอเตอร์บัซซ์สังเคราะห์ `DroneSound` (4 มอเตอร์ detune, ตอบสนองไว · รับไฟล์ `sound/drone_loop.mp3` จาก Suno — `PROMPTS_DRONE.md`) · เกตแข่งเรืองแสง (torus) ตกแต่ง · OSD สีเขียว FPV + เรติเคิลกรอบ + ขอบจอมืด · ยืนยัน preview: 21 ตึก/880 solids/บินหน้า 22.8 m/s/ชนดันออก/เก็บตัวอักษรบินเฉียด/การ์ด 3 สถานะ — version .61 · **⚠️ ค้าง: (1) ผู้ใช้ publish rules โซน drone (Artifact ส่งแล้ว — ยังไม่ publish=เล่นคนเดียวได้ แต่ multiplayer โดรนยังไม่ทำงาน) (2) ผู้ใช้เจนเสียง Suno + ทดสอบจริงมือถือ**
- ✅ **รอบ 84: ลูกศรขอบเรดาร์ชี้ตัวอักษรที่ต้องเก็บนอกระยะ ➡️** — ต่อจากรอบ 83 · ในส่วน rotated ของ `drawMinimap` ตัวอักษรที่ยังต้องเก็บ (`need[ch]>0`) ซึ่ง `len*sc>S/2-3` (นอกวง) → วาดลูกศรเหลืองที่ขอบวง (`edgeR=S/2-6`) ชี้ออกตามทิศ · **จัดกลุ่มตามมุมทุก 30° เก็บตัวใกล้สุดต่อกลุ่ม** กันรก (เทสต์: 14 ตัวนอกเรดาร์ → 3 ลูกศร) · ยืนยัน pixel เจอเหลืองครบทุกตำแหน่งที่คำนวณ (หมุน heading-up ถูก) — version .60
- ✅ **รอบ 83: มินิแมป heading-up + ไฮไลต์ตัวอักษรคำปัจจุบัน 🧭** — `drawMinimap` เดิม north-up ผู้เล่นลอยตามตำแหน่ง → ใหม่: **จุดผู้เล่นกลางเรดาร์ชี้ขึ้นเสมอ** โลกหมุนรอบตามทิศ (`translate(S/2,S/2); rotate(yaw)` · พล็อตเทียบผู้เล่น `(e-cam)*sc` · clip วงกลมกันล้น) · ตัวอักษร "ยังต้องเก็บ" ของ `words[0]` (หัก inv แล้ว) = เหลืองสด+ขอบขาว ตัวอื่น=เทาจางเล็ก · ยืนยัน: ตัวหน้ากล้องวาดชี้ขึ้น (y≈-27) + pixel 2 สี (278 เหลือง/61 เทา) — version .59
- ✅ **รอบ 82: คนที่ invite กันเห็นคำเดียวกัน (party word sync) 🤝** — publish `cw="en|th"` ใน /world **เฉพาะตอนมีเพื่อนปาร์ตี้จริง** (คนทั่วไปไม่ส่ง → ไม่ผูก rules ใหม่) · หัวหน้า = key น้อยสุดในกลุ่ม `tinvLinked` (ทุกเครื่องคำนวณตรงกัน) · ลูกทีม `syncPartyWord()` (ใน tickPeers ทุกเฟรม) ดันคำหัวหน้าขึ้น `words[0]` → เห็นคำเดียวกัน เก็บตัวอักษรของตัวเอง · `lastSharedDone` กันลูกทีมที่ทำเสร็จก่อนวนซ้ำ (เล่นคำตัวเองรอหัวหน้าเปลี่ยน) · completeWord force `sendPos(true)` ดันคำใหม่ทันที · ⚠️ **letters ยังเป็น local (ตำแหน่งคนละที่)** — "ช่วยกันหา" = ระดับคำ/เป้าหมาย ยังไม่แชร์ตำแหน่งตัวอักษร (ต่อยอดได้: seed spawn ร่วม) · ยืนยัน 4 เคสด้วยการจำลอง peer · **✅ publish rules แล้ว 9 ก.ค. + เทสต์ logic จำลอง peer 8/8 ผ่าน (leader/ลูกทีมตามคำ/guard lastSharedDone/คนทั่วไปไม่ส่ง cw/ดันคำไม่ซ้ำ)** — เหลือทดสอบจริง 2 เครื่อง — version .58
- ✅ **รอบ 81: ตึกโลกเฮลิฯมีหน้าต่าง + probe รูปผนังจริง 🏙️** — เดิมตึกเป็น `BoxGeometry` สีล้วน → เพิ่ม `buildingFacadeTexture(n)` วาดหน้าต่าง procedural (3×3/tile บางบานติดไฟ) `RepeatWrapping` · ตอนสร้างตึก `facade.repeat.set(w/8, h/6)` tile ตามความสูง (ตึกสูง=หลายชั้น) · probe `img/buildings/facade_1..6.png` (seamless) → swap รูปจริงเมื่อโหลดได้ · ลบ `cols` เดิม (1 rnd()/ตึกเท่าเดิม → ผังเมือง seed คงเดิม) · **`PROMPTS_BUILDINGS.md`** 6 แบบ (เน้น seamless tileable) · ยืนยัน scene graph 20 ตึกมี map+repeat+wrap + ภาพ render เห็นหน้าต่างจริง — version .57 · **ค้าง: ผู้ใช้เจนภาพวาง `img/buildings/`**
- 🕹️ **ค้างรอผู้ใช้ตัดสินใจ: multiplayer "คำเดียวกัน/ไม่ซ้ำ"** — พบว่า **letters เป็น local ไม่ sync** (บรรทัด 1090) → ไม่มีการแย่งตัวอักษรจริง · ถ้าจะทำ "คนที่ invite กันเห็นคำเดียวกัน" ต้อง sync คำผ่าน `/world` (เพิ่ม field + rules) — รอผู้ใช้ยืนยัน scope
- ✅ **รอบ 80: คำเป้าหมายใหญ่ทีละคำ กลางบนจอ 🎯🔤** — เลิกลิสต์ 10 คำ/แถบแนวนอน (รอบ 79) → โชว์ `words[0]` เป็นคำใหญ่ chip ตัวโต กลางบน (`#adv-words` repurpose: `top:82` กลางจอ · font `clamp(20,5.5vw,34)`) · ตัวอักษรที่เก็บแล้วไฮไลต์เขียว (`.adv-fch.got`) + คำแปลไทยใต้คำ · จบคำ→แบนเนอร์ฉลอง→เด้ง `words[0]` ถัดไปเอง (logic `tryCompleteWords`/`completeWord` เดิม ไม่แตะ · แค่เปลี่ยน `renderHudWords` เป็นคำเดียว) · top:82 เลี่ยงชน "👻 หนี!"(52)+topbar · เหนือ crosshair/คอนโซล · pointer-events:none ไม่บังนิ้ว · ครอบทั้ง 3 โลก · ยืนยันภาพ+เทสต์จบคำ(FISH→BOOK) — version .56
- ✅ **รอบ 79: ย้ายแผงคำเป็นแถบแนวนอนบน (มือถือ) 📱📋** — เดิมแผงคำ `#adv-words` เป็นลิสต์แนวตั้งซ้าย สูงคลุมครึ่งจอ → บนมือถือแนวนอนจอเตี้ย (เทสต์ 812×375) ทับ **คอนโซลเฮลิฯ** (words b362 vs cockpit t233 = ทับ 129px) + คลุมโซนนิ้วซ้าย · แก้ (scope `.adv-touch` เท่านั้น เดสก์ท็อปคงเดิม): `top:60 left:8 right:140 max-height:30vh` + `flex-wrap` = แถบแนวนอน 2-3 แถวใต้กระดานคะแนน เว้นขวาให้ minimap · ยืนยัน 3 โลก: heli เกจ SPD/ATT/ALT/VS/RPM โล่งไม่โดนบัง · adv/haunt มุมล่างซ้ายโล่งให้จอยสติ๊ก · พ้น crosshair — version .55
- ✅ **รอบ 78: 🐞 fix คันบังคับซ้าย(เดิน)ตายในมือถือทั้ง 3 โลก 📱** — ผู้ใช้เทสต์จริงเจอ: คันซ้ายกดไม่ติดเลย คันขวา(มอง)ปกติ · ต้นตอ: แผงคำ `#adv-words` (`pointer-events:auto` สูงเกือบเต็มจอซ้าย) คลุมโซนหัวแม่มือซ้าย + touchstart มี `#adv-words` ในลิสต์ "แตะโดน=ข้าม" → คันซ้ายไม่เริ่ม · แก้: `#adv-words` เป็น `pointer-events:none` (เป็นแค่ HUD ลิสต์คำ ไม่มีปุ่ม) + เอาออกจากลิสต์ exclude · พิสูจน์ A/B `elementFromPoint` ตรงแผงคำ: เดิมโดน `#adv-words`(บล็อก) → ใหม่ทะลุโดน `adv-canvas`(คันบังคับติด) · handler ร่วมทั้ง adv/haunt/heli แก้ทีเดียวครบ — version .54
- ✅ **รอบ 77: ปล่อยผีเฉพาะตอนภาพโหลดเสร็จ (เลิกโผล่ emoji ตอนเข้าด่าน) 👻⏳** — เดิมผีชุดแรก spawn ก่อน onload เสมอ → เห็น emoji ~0–20 วิ · ใหม่: `probeGhostImages` นับ onload/onerror ครบทุกภาพ → `whenGhostsReady(cb)` · ที่ line ~2447 เลื่อน spawn ไปรอ callback (ยังโหลด=ด่านว่างไว้ก่อน ไม่โผล่ emoji) · กันเหนียว `setTimeout(spawnAll,9000)` เน็ตแย่/ภาพหายก็ยังปล่อยผี(emoji) · `ghostGen` token (บัมพ์ใน `clearEntities`) กัน spawn ค้างปล่อยผีผิดด่านตอนผู้เล่นออกก่อนโหลดเสร็จ · ยืนยันตรรกะ 4 เคสด้วยการจำลอง — version .53
- ✅ **รอบ 76: ผีฟิตสเกลอัตโนมัติจากสัดส่วนภาพจริง 👻📐** — `measureGhostBox()` วัดกรอบตัว (alpha bbox) ตอนโหลด → เก็บ `aspect/fhFrac/belowFrac` ใน `userData` · `applyGhostSize()` คำนวณ `sy=Hf/fhFrac, sx=sy*aspect*squeeze, baseY=sy*(0.5-belowFrac)` · เลิก hardcode รายตัว เหลือ `GHOST_STYLE={2:{h:5.8,squeeze:.6}}` (เปรต) + `GHOST_H_DEFAULT=2.5` · **แก้ `ghost_3`(แม่นาค)+`ghost_5`(ผ้าขาว) ที่โดนบีบเตี้ย~33%** (canvas 1024×1536 เคยใช้สเกลจัตุรัส) ตอนนี้เพรียวตามภาพจริง · เปรตคง look A (3.59×5.99) · ทุกตัวเท้าแตะพื้น · **ครอบภาพผีที่เจนใหม่ในอนาคตอัตโนมัติ** — version .52
- ✅ **รอบ 75: แก้เปรตยืดเบี้ยว 👻** — สเกล `ghost_2` `2.7×6.4`→`3.6×6.0` baseY 3.15→2.96 · ของเดิมยืดแนวตั้ง ~2.4 เท่า (สัดส่วนตัว 2.29→5.42) หัวเป็นไข่ยาว/ตัวเป็นเส้น · ใหม่ H/W~3.8 สูงโย่งเท่าต้นตาลแต่ไม่บิด (ผู้ใช้เลือกเอง จากภาพเทียบ) — version .51 · **แทนที่ด้วยรอบ 76 (auto-fit) แล้ว**
- ✅ **รอบ 74: เปรตตัวสูงพิเศษ + prompt ผี Ju-on 👻** — `ghost_2.png`=เปรต probe เก็บ `userData.gi` · `applyGhostSize()` ยืดตัวที่ใช้ ghost_2 เป็น `2.7×6.4` (สูง ~2.5 เท่า)+baseY 3.15 เท้าอยู่พื้น · bobbing ใช้ `g.baseY` ต่อตัว · `PROMPTS_GHOSTS.md` เขียน prompt ใหม่ทั้ง 5 เป็นสไตล์หนัง Ju-on (โฟโต้เรียล ตาโบ๋ดำ ผิวซีดเทา โทนหม่น) เพราะ 1-4 เดิมออกมาการ์ตูน — version .50
- ✅ **รอบ 73: jump scare ภาพผีไทยเต็มจอ 👻** — โดนผีจับ สุ่มภาพจาก `img/ghosts/` (ที่ probe แล้ว) พุ่งเต็มจอแทน emoji · ไม่มีภาพ/โหลดไม่ทัน=fallback emoji เดิม · `ghostScareSrc()`+class `has-img` (CSS สลับ img↔span) · haptic แรงขึ้น — version .50 · **ค้าง: ผู้ใช้เจนภาพ + (ถ้าต้องการ) เสียง Suno `PROMPTS_SOUND.md` โค้ดพร้อมรับหมดแล้ว**
- ✅ **รอบ 72: ภาพผีไทยในโลก 3D 👻** — โลกผีสิง probe `img/ghosts/ghost_1..5.png` เอง มีกี่ภาพสุ่มใช้ ไม่มีใช้ emoji เดิม · ผีสลับภาพจริงตอน respawn (20 วิ) · `PROMPTS_GHOSTS.md` prompt ผีไทย 5 ตัว (กระสือ/เปรต/แม่นาค/ปอบ/ผีผ้าขาว) หลอนแบบไทย เรืองแสง no blood เหมาะเด็ก — version .49 · **ค้าง: ผู้ใช้เจนภาพวาง `img/ghosts/`**
- ✅ **รอบ 70: สถิติสายฟ้าแลบ + เข็มสายฟ้า ⚡** — นับสะสม (โชว์หน้าสถิติ) · ครบ 5/15/30 → เข็ม ⚡/🌩️/⛈️ ติดท้ายชื่อใน map ต่อจากเข็มนักบิน (ไม่มี field/rules ใหม่) — version .48
- ✅ **รอบ 71: เสียงชื่อตัวอักษร 🔠 + ผู้ทดสอบน้องโตเต็มวัย 🧪** — เก็บตัวอักษรโลก 3D (ทั้ง 3 โลก) → อ่าน "เอ บี ซี" (`sound/letters/` 26 ไฟล์ Neural · `speakLetter` util.js) · testerBoost อัปเกรด: สัตว์ทุกตัวของผู้ทดสอบโตเต็มวัย Lv.3 ทันที (login+หลังซื้อ — ตั๋ว 3D ปลดล็อก) — version .47
- ✅ **รอบ 69: badge เลขป่วย 💊②+เสียงหวอ 🚨** — badge บนปุ่มรักษาบอกป่วยกี่ตัว · น้องเพิ่งล้มป่วย (careTick/พิษ) → หวอเบา 2 รอบ+toast ชื่อน้อง เด็กรู้ตัวไวแม้กำลังเล่น — version .46
- ✅ **รอบ 68: นักบินตอบวิทยุ 🎙️** — หอพูดจบมีปุ่มตอบ Roger!/Copy that!/Wilco! พร้อมคำแปลไทย + แถบชวน "ลองพูดตามดังๆ" · แตะแล้วเสียงนักบิน (โทนเด็ก) ตอบจริง หอสุ่มตอบปิดท้าย — version .45
- ✅ **รอบ 67: เอฟเฟกต์สายฟ้าแลบ ⚡** — จับคู่ครบ 4 คู่ไม่พลาดใน ≤5 วิ / สอบถูกทุกข้อ ข้อละ ≤5 วิ ("สอบสายฟ้า") → ฟ้าผ่า canvas เต็มจอ+จอสั่น+เสียง spark สังเคราะห์ (อัปเกรดได้ `sound/spark.mp3` — prompt ใน PROMPTS_SOUND.md ข้อ 4) — version .44
- ✅ **รอบ 66: หอบังคับ ATC เป็นอังกฤษล้วน 📻🇬🇧** — เสียงพูด+ข้อความ Aviation English (เสียง en ดีสุดของเครื่องผ่าน pickSpeakVoice) — version .43
- ✅ **รอบ 65: หอบังคับการบิน 📻** — วิทยุ ATC พูดไทย (Web Speech) + squelch "ซ่า-คลิก" + ข้อความเขียวเรือง: อนุญาตขึ้นบิน/รายงานลม-ทัศนวิสัย-ความสูงทุก 45–75 วิ/เตือนเพื่อนร่วมน่านฟ้า/ชมลงจอดนุ่ม + ครูฝึกลุ้น "อีกคำเดียวได้เข็ม!" — version .42
- ✅ **รอบ 64: ปุ่มรักษาด่วน 💊** — บนสุดรางเมนูซ้าย กดได้เฉพาะมีน้องป่วย (จาง↔กะพริบแดง) กดปุ่มเดียวสลับแท็บ+รักษาให้เลย — version .41
- ✅ **รอบ 63: แผงสถานะใส sci-fi 2 ฝั่งขนาบตัวน้อง 🛸** — เลิกทับตัวสัตว์ ซ้าย=ข้อมูล ขวา=การดูแล กระจกฟ้าเรือง+scanline (ui.js+lobby.css) — version .40
- ✅ **รอบ 62: ใบอนุญาตนักบิน 🎖️** — สตรีคประกอบคำไม่ชน (สะสมข้ามรอบ) ครบ 5/15/30 → เข็ม 🥉🥈🥇 ติดท้ายชื่อทุกโลก (ไม่มี field/rules ใหม่) · ชน=สตรีคขาด เข็มไม่หาย — version .39
- ✅ **รอบ 61: เกจห้องนักบินเข็มขยับจริง 🎛️** — SPD/ATT เส้นขอบฟ้าเทียม (ฟ้า-ดินเอียงตามก้มเงยจริง)/ALT/V-S/RPM วาดสดทุกเฟรม + ภาพ cockpit ผู้ใช้เข้า repo ใช้จริงแล้ว — version .39
- ✅ **รอบ 60: ตัด scrollbar ระดับเพจถาวร 📵** — `html,body{overflow:hidden}` + `min-height:100dvh` (lobby.css) — เพจพอดีจอเสมอ ทุก screen scroll ภายในตัวเอง — version .38
- ✅ **รอบ 59: ฉาก Rank Up พอดีจอทุกขนาด 🎖️** — fix ล้นจอแนวนอน (หัวข้อ/ปุ่มโดนตัด) ด้วย clamp+vmin ครอบ 3 ฉากที่ใช้ CSS ร่วม (อัปแรงค์/ของขวัญ/ของสะสม) — version .37
- ✅ **รอบ 58: ป้ายโฆษณาบนยอดตึก 📢** — 10 ป้ายพื้นหลังต่างกัน "ติดต่อโฆษณา โทร 064-357 6645" + เลขป้ายกำกับ · วาง `img/ads/ad_<n>.png` = โฆษณาลูกค้าขึ้นแทน · ผังเมือง seed คงที่ (ลูกค้าจองเลขป้ายได้ + multiplayer เห็นเมืองเดียวกัน) — version .36
- ✅ **รอบ 57: เตือนภัยใกล้ชนในห้องนักบิน ⚠️** — บี๊บถี่ขึ้น 3 ระดับตามระยะตึก + ไฟแดงกะพริบใต้หน้าปัด + PULL UP ตอนดิ่งเร็วใกล้พื้น (บินเหนือยอดตึก=ไม่เตือน) — version .35
- ✅ **รอบ 56: บัญชีผู้ทดสอบเกม 🧪** — `TESTER_EMAILS` (auth.js · ตอนนี้มี sumpajitshami@gmail.com) login แล้วเหรียญ<60,000 เติมเป็น 60,000 อัตโนมัติ (พอตั๋ว 3D ครบ 3 โลก) + toast แจ้ง — version .34
- ✅ **รอบ 55: โลก 3D อ่านออกเสียงคำที่ผสมสำเร็จ 🔊** — `completeWord` (adventure3d.js) เรียก `speakWord` หลังแตรฉลอง 0.7 วิ — ครอบทั้ง 3 โลกอัตโนมัติ (โค้ดร่วม) — version .33
- ✅ **รอบ 54: เสียงเครื่องยนต์เฮลิฯ สมจริง 🚁🔊** — ซีเควนซ์สตาร์ทเครื่อง (บินไม่ได้จนใบพัดเต็มรอบ ~3.6 วิ) + โมเดล RPM แรงเฉื่อยเร่ง-เบาเครื่อง + รองรับไฟล์ Suno 3 ไฟล์ (start/rotor/rotor_high crossfade — prompt ใน `PROMPTS_HELI.md` 2.1–2.3) — version .32
- ✅ **รอบ 53: เสียงคำศัพท์ระดับ Edge ทุกเบราว์เซอร์ 🎙️** — เจน MP3 เสียง Neural (en-US-JennyNeural) ครบ 400 คำด้วย `tools/gen_word_audio.py` (edge-tts ฟรี) → `sound/words/` 5.1MB · `speakWord` เล่นไฟล์ก่อน fallback Web Speech · เพิ่มคำใน vocab.js แล้วรันสคริปต์ซ้ำได้ — version .32 (เลขร่วมกับงาน 🚁 session คู่ขนาน)
- ✅ **รอบ 52: โลกเฮลิคอปเตอร์ Bell 🚁 (การ์ดที่ 3)** — ตั๋ว 15,000 · cockpit view + ฟิสิกส์บิน · ตัวอักษรบนยอดตึก ต้องลงจอดเก็บ · 30🪙/คำ · เสียงใบพัดสังเคราะห์ + `PROMPTS_HELI.md` (ภาพ cockpit+เสียง Suno) · multiplayer ครบ — version .30 · **✅ rules (map `heli`+field `y`) publish แล้ว 9 ก.ค.**
- ✅ **รอบ 51: เสียงอ่านคำศัพท์อังกฤษ 🔊** — แตะการ์ดอังกฤษในเกมจับคู่ / การ์ดโจทย์ตอนสอบ (มีไอคอน 🔊) → อ่านออกเสียง Web Speech API เลือกเสียง human สุดที่เครื่องมี (`speakWord` util.js) — version .29
- ✅ **รอบ 50: คู่มือครูในเกม 👩‍🏫** — ปุ่มในหน้าตั้งค่า (เห็นเฉพาะบัญชีครู) สรุปเครื่องมือคุมห้อง 7 หัวข้อ + สูตรจัดแข่งในคาบ · fix วิธีเล่นที่ยังเขียน "โลก 3D กำลังก่อสร้าง" — version .28
- ✅ **รอบ 49: พิธีประกาศแชมป์ใน map 🏁** — ครูกด "จบรอบแข่ง" → โพเดียม 🥇🥈🥉 ทุกเครื่อง + แตรฉลอง + โบนัส 100/50/25 + คะแนนรีเซ็ตเริ่มรอบใหม่ (`/class/<map>/podium` — rules publish แล้ว) — version .27
- ✅ **รอบ 48: toast "🎉 เหรียญพอรับน้อง...แล้ว!" เด้งทันทีที่เหรียญข้ามเส้นราคาน้องที่ยังไม่มี** (hook ใน `addCoins` state.js — ครอบทุกทางได้เหรียญ) — version .26
- ✅ **รอบ 47: การ์ดร้านสัตว์เลี้ยงโชว์เป้าหมาย "ขาดอีก 🪙X ≈ เล่นอีก Y คำ"** (เฉพาะการ์ดที่เหรียญไม่พอ · ฐาน 10🪙/คำ) — version .25
- ✅ **รอบ 46: กระดานคะแนนสดใน map 🏆** — มุมซ้ายบน จัดอันดับใครประกอบคำเยอะสุดรอบนี้ (me+เพื่อน · 👑 คนนำ · field `w` ใน `/world`) — version .24 · **rules ชุดเต็มรอ publish**
- ✅ **รอบ 45: หน้าร้านสัตว์เลี้ยงพอดีจอไม่มี scrollbar (fix margin collapse `#app{flow-root}` — ตัด scrollbar 8–12px ทั้งเว็บ) + ลิงก์ 🎮 เข้าเล่นเกมใต้เหรียญ** — version .23
- ✅ **รอบ 44: ไอคอน 🎤 เหนือหัวคนเปิดไมค์ + ปุ่มครูปิดเสียงทั้งห้อง 👩‍🏫** — บัญชีครู=`TEACHER_EMAILS` ใน auth.js · โซน `/class` ใหม่ + `/world` เพิ่ม `m` — version .22 · **rules ชุดเต็มรอ publish**
- ✅ **รอบ 43: Voice chat 🎤 + quick chat + bubble ธีมหลอน** — WebRTC P2P (signaling `/rtc`) · ปุ่ม 🎤(default ปิด)/🔊/🌐-👥(เฉพาะเพื่อน invite) · เสียงเบาตามระยะ · ชิปแตะเดียวส่ง 6 ข้อความ — version .21 · **rules ทั้งชุด (/world+/tinv+/rtc) รอผู้ใช้ publish + เสียงจริงต้องเทสต์ 2 เครื่อง**
- ✅ **รอบ 42: แชทลอยหัวใน map แบบ Roblox 💬** — ปุ่ม 💬/Enter พิมพ์ ≤60 ตัว กรองคำหยาบ โชว์เหนือหัวเพื่อน 5 วิ (field c/ct ใน `/world` เดิม — **rules /world ต้องเพิ่ม c/ct ด้วย ก้อนเต็มใน RULES.md**) — version .20
- ✅ **รอบ 41: โลกผีสิงกลางคืน 👻 + multiplayer 2 โลก + ชวนเพื่อนเงินคืน 2,000** — ตั๋วผี 10,000 (ต้องมีตั๋วแรกก่อน) 25🪙/คำ · ผี 8 ตัวโผล่ 20 วิย้ายที่ สู้ไม่ได้ต้องหนี โดนจับ=jump scare+game over · เสียงหลอนสังเคราะห์ (อัปเกรดได้ด้วยไฟล์ Suno — `PROMPTS_SOUND.md`) · ผู้เล่นอื่นโผล่ใน map (`/world`) · คำชวน (`/tinv`) เจอกันจริงรับคนละ 2,000 — version .19 · **⚠️ รอผู้ใช้ publish rules โซน /world+/tinv (`handoff/RULES.md`) + ทดสอบจริง**
- ✅ **รอบ 40: ข้อ 8 โลกผจญภัย 3D เสร็จ!** — Three.js ฝัง repo (`js/vendor/`) · first-person เก็บตัวอักษรประกอบคำ 15🪙/คำ · minimap เรดาร์ · monster ยิงสู้ได้ · KO→รักษา 1,000 · เข้าจากการ์ดตั๋ว — version .18 · **รอผู้ใช้ทดสอบจริงบน Pages (เดสก์ท็อป+มือถือ touch)**
- ✅ **รอบ 39: ภาพชุดตัวละคร/รูปร่างครบ 11/11** (dragon 3 ภาพสุดท้ายเข้า repo) — version .17
- ✅ **รอบ 38: ตัวละครผู้เลี้ยงโผล่ในฉากเกม (เด้งเชียร์ตอนตอบถูก) + หัวการ์ดสถิติ/สรุปส่งครู** — version .16
- ✅ **รอบ 37: เลือกตัวละครผู้เลี้ยง (ข้อ 4) + การ์ดตั๋วโลกผจญภัย (ข้อ 7) + 🐞 fix sw.js cache 404 รูป** (ภาพเจนใหม่ไม่โผล่ให้ผู้เล่นเก่า — CACHE_VERSION v2) — version .15
- ✅ **รอบ 36: ระบบรูปร่างตามคุณภาพการกิน (ข้อ 5.2 ระบบจริง)** — กินดี 3 มื้อติด=ล่ำ 💪 (+2 EXP/คำ) · ของโทษ 3 มื้อติด=อ้วน 🍩 · อดข้าว 2 มื้อ=ผอมโซ 🦴 · ภาพ 9 ร่างวางใน `img/` เกมโชว์เอง (**ผู้ใช้กำลังทยอยเจนภาพจาก `PROMPTS_CHARACTERS.md`**) — version .14
- ✅ **รอบ 35: prompt กลุ่ม C (ข้อ 4+5.2 → `PROMPTS_CHARACTERS.md` 11 ภาพ รอผู้ใช้เจน) + 🛡️ ควิซอาหารปลอดภัย** (ปุ่มเขียวแถบล่าง lobby · รางวัลวันละรอบ +10/ข้อ +25 ครบ 5) — version .13
- ✅ **รอบ 34: คิว 7725691507 ข้อ 5.1 แยกอาหารคน/สัตว์ + พิษสะสม เสร็จ** (version .12) — เมนู 2 ชุดตามชนิดสัตว์ · เตือนก่อนป้อนอาหารโทษ · บาร์พิษไม่ลดเอง เต็ม 100 ป่วยทันที · ขับพิษ 1,000 — **รอผู้ใช้ทดสอบจริงบน Pages ทั้ง 2 รอบ**
- ✅ บั๊ก "ของขวัญโดนบัง" แก้เสร็จ + ผู้ใช้ทดสอบจริงยืนยันแล้ว (รอบ 31, version .2) · **ไม่มีบั๊กค้าง**
- ✅ เกม feature-complete · item 0 สังคมออนไลน์เสร็จครบ 0.1–0.5 (login/ชื่อ/เพื่อน/แชท/ของขวัญ)
- ✅ **rules ชุดเต็ม publish แล้ว 8 ก.ค.** (ครบทุกโซนถึงรอบ 49 — ตรวจ REST แล้ว) · ⚠️ **ค้างฝั่งผู้ใช้:** ทดสอบจริง 2 บัญชี/2 เครื่อง (ของขวัญ + โลก 3D ทุกระบบ online — checklist ใน TASKS.md) + (ถ้าต้องการ) เจนเสียงผีจาก Suno (`PROMPTS_SOUND.md`)
- 🎯 งานถัดไป: เลือกจาก `handoff/TASKS.md` → backlog (item 8 รายได้ออนไลน์ · item 2 ตลาดออนไลน์ · item 3 daily quest)

## 🧪 testkit — mock login เทสต์ preview (copy วางใน 1 eval ได้เลย)
เกมบังคับ Google login (login จริงใน preview ไม่ได้) — ก้อนนี้จำลองครบ:
```js
window.authFetchCloud=()=>Promise.resolve(null);      // cloud ว่าง (หรือคืน {data,at} จำลองเซฟ)
window.authWriteCloud=()=>Promise.resolve();          // กันเขียน DB จริง
window.authDeleteCloud=()=>Promise.resolve();
window.authWriteProfileName=()=>Promise.resolve();    // กันเขียน /users/<uid>/profile/name
window.onlineStart=()=>{};                            // กัน presence/leaderboard เขียนจริง
authOnLogin({uid:'test1',email:'t@test.com'});        // → เข้าหน้าลงทะเบียน เล่นต่อได้
```
- **เพื่อน/แชท/ของขวัญ:** ตั้ง `Online.ready=true` + fake `Online.db={ref:path=>({...})}` (firebase SDK โหลดจริงบน localhost ได้ · ServerValue.TIMESTAMP ใช้ได้) — fake db ต้องรองรับ push/on/off/once/update/orderByKey/limitToLast/get/set/remove/child
- **จบงานล้างเสมอ:** `localStorage.removeItem('petVocabAdventure_v1')` + reload คืนหน้า login

## 🖥️ preview gotchas (เจอซ้ำทุกรอบ — อ่านก่อนเทสต์)
- server ชื่อ **`english-pet-game`** (python http.server, config ใน `.claude/launch.json`) · **ไม่มี Node มีแต่ Python 3.12**
- **จอ preview เป็นจัตุรัส → ถูกมองเป็น portrait → เกมเด้ง overlay "หมุนจอ" (`#rotate-overlay`) มาบัง** · แก้: `preview_resize` เป็น **landscape** (เช่น 1000×640 หรือ 1280×720) ก่อนเสมอ
- **screenshot/`elementFromPoint` มี scale mismatch เชื่อไม่ได้** (แอพ render เล็กมุมบนซ้าย) → วัดขนาด/ตำแหน่งด้วย **`getBoundingClientRect`** เท่านั้น
- **http.server cache เหนียวมาก** (แก้ js/css แล้ว reload ยังได้ของเก่า) → force ใหม่ด้วย eval:
  `await fetch('css/style.css',{cache:'reload'}); location.reload();`
- **Pages build หน่วง 2–5 นาที** หลัง push → เช็ก live ก่อนบอกผู้ใช้: `curl -s ".../version.json?t=$(date +%s)"`

## 💾 commit / deploy
- git identity = iamsuperrich2025 / freddommun@gmail.com · push HTTPS credential ในเครื่องใช้ได้ (ไม่มี gh CLI)
- **`git add` เฉพาะไฟล์ที่แก้ · ห้าม `git add -A`** (มี `js/data/vocab/` untracked ของงาน Sonnet ค้าง — ห้ามแตะ)
- **ทุก push บัมพ์ `version.json`** (ผู้ใช้ที่เปิดค้างถึงเห็นแถบ "มีเวอร์ชันใหม่") · commit message ลงท้าย `Co-Authored-By: Claude ...`

## 🗣️ ตอบผู้ใช้
- ไทย สุภาพ ลงท้าย "ครับ" · ใช้ตาราง/emoji ให้อ่านง่าย · กระชับ ไม่เล่าโค้ดยาว · **ท้ายคำตอบเสนอไอเดียต่อยอด** (ผู้ใช้เป็นครู/ผู้ปกครองไทย)

## 🗺️ เปิดไฟล์ไหนเมื่อไหร่ (path เต็ม — เปิดเฉพาะตอนต้องใช้)
| จะทำอะไร | เปิดไฟล์ |
|----------|----------|
| งานถัดไป / อาการบั๊ก(ยืนยัน) vs เดา(ยังไม่พิสูจน์) / backlog สรุป | `C:\Users\rober\english-pet-game\handoff\TASKS.md` |
| สภาพแวดล้อม + ข้อควรระวังหายาก (image probe, ห้ามหลายชุดแต่งตัว, ลิขสิทธิ์) | `C:\Users\rober\english-pet-game\handoff\NOTES.md` |
| แตะ Firebase / publish rules (ส่งเต็มทั้งหน้าเสมอ) | `C:\Users\rober\english-pet-game\handoff\RULES.md` |
| หาไฟล์/ฟังก์ชัน/โครงสร้างโค้ด | `C:\Users\rober\english-pet-game\handoff\ARCHITECTURE.md` |
| แก้ระบบเกม (สัตว์/บ้าน/บิล/แรงค์/โรงงาน/ตลาด/ออนไลน์/ของขวัญ) | `C:\Users\rober\english-pet-game\handoff\GAME_RULES.md` |
| ดูสเปก backlog เต็ม | `C:\Users\rober\english-pet-game\handoff\BACKLOG.md` |
| รอบเก่า 1–30 ทำอะไรไปแล้ว | `C:\Users\rober\english-pet-game\handoff\HISTORY.md` |

## 🔗 ลิงก์
- **เกม (live): https://vocabworld.web.app** — Firebase Hosting site `vocabworld` ในโปรเจกต์ `english-pet-game` (ย้ายรอบ 134 หลังบัญชี GitHub โดน flag ทำ Pages 404 ทั้งเว็บ · deploy: `bash tools/deploy_firebase.sh` — CLI login ค้างในเครื่องแล้ว · Node พกพา `C:\Users\rober\bin\node` + firebase-tools ลงแล้ว)
- ~~https://iamsuperrich2025.github.io/english-pet-game/~~ (GitHub Pages — ใช้ไม่ได้จนกว่าบัญชีปลด flag · repo ยังใช้เก็บโค้ด push ได้ปกติ)
- repo: `iamsuperrich2025/english-pet-game` (branch `main`)
- งานมอบ Sonnet: `TASK_VOCAB_SONNET.md` (คำศัพท์) · `TASK_DICTIONARY_SONNET.md` (พจนานุกรม)
