# PROMPT_PICDICT_BOOKMARK_SONNET.md — ที่คั่นหนังสือ Picture Dictionary (งานมอบ Sonnet 5)

> **ผู้ทำ: Sonnet 5 ใน session แยก** · เริ่มงาน: อ่านไฟล์นี้ **ไฟล์เดียว** — ห้ามบูตตาม skill vocab-world / ห้ามอ่าน HANDOFF (เปลือง token)
> 📂 ราก `C:\Users\rober\english-pet-game\` · working dir = `C:\Users\rober` → เปิดไฟล์ใช้ path เต็มเสมอ

## เป้าหมาย

หนังสือ Picture Dictionary (`js/picdict.js` · จอ `#screen-picdict`) มี 46 แผ่น 48 หน้า
ตอนนี้ **ออกจากหนังสือแล้วกลับเข้ามาใหม่ ต้องพลิกหาหน้าเดิมเอง** และไม่มีที่เก็บ "หน้าที่ชอบ"

เพิ่ม 2 อย่าง:
1. **🔖 จำหน้าล่าสุดอัตโนมัติ** — เปิดหนังสือครั้งถัดไปมาที่หน้าที่อ่านค้างไว้ (ไม่ใช่สารบัญ)
2. **⭐ ที่คั่นหน้าโปรด** — ปุ่มติดดาวหน้าไหนก็ได้ · ดูรายการหน้าที่ติดดาวแล้วกดกระโดดไปได้

## สภาพโค้ดปัจจุบัน (อ่านให้ครบก่อนแก้ — จะได้ไม่ต้องไล่หาเอง)

ทั้งหมดอยู่ใน `js/picdict.js` (ไฟล์เดียว ~530 บรรทัด · IIFE ปิดตัวเอง) + `css/picdict.css`

| ของที่มีอยู่แล้ว | ใช้ยังไง |
|---|---|
| `pd.pages[]` | รายการหน้า: `{type:'toc',part}` 2 ใบแรก · `{type:'sheet',file,en,th,icon,num}` · `{type:'end'}` |
| `pd.s` | เลข "หน้าคู่" (spread) ที่กางอยู่ — หน้าซ้าย = `pages[pd.s*2]` ขวา = `pages[pd.s*2+1]` |
| `goTo(spread)` | พลิกไปหน้าคู่ที่ต้องการ (มีแอนิเมชันพลิก ~750ms) |
| `renderSpread()` | วาดหน้าคู่ปัจจุบันใหม่ทั้ง 2 หน้า (เรียกทุกครั้งที่พลิกเสร็จ/จอเปลี่ยนขนาด) |
| `open()` | เปิดจอหนังสือ (ปุ่ม `#btn-picdict` ในล็อบบี้เรียกตัวนี้) |
| `pd.opened` | เคยผ่านหน้าปกเข้ามาแล้วหรือยัง (ยังไม่ผ่าน = โชว์ปก `#pd-closed`) |
| `qz.on` | โหมดครูถามศัพท์กำลังเล่นอยู่ (รอบ 994) |
| ปุ่มมุมบนขวา | `.pd-toc-btn` (📑 สารบัญ) และ `.pd-quiz-btn` (🎧 ครูถามศัพท์) — วาง `position:absolute` |

**เซฟของเกม:** ใช้ `state.<ฟิลด์>` แล้วเรียก `saveState()` (มีอยู่แล้วทั้งคู่ ไม่ต้องสร้างระบบเซฟใหม่)
เซฟลง cloud/localStorage ให้เองอัตโนมัติ — **ห้ามใช้ `localStorage` ตรง ๆ**

## สิ่งที่ต้องทำ

### 1. จำหน้าล่าสุด
- ตอนพลิกหน้าเสร็จ (ใน `renderSpread()` หรือหลัง `flipTo` จบ) → `state.pdLast = pd.s; saveState();`
  · **ยกเว้นตอน `qz.on`** (กำลังเล่นโหมดครูถาม ไม่ต้องจำ — เดี๋ยวหน้าเด้งไปมา)
- ตอน `open()` ถ้า `pd.opened` ยังไม่เคยเปิดในรอบนี้ → ตั้ง `pd.s = state.pdLast || 0` ก่อนวาด
- ค่าที่อ่านมาต้องอยู่ในช่วง `0..maxSpread()` (ถ้าเกิน = หนังสือเปลี่ยนจำนวนหน้า ให้ถอยเป็น 0)

### 2. ที่คั่นหน้าโปรด
- เก็บใน `state.pdMarks = [เลขหน้า num ของแผ่น...]` (ใช้ `pg.num` ของ `{type:'sheet'}` ไม่ใช่เลข spread — ปลอดภัยกว่าถ้าลำดับหน้าเปลี่ยน)
- **ปุ่มติดดาวบนหน้ากระดาษ**: เพิ่มในเนื้อหาหน้าแผ่น (ฟังก์ชัน `pageHTML()` ส่วน "หน้าแผ่นภาพ") เช่นมุมขวาบนของหน้า
  ⭐ = ติดแล้ว · ☆ = ยังไม่ติด · กดสลับได้ แล้ว `saveState()` + อัปเดตหน้าตาปุ่มทันที (อย่าวาดหน้าใหม่ทั้งใบ ภาพจะกระพริบ)
  ⚠️ ปุ่มต้อง `ev.stopPropagation()` และ **ห้ามวางทับตารางช่องคลิกคำศัพท์** (`.pd-cells` วางทับภาพเต็มพื้นที่ — ปุ่มต้องอยู่นอกกรอบภาพ เช่นแถวหัวเรื่อง `.pd-ph` หรือแถบเลขหน้า `.pd-pno`)
- **ปุ่มดูรายการ**: เพิ่มปุ่ม `🔖 ที่คั่น` มุมบน (แถวเดียวกับ 📑 สารบัญ) → เปิดแผงเล็ก ๆ ลอยกลางจอ
  แสดงชื่อไทยของหน้าที่ติดดาวทั้งหมด · แตะแล้ว `goTo()` ไปหน้านั้น + ปิดแผง · มีปุ่มปิด
  ยังไม่มีที่คั่นเลย → บอกว่า "ยังไม่มีหน้าที่ติดดาว — กด ☆ บนหน้าที่ชอบได้เลย"
- ระหว่างโหมดครูถาม (`qz.on`) ให้ **ซ่อนปุ่ม 🔖 เหมือนที่ซ่อน 📑 สารบัญ** (มีกฎ CSS `#screen-picdict.quiz .pd-toc-btn{display:none}` อยู่แล้ว เติมตัวใหม่เข้าไปในกฎเดียวกัน)

## ⛔ กฎเหล็ก

1. แก้ได้ 3 ไฟล์เท่านั้น: `js/picdict.js` · `css/picdict.css` · ตารางท้ายไฟล์นี้ · **ห้ามแตะไฟล์อื่นทุกกรณี**
   (ห้ามแตะ `js/data/picdict_words.js` และ `js/data/picdict.js` — มี session อื่นดูแลอยู่)
2. **ก่อนเริ่มงานให้ `git pull` เสมอ** (session อื่นแก้ `js/picdict.js` อยู่บ่อย ๆ) · ก่อน commit ให้ `git log --oneline -3` ดูว่ามีใครแก้ไฟล์เดียวกันแทรกเข้ามาไหม
3. **ห้ามแตะโค้ดโหมดครูถาม (โซน `qz`) และระบบพลิกหน้า (`flipTo`)** — ทำงานถูกอยู่แล้ว ผ่านการทดสอบมาแล้ว
4. **จอเตี้ย 812×375 ต้องไม่มี scrollbar** (กฎถาวรของผู้ใช้) — ทดสอบด้วย `sec.scrollHeight === sec.clientHeight` และแผงที่คั่นต้องอยู่ในจอครบทั้งใบ
5. ทดสอบใน preview จริงก่อน commit (ห้ามเดา):
   ```
   เปิด preview → mock login → ลงทะเบียน → เข้าหนังสือ
   window.authFetchCloud=()=>Promise.resolve(null); window.authWriteCloud=()=>Promise.resolve();
   window.authDeleteCloud=()=>Promise.resolve(); window.authWriteProfileName=()=>Promise.resolve();
   window.onlineStart=()=>{}; authOnLogin({uid:'test1',email:'t@test.com'});
   ```
   ต้องพิสูจน์: พลิกไปหน้า X → ออก → เข้าใหม่ = อยู่หน้า X ✓ ติดดาว 2 หน้า → รายการขึ้นครบ กดแล้วไปถูกหน้า ✓
   ถอดดาวแล้วหายจากรายการ ✓ โหมดครูถามยังเล่นได้เหมือนเดิม ✓ console ไม่มี error ✓ 812×375 ไม่มี scroll ✓
   จบแล้วล้าง `localStorage.removeItem('petVocabAdventure_v1')`
6. `node --check` ต้องผ่านก่อน commit: `"C:\Users\rober\bin\node\node.exe" --check js/picdict.js`
7. commit + push แบบ pin pathspec (**ห้าม `git add -A`**):
   ```
   git add js/picdict.js css/picdict.css PROMPT_PICDICT_BOOKMARK_SONNET.md
   git commit -m "picdict: ที่คั่นหนังสือ + จำหน้าล่าสุด" -- js/picdict.js css/picdict.css PROMPT_PICDICT_BOOKMARK_SONNET.md
   git push
   ```
   ⚠️ **ไม่ต้อง deploy** — รอบถัดไปของ session หลัก deploy ให้เอง

## 📊 ตารางความคืบหน้า (อัปเดตก่อน commit)

| สถานะ | รายละเอียด |
|---|---|
| ⬜ ยังไม่เริ่ม | จำหน้าล่าสุด (`state.pdLast`) · ที่คั่นหน้าโปรด (`state.pdMarks`) |
